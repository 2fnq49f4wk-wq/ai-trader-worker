> ## ⚠️ 결함 목록 — `docs/OPEN-DEFECTS.md`
> 미해결 결함과 **V33.348 에서 고친 것**이 함께 정리돼 있다. 작업 전 반드시 확인할 것.
> - 미해결·치명: `A-1` 시간외 안전장치가 실제 경로에 안 걸림.
> - 고쳤지만 한 번 더 확인: `D-3` D1 재시도 매수 이중기록(과거 중복 행 조회 필요) ·
>   `D-2` 한국 매도세 연도별 · `C-3`·`C-4` D1 부하 · `B-7` 반사실 라벨 진입정렬 · `F-1`·`F-4`.
> - 고쳤지만 실효는 재수확 때: `B-8` 수확 표본 ts 가 가짜 달력(featVer 상향 시점은 사람이 정한다).
> - 고친 항목은 그 파일에 해결 표시 + 커밋해시를 남길 것.

# AI 작업 인계 상태

> 이 문서는 Claude Code와 Codex가 토큰 소진·세션 중단 후에도 커밋 경계에서 안전하게 이어가기 위한 체크포인트다.

## Current handoff

- Status: **V33.416 — GBDT 가 살아났다 · 그리고 ★워커 시계는 계산 중에 안 움직인다★.**
  ✓ "GBDT 모델 없음" 해결 — trees=★184★ · 하한 ★64.6%★(블록 7개) · wGbdt ★0.7861 신뢰★ ·
    발언점도 무실력 59.4%+여유 문턱 60.9% 를 넘겼다(V33.414 기저율 보정 뒤 숫자다).
    적재 7초 — 앞 회차의 300초는 V33.414 행 상한이 이미 잡았다.
  BF-2 ★워커의 Date.now() 는 순수 계산 중에 안 움직인다★ (I/O 때만 밀린다)
    시간[적재 7s · CV ★0s★ · 최종 ★0s★] — CV 가 나무 ~160그루를 세우고도 0ms 다.
    ★계측이 틀린 게 아니다.★ 그래서 ★이 저장소의 CPU 예산 가드는 순수 계산을 못 막는다.★
    나무 0개 사고가 정확히 그 모양이었다 — 적재(I/O) 300초가 시계를 deadline 너머로
    밀었고, 최종 적합은 첫 줄에서 break 했다(계산이 느려서가 아니다).
    V33.415 의 finalMinMs 가 듣는 이유도 같다 — ★Date.now() 기준으로 다시 잡기★ 때문이다.
    → 로그에 "I/O기준 · 0s = 계측 불가, 미실행 아님" 을 못 박았다.
  BF-1 고르는 쪽 날짜 창이 ★비어 있었다★(0/7555) — 수확·소급 적재는 날짜별로 고르지 않다.
    ★고르는 쪽은 날짜가 필요 없다★(임계값 하나를 고를 뿐) → 채점 쪽은 날짜로,
    고르는 쪽은 ★경계 바로 앞의 행★ 을 세어서. 빌 수가 없다.
    ※ ★같은 결론에 이번이 세 번째다★(V33.412 발언점 · V33.413 _needA · 여기).
      "블록이 필요한 건 재는 쪽뿐" 을 한 군데 적어 뒀다면 두 번을 아꼈다.
  게이트 **167종 전부 통과** · 돌연변이 F1~F5.
  ★다음 회차★: ① 달력 분할이 ★걸리는가★(걸리면 홀드아웃 82일 → 60일·6블록으로 정돈된다 —
    지금 행 기반 82일/7블록은 ★우연히★ 측정 가능했던 것이다)
    ② DNN·FLOW·XALPHA·STACK 은 그대로 — 남은 지렛대는 ★라벨·지평★ 하나다.

- 직전: **V33.415 — 화면이 말한 둘을 고쳤다: GBDT "모델 없음" · TOP MOVERS 에 미국 없음.**
  BE-1 "GBDT 모델 없음" 은 ★내 앞 판이 만든 것★ 이다. 화면의 gStored 는 ★gtrees > 0★ 로
    판정하므로 V33.413 이 저장한 trees=0 모델이 "모델 없음" 으로 읽혔다(판정은 맞다).
    V33.414 가 ★저장★ 은 막았지만 ★막기만 하면 영원히 모델 없음★ 이다.
    ★막는 것과 되게 하는 것은 다른 일이다.★
    실측 [GBDT] trees=0 · 단계 ★326초★(예산 90초). _gbdtFit 의 마감 검사는 루프 맨 위라,
    최종 적합 시작 시 deadline 이 지나 있으면 한 그루도 못 세운다.
    → 최종 적합에 ★Date.now() 기준★ 최소 시간 보장(finalMinMs 25s). 절대 deadline 은
      이미 지났을 수 있으므로 그것만 믿으면 안 된다.
    → 그리고 ★326초가 어디로 갔는지 재서 적는다★: 시간[적재 …s · CV …s · 최종 …s · 나무 n/m].
      ★추측은 두 번이면 사고다.★
  BE-2 TOP MOVERS 에서 한 시장이 ★조용히★ 사라졌다. 원인 둘:
    ① 신선도 폴백이 ★전체 기준★ — 한국이 열리고 미국이 닫히면 미국(24h 초과)이 탈락해도
       한국만으로 10개가 차서 폴백이 ★발동 안 한다★ → 미국이 통째로 빠진다.
    ② 코스닥 ±30% 가 흔해 한 통에 줄 세우면 변동성 큰 시장이 ★독식★ 한다.
    둘 다 "고장" 처럼 안 보이고 ★그냥 안 보인다★ — 그래서 오래 안 잡혔다.
    → 시장별로 따로 모으고·따로 폴백하고·자리를 나눈다(시장당 10칸). 한쪽이 없으면
      남는 자리는 다른 쪽이 채운다. q.market 은 시세가 ★이미 싣고 있다.★
  ★나머지 셋은 고장이 아니라 실제 성적이다 — 문턱으로 덮지 않았다★:
    DNN 억제(외부 49.9% · 워커 40.8% — 둘 다 무실력 이하) ·
    SEQ 잠정가동(★설계대로★ — 전진 유의성 미확립 위원은 가중을 줄여 실제로 투표 중) ·
    FLOW/XALPHA/STACK 보류(홀드아웃 t < 1.65 = 잡음과 구별 안 됨).
    벽은 이미 측정돼 있다 — 10일 sign 은 어떤 모델족이든 49~52.5%, 60분 삼중배리어는 57%.
    ★그 벽은 모델이 아니라 라벨·지평이다.★
  게이트 **167종 전부 통과**(`check-top-movers` 신설 · 돌연변이 9종 · GBDT 시간보장 6종).
  ※ 내 검사가 ★세 번★ "선언만 보고 사용을 안 봤다"(flag · fwdTrust · 시간내역).
    ★만드는 것과 싣는 것은 다른 일이다★ — 두 줄을 같이 두는 것을 규칙으로 삼았다.

- 직전: **V33.414 — 실측이 답했고, ★내가 만든 함정에 내가 빠진 것★ 도 드러났다(BD-1/2/3).**
  ✓ 고친 것이 먹혔다: GBDT 달력 분할 ★걸렸다★(홀드아웃 달력10+60일) · 하한 70.1% → ★47.0%★ ·
    FLOW 원인이 ★이름으로★ 나왔다 — ★정적칸 8/13★(공매도 3칸은 시간 설명 몫 ★0%★).
    모델 순위가 언제나 같으니 고르는 게 아니라 ★외우는★ 것이다.
    XALPHA 는 정적칸 1/25 뿐 — ★원인이 다르다. 같은 처방을 쓰면 안 된다.★
  BD-1 ★치명★ [GBDT] ★trees=0★ — _gbdtFit 의 마감 검사가 루프 맨 위라, 최종 적합 시작 시
    예산이 지나 있으면 한 그루도 못 세우고 break 한다. ★그래도 저장됐다.★ 그 모델은 상수만
    돌려주는데 화면 정확도는 ★CV 폴드 모델★ 값이다 — 물건과 숫자가 다른 것을 가리킨다.
    → 나무 0개면 ★저장 안 한다.★ 원인 절반은 V33.413 이 만들었다(적재량 60,000 → 67,653)
      → 학습 한도에서 홀드아웃만큼 빼고 칸도 12 → 6.
  BD-2 ★내 결함★ [DUAL-BULL] 발언점 68.4% 인데 ★무실력 74.7%★ — 다수클래스만 찍어도
    74.7% 인데 "목표 60% 통과" 로 판정했다. V33.411 에 "숫자만 60% 로 만들지 않겠다" 고
    써 놓고 기저율을 안 보면 ★정확히 그 일★ 이 벌어진다.
    → 문턱 = max(목표, 발언 구간 무실력 + 여유). 표기에 무실력·문턱을 함께 적는다.
  BD-3 정적 칸을 짚어 놓고 "빼면 나아진다" 를 안 쟀다 → 같은 표본·분할로 ★정적칸만 중립화한
    팔★ 을 하나 더 돌려 숫자를 남긴다(★보고만 한다 — 운영 모델 불변★).
  게이트 **166종 전부 통과** · 돌연변이 D1~D10.
    ★D5 가 한 번 빠져나갔다★ — 앞 관문이 가려 주고 있었다. 두 구간의 쏠림이 다른 경우를
    따로 만들어 막았다. ★가려 주는 관문이 있다고 뒤를 안 세우면 앞이 바뀌는 날 뚫린다.★
  ★다음 회차★: ① [GBDT] trees= 가 0 이 아닌지 ② [FLOW] ★정적칸빼면★ 블록IC 가 −0.0589 보다
    나아지는가 — 나아지면 그 8칸을 중립화한다. ★안 나아지면 원인은 다른 데 있다★
    (그때는 피처가 아니라 라벨·지평 — 실험대가 60분 삼중배리어에서 57% 를 이미 쟀다).

- 직전: **V33.413 — "못 쟀다" 고 해놓고 그 숫자를 화면에 올리고 있었다(BC-1/2/3).**
  사용자 관측 둘 다 맞았다.
  BC-1 화면이 ★"하한 70.1%" 와 "못 쟀다" 를 나란히★ 띄웠다 — V33.408 이 무효라 판정한
    숫자를 승격만 막고 표시로는 내보냈다. → 하한 null · 행 기반은 gbdtAccLBRow 로 보존.
    (거래엔 안 닿았다 — trusted·wGbdt 로 막힘. 틀린 건 ★표시★ 였고 사람이 그걸 본다.)
  BC-2 ★치명★ 막기만 하고 길을 안 열어 뒀다 — 필요 이력 120일 > 실측 100일이라
    GBDT 가 ★영구 미승격★ 이었다. ★문턱은 한 칸도 안 깎고★ 둘을 고쳤다:
    ① 고르는 쪽(블록 불필요)만 30일 → 지평 한 칸 ② 홀드아웃을 ★날짜로★ 따로 긁는다
    (한 방 LIMIT 이면 관측 기간이 행 예산의 부산물이 된다 — 다른 학습기는 이미 이 구조였다).
    필요 이력 120일 → ★70일★ · 채점 구간 60일·6블록 ★그대로★.
  BC-3 신규 위원 — 풀드 IC ★양수★ 인데 블록 IC ★음수★ 다(FLOW +0.0155/−0.0589).
    잡음이 아니라 ★구조★ 다(심프슨) — ★같은 날 안에서는 종목을 못 고른다.★
    흔한 원인은 ★칸이 종목을 가리킬 때★ — 시간에 안 변하는 칸은 모델을 "외우게" 만든다.
    → _withinSymbolShare 가 칸별 시간 설명 몫을 재고 ★정적 칸★ 을 이름과 함께 적는다.
    ★아직 안 고쳤다 — 재기만 한다.★ t −1.00 이면 뒤집어도 유의하지 않다(곡선맞추기 금지).
  게이트 **166종 전부 통과**(`check-gbdt-lb` 신설 · 돌연변이 C1~C15 · 되세운 계약 3건 R1~R8).
  ★다음 회차★: ① GBDT 달력 분할이 걸리는가 ② FLOW·XALPHA 의 ★정적칸 이름★ — 다음 수술 자리다.

- 직전: **V33.412 — 수동 학습 실측(회차 35671199889)이 답했고, 결함 둘을 드러냈다.**
  ★V33.408 이 옳았다★ — 위원회 0.6677 을 쥐던 GBDT 하한 70.1% 가 사라졌다
  ("겹치지 않는 블록 2개 < 4 — 행 9000개는 같은 사건을 여러 번 센 것이다").
  ★데드밴드는 신규 위원을 못 살렸다★ — FLOW t −1.00 · XALPHA t −1.38 · 셋 다 무실력 이하.
  V33.409 에 적어 둔 조건이 충족됐다: ★라벨이 아니라 피처가 문제다.★
  ★DUAL-BULL 이 진짜 기회다★ — valAcc 74.8% 인데 무실력도 74.8%(소수점까지 같다).
  그런데 블록IC 0.3438 t 4.91 — ★순서는 아는데 자르는 자리를 모른다.★ 60% 는 여기 있다.
  BB-2(치명) DNN 조기반환 경로가 setBigState 를 건너뛰어 체크포인트가 영영 저장 안 됐다
    → 작은 전용 키 dnn_ckpt 로 두 경로 모두. 외부 모델엔 maxId 를 ★일부러 안 넣는다.★
  BB-3 발언점이 홀드아웃을 반으로 갈라 블록도 반이 됐다 → calFrac 0.25, 경계는 날짜로.
  게이트 **165종 전부 통과**(돌연변이 F14~F17 · S14~S16 추가).
  ★다음★: ① GBDT 이력 100일 < 필요 120일(BB-1) ② FLOW·XALPHA 는 ★피처★ 차례
    ③ DUAL-BULL 의 컷 — 여기가 사용자가 요구한 60% 의 자리다.

- 직전: **V33.411 — "모든 모델 60% 이상" 을 ★정직하게★ 만드는 유일한 길: 기권.**
  사용자 요구는 옳다(50% 위원이 실제 돈에 표를 던지면 안 된다). 문제는 ★어떻게★ 다.
  이 저장소가 이미 측정해 둔 벽: 스윙 10일 sign 은 어떤 모델족이든 49~52.5% 에 수렴하고
  60분 삼중배리어는 57% 다 — "그 벽은 모델이 아니라 ★라벨·지평★ 이다".
  ★전 구간 10일 방향에서 60% 는 모델을 고쳐서 될 일이 아니다.★
  → `_speakPoint`: 문턱은 캘리브레이션에서만 고르고 홀드아웃에서 잰다(선택 편향 없음) ·
    하한은 사건 기반(블록) · ★적용률을 항상 함께★ 돌려준다.
  ★종전보다 엄하다★ — 지금은 52% 모델이 전 구간에 표를 던진다.
  기존 게이트는 한 톨도 안 건드렸다(trustFloor 0.505 ×4 · tMin 2.5 · minBlocks 4 그대로).
  ★이 판은 재기만 한다★ — 적용률 2% 짜리를 지금 배선하면 위원회가 조용히 멈춘다.
  게이트 **165종 전부 통과**(`check-speak-point` 신설 · 돌연변이 15/15).
  **다음 회차**: 모델별 `발언점[…% 하한 · 적용률 …%]`. 적용률이 어느 모델에서도 안 나오면
  답은 기권이 아니라 ★60분 삼중배리어 라벨★ 이다(실험대가 57% 를 이미 쟀다).

- 직전: **V33.410 — 배포된 모델의 ★지금★ 신뢰도를 재기 시작한다(1단계·측정전용).**
  위원회 지분은 학습하던 밤에 얼어붙은 하한에서 나오는데 배포 후 성적을 보는 곳이 없었다.
  재료(icForwardCheck·fwd_ledger)는 이미 있었고 ★배선이 거꾸로★ 였다 —
  지분이 가장 작은 잠정 위원만 매일 재고 GBDT(0.6677)·DNN 은 측정이 아예 없었다.
  → 둘에 배선하고 체크포인트(ts·maxId·maxTs)를 남긴다. DNN 은 청크 모델이라
  getState 가 null 이다 — loadFn 을 열어 ★조용한 무동작★ 을 피했다.
  ★어떤 판정도 바꾸지 않는다★ — 원장이 비어 있어 지금 규칙을 만들면 3일치로 현직을 죽인다.
  게이트가 그 경계를 계약으로 못 박는다(허용된 세 자리를 지우면 fwdTrust 가 안 남아야 한다).
  게이트 **164종 전부 통과**(`check-fwd-trust` 신설 · 돌연변이 13/13).
  **다음 회차**: 첫 밤은 "원장 없음" 이 정상. 3일 뒤부터 블록IC·t 가 나온다.
  ★2단계는 2주쯤 쌓인 뒤★ — 그때도 방향은 ★강등과 존속만, 승격은 아니다.★

- 직전: **V33.409 — 신규 위원은 ★라벨이 코인 던지기인 행★ 에 학습 용량을 쓰고 있었다.**
  FLOW 홀드아웃 블록IC t −0.59 · XALPHA −0.89 — 약한 게 아니라 ★부호가 반대★ 다.
  라벨이 `pnl_pct > 0` 이라 10일에 ±0.02% 움직인 행까지 1/0 이 된다.
  → 시장별 |pnl| 중앙값의 0.25배 아래를 ★학습에서만★ 뺀다(`_deadbandMask`).
  ★홀드아웃은 한 행도 안 건드린다★ — 게이트·문턱·모집단 전부 불변이라
  홀드아웃 수치가 오르면 그건 모집단이 바뀐 게 아니라 진짜다.
  실험대 D(+3.12%p/IC 0.0872)를 ★그대로 베끼지 않았다★ — 실험대 자신의 경고
  ("데드밴드 후보는 모집단이 다르다")를 받아들여 D 보다 엄하게 썼다.
  게이트 **163종 전부 통과**(`check-deadband` 신설 · 돌연변이 13/13).
  **다음 회차가 답할 숫자**: FLOW·XALPHA 홀드아웃 블록IC t 와 `데드밴드[…% 적용]`.
  t 가 음수에서 못 벗어나면 라벨이 아니라 ★피처★ 가 문제다.

- 직전: **V33.408 — V33.398 은 옳았고 ★자리★ 가 틀렸다. τ* 캘리브레이션이 블록 하한을 도로 올려서, 배포하고도 화면의 GBDT 하한 70.1% 가 살아 있었다.**
  게이트 **162종 전부 통과**(`check-block-order` 신설 · 돌연변이 17종 전수 · `check-block-accuracy` 계약 2건을 뜻으로 되세워 강화 · 돌연변이 6/6).
- Branch: `main`

### AX-1(치명) — 재는 자리와 씌우는 자리가 뒤바뀌어 있었다
```
9,000행 = 600종목 × 15거래일 · 지평 10일 → 겹치지 않는 블록 ★1개★ → "못 쟀다"
같은 형상의 ★행 기반★ Wilson(유효 433) = 0.7009   ← 화면의 70.1% 와 같은 산수다 (게이트 ③ 이 실제로 계산)
```
V33.398 이 `accLB = min(accLB, 블록하한)` 을 **먼저** 했는데, 바로 아래
`if (accLBC > accLB) accLB = accLBC` 가 **행 수 기반 Wilson 을 다시 얹어** 보정을 지웠다.
→ **재는 것과 적용하는 것을 분리**하고, `accLB` 확정 뒤(τ* 포함) **한 번만** 최소값을 씌운다.
τ* 가 이기면 하한의 출처가 `calB` 이므로 **블록도 calB 로 다시 잰다**(AX-2).

### AX-3 — 막기만 하면 반쪽이다
15일 창에서는 블록 1개라 **영원히 승격 불가**가 된다. V33.326 이 FLOW·XALPHA·STACK 에
쓴 처방(달력 고정)을 GBDT 홀드아웃에도 쓴다 — `_gbdtCalSplit`.
**기간은 ★채점 구간 기준★** 이다(여기서 한 번 틀렸고 **게이트가 잡았다**: 60일을 반으로
나누면 채점 쪽 30일 → 블록 3개 < 최소 4개). 채점 60일 + τ* 선택 30일 = 90일.
분할 함수가 **채점 구간의 블록 수를 실제로 확인**하고, 못 채우면 **포기**해 행 기반으로 물러선다.
**검증 하한은 한 톨도 안 낮췄다** — `trustFloor` 4종 전부 0.505 그대로.

### 다음 작업자에게 — 남은 것
1. **신규 위원(FLOW·XALPHA·STACK·듀얼헤드) 성능 개조** — 사용자 지시, 착수 전.
2. **재학습이 좋은 모델을 버리는 문제 / 배포된 모델의 ★현재★ 신뢰도 측정** — 사용자가
   *"12번은 아직 하지 말고 너의 의견부터 제시해"* 라 했다. **구현 금지, 의견 먼저.**
   단서: `src/index.js` 의 `_num(prevModelEarly.valAccLB, 0) >= dnnLB` 가 이미 "이전 모델이 더 낫다" 를 본다.
3. **한국장 거래 부재** — V33.407 의 시장별 깔때기 로그(`__funCand/__funTry/__funBuy`)가
   찍히기를 기다리는 중. 상류(후보)인지 하류(체결)인지 그 줄이 가른다.
4. **짧은 학습 창이 진짜 실력인가** — 사용자 가설. 같은 긴 정직 홀드아웃에서 재는 실험대 팔 필요.

### AN-1(치명·개혁) — 고유도는 ★같은 종목 안에서만★ 겹침을 센다
**화면 실측을 산수로 옮기면 답이 나온다:**
```
워커 GBDT  검증  9,000행 · 유효  432 · 하한 70.4% → 지분 0.7106
Modal LGB  검증 276,251행 · 유효 8,442 · 하한 51.1% → 지분 0.1955

 9,000행 ÷ 종목 600 = 종목당  15봉 ≈  15거래일 · 라벨 지평 10일 → ★블록 1.5개★
276,251행 ÷ 종목 600 = 종목당 460봉 ≈ 460거래일 ·      〃      → ★블록  46개★
```
**워커 GBDT 의 "74%" 는 그 15일 동안 시장이 올랐다는 ★사건 하나★ 다.**

유효표본(432)이 왜 못 걸렀나 — `_uniqWeights` 주석이 스스로 답한다:
> *"같은 종목 안에서만 센다 — 다른 종목의 같은 기간은 상관은 있어도 같은 사건이 아니다"*

횡단면 수익에는 맞는 말이지만 **방향성 라벨에는 틀리다.** 600종목이 같은 10일 동안
같이 오르면 그건 600개 관측이 아니라 **한 개**다. Wilson 하한을 432 로 재면
**15일치 시장 방향이 70.4% 의 확신으로 둔갑한다.**

### 고친 방향 — ★문턱이 아니라 자를 고친다★
이 저장소는 **IC 에 대해서는 이미 블록으로 센다**(`t = ICIR × √K`). **정확도만 예외였다.**
같은 규율을 적용한다:
1. 검증행을 **라벨 지평 길이의 겹치지 않는 시간블록**으로 나눈다
2. 블록마다 정확도를 재고 **블록 간 산포**로 표준오차를 낸다(단측 1.64σ)
3. **블록이 4개 미만이면 "못 쟀다"** — 못 잰 것을 통과로 읽지 않는다(IC 경로와 같은 규율)

**합성표본으로 실제로 돌려 확인했다**(전 종목이 그날의 시장 방향을 공유하는 구조):
```
15거래일 창  → 블록  1개 → ★못 쟀다★
460거래일 창 → 블록 45개 → 하한 65.8%      ← 긴 창은 그대로 산다
평균이 같아도(0.60) 블록이 흔들리면: 안정 60.0% vs 요동 30.7%
```

**적용**: 워커 GBDT·워커 DNN 둘 다 **둘 중 더 작은 하한**만 채택한다
(**느슨해지는 방향이 없다**). 못 쟀으면 **그 자리에서 멈추고 사유를 남긴다**.
`blockAccLB`·`blockK` 를 기록에 남겨 화면이 이유를 말할 수 있다.

**★문턱 상수는 한 글자도 안 건드렸다★** — 4종 전부 0.505 그대로(게이트가 전수 확인).
바꾼 것은 *"무엇을 세는가"* 하나다.

### 예상되는 결과 — 먼저 말해 둔다
워커 GBDT 는 **승격이 막힌다**(블록 1~2개). 위원회는 5/10 → 4/10 이 될 수 있다.
**그게 맞다** — 그 자리는 애초에 사건 하나를 실력으로 읽어 차지한 자리였다.
Modal GBDT(블록 46개)가 그 슬롯을 가져가는 것이 정상 경로다.

### ★돌연변이 13/13 — 그리고 안 잡히는 1건은 이유를 적었다★
`if (K < minBlocks)` 를 무력화해도 안 잡힌다. **무해하기 때문이다** — 두 번째 가드
(채워진 블록 수)가 같은 경우를 덮고, `K=0` 이면 길이 0 배열이라 조용히 무시되어
`acc.length=0` → '못 쟀다' 가 된다. **잡히지 않는 이유를 안 적으면 다음 사람이
이 게이트를 헐겁다고 오해한다.**

### AM-1(치명) — 위원회 지분이 ★무실력 기준점★ 이 아니라 0.5 에서 잰 거리였다
**사용자 화면 실측(2026-09-21 08:24)**:
```
GBDT  하한 70.4% · ★Worker★ · 검증 유효   432/9,000   (D1 풀)      → 지분 0.7106
LGB   하한 51.1% ·  Modal   · 검증 유효 8,442/276,251             → 지분 0.1955
Cat   하한 50.3% ·  Modal   ·        〃                            → 지분 0.1812
DNN   하한 49.7% · 문턱 50.5% → 억제
```
**메커니즘을 역산해 확정했다** — `w = e^{T(lb−0.5)} / (e^{T(lb−0.5)} + e^{T(mindLB−0.5)})`,
`T = 12`. GBDT 지분에서 역산한 **MIND 하한 = 0.6291** 을 넣으면
**LGB 0.1950(실측 0.1955) · Cat 0.1804(실측 0.1812)** — 오차 5e-4·8e-4.
**추측이 아니라 이 식이 맞다.**

**무엇이 틀렸나**: `0.5` 가 무실력인 것은 **기저율이 50% 일 때뿐이다.**
V33.292 가 이미 그 사실을 알고 `accBase` 를 받아 **문턱**에 반영했는데,
**지분에는 끝내 반영하지 않았다 — 반쪽만 고쳐져 있었다.**

**증거는 이미 화면 안에 있다**: 승격 조건이 `accLB ≥ base + 0.015` 이므로
GBDT 가 통과했다는 사실 자체가 **`base ≤ 0.689`** 를 증명한다. 같은 라벨에서
Modal 부스터 셋이 50.3~51.2% 인데 GBDT 만 18%p 실력이라는 해석은 그 셋과 양립하지 않는다.
즉 base 는 상단 쪽이고, **실제 실력은 1.5~5.4%p 인데 0.5 영점으로는 20.4%p 로 읽힌다.**

| GBDT base | 실제 실력 | 현행 지분 | 교정 지분(MIND base 0.52 / 0.55) |
|---|---|---|---|
| 0.500 | 20.4%p | 0.7107 | 0.7575 / 0.8174 |
| 0.600 | 10.4%p | 0.7107 | **0.4847 / 0.5741** |
| 0.650 | 5.4%p | 0.7107 | **0.3405 / 0.4253** |
| 0.680 | 2.4%p | 0.7107 | **0.2648 / 0.3405** |
| 0.689 | 1.5%p | 0.7107 | **0.2443 / 0.3167** |

**★내가 처음 한 계산은 틀렸다.★** 모든 모델을 같은 base 로 옮기면 softmax 는 **이동불변**이라
아무것도 안 바뀐다(위 표 첫 행이 그 증거다). 왜곡은 **base 가 서로 다를 때만** 생기고,
실제로 다르다 — **워커 GBDT 는 D1 9,000행, MIND·부스터는 Modal 276,251행.**
**다른 풀, 다른 기저율인데 같은 영점으로 비교해 왔다.**

**고친 것**: `_skillExp(lb, base, temp) = exp(temp × (lb − max(0.5, base)))` 하나로 모으고
**판정에 쓰는 지분식 5곳 전부**를 여기로 보냈다(워커 GBDT·워커 DNN·외부 GBDT·`_dnnAdmit`·
위원회 `_icOf`). 위원장 쪽 영점(`mind.valAccBase`)도 같이 넘긴다 —
**한쪽만 교정하면 비교가 더 틀어진다.**

**안전장치**
- `base` 를 모르면 **0.5 로 떨어져 종전과 비트 단위로 같다**(게이트가 확인한다).
- `base < 0.5` 는 0.5 로 자른다 — **영점을 낮춰 지분을 부풀릴 수 없다.**
- **교정 전 값**(`wGbdtRaw05`·`wDnnRaw05`)을 지분 확정 3곳 전부에 남긴다 —
  다음 회차 로그에서 **얼마나 부풀어 있었는지 숫자로** 나온다.
- **문턱 상수는 한 글자도 안 바꿨다**(0.505 · +1.5%p). 바꾼 것은 *"무엇에 대해서"* 하나뿐이다.

**게이트가 내 게이트를 두 번 잡았다**: ① `\w+ - 0.5` 로 좁게 써서 `_num(mindLB, 0.5) - 0.5` 를
놓쳤다 → 괄호 안이 무엇이든 세게 바꿨다. ② 교정 전 기록을 **존재만** 확인해 두 곳 중 하나를
지워도 통과했다 → **개수를 세어** 지분 확정 지점 수를 덮는지 본다.

### AL-1(중) — 보내는 쪽과 쓰는 쪽이 ★다른 값을 믿고 있었다★
```python
K = SEEDS_OVERRIDE or cfg.get("seeds", 4)     # SEEDS_OVERRIDE = 6 → ★항상 참★
```
`or` 는 단락한다. **오른쪽은 평가조차 되지 않는다.** 워커가 매 회차 `seeds` 를 보내는데
트레이너는 한 번도 읽은 적이 없다 — V33.260(`dropout`·`l2`)·V33.341(`liveSrcWeight`)과 같은 병이다.

**증상은 더 나빴다.** 구조 관측 화면은 `params = 넷당 × DNN.seeds` 로 그리는데
`DNN.seeds = 4` 였고 실제로 도는 것은 **6시드**다 → **화면이 실제 파라미터의 2/3 만 적고 있었다.**

→ 트레이너는 **설정이 먼저, 상수는 폴백**(`K = _k_cfg or SEEDS_OVERRIDE`, 범위 2~8 밖은 무시).
워커의 `DNN.seeds` 를 **6** 으로 맞췄다 — **보내는 값·도는 값·화면에 적히는 값이 같아진다.**
시드를 어느 쪽에서 가져왔는지 로그가 말한다.

### AL-2 — 문턱을 보내 주는데 트레이너는 안 보고 있었다
`trustFloor`·`icFloor` 는 워커 승격 게이트 상수다. 매 회차 wire 를 타고 오는데 안 읽혔다 —
그래서 로그가 **"valAcc 47.30%"** 라고만 하고 **통과인지 미달인지는 워커 로그를 따로** 봐야 했다.
```
[승격예고] 하한 47.27% vs 정확도 문턱 50.50% → ★미달 3.23%p★ · 블록IC 0.0nn vs 문턱 0.015 · t n.nn
           (최종 판정은 워커가 한다 — 이 줄은 예고다)
```
**판정 주체는 하나로 둔다** — 트레이너가 판정까지 하면 두 곳이 언젠가 갈라진다.

**게이트** `check-config-wire`: `_mlExportConfig` 가 보내는 최상위 키를 **전수로 뽑아**
트레이너가 읽는지 대조한다. 안 읽는 것은 **이유를 적어** `INFO_ONLY` 에 넣어야 하고,
**목록에 있는데 실제로 읽히면 그것도 실패**한다(목록이 낡는 것도 거짓말이다).
`상수 or cfg.get(...)` **단락 모양을 금지**하고, `DNN.seeds == SEEDS_OVERRIDE` 를 못 박아
화면·실제가 다시 갈리면 실패한다. 돌연변이 **9/9**.

*(첫 실행에서 이 게이트가 자기 버그를 드러냈다 — 블록 주석 안의 `trainer:` 인용을 키로 잡았다.
주석을 먼저 걷어내게 고쳤다.)*

### AK-1(중) — ★몇 명이 투표했나★ 와 ★누가 정했나★ 를 구분할 방법이 없었다
위원회 가중은 `exp(icTemp × IC)` 이고 **`icTemp = 60`, IC 상한 `0.25`** 다.
두 위원의 IC 가 **0.1 만 벌어져도 `exp(6) = 403배`**, 0.2 면 **16만 배**다.
**한 명이 결합확률을 통째로 끌고 갈 수 있는 구조다.**

**그게 틀렸다는 말이 아니다** — V33.77 이 *일부러* 그렇게 만들었다(정확도 가중은
실력 차이를 0.505~0.535 좁은 구간에 뭉개서 "투표하면 오히려 부정확" 을 만들었다).
**문제는 그 사실이 어디에도 안 적힌다는 것이다.** 화면은 "위원 N명 투표 중" 만 말하고,
그 N명이 고르게 기여했는지 한 명이 97% 였는지 **구분할 방법이 없었다.**

→ **실현 지분**을 결정 결과에 싣는다. `w = 그 위원의 가중 / 전체 가중` —
**다시 계산하지 않고 실제 결합에 쓴 그 값**이다. 한 명이 **90% 이상**이면
`dominant:{name, share, n}` 로 한 줄 요약도 남긴다.
**판정은 한 글자도 안 바꿨다 — 기록만 붙였다.**

읽는 법을 코드가 스스로 적는다: 이 지분은 **IC 가중 결합 시점**의 값이다.
뒤에서 절사평균과 STACK 이 확률을 더 손보므로 최종 기여와는 다르다.

### ★내 게이트가 두 군데서 헐거웠다 — 돌연변이가 잡았다★
- **반환 경로가 넷**(정상·DI기권·불확실·모호)인데 **존재 여부만** 확인해서, 하나만 지워도
  나머지에 걸려 통과했다 → **개수를 세어** `experts: _expOut` 를 싣는 곳 전부가
  `dominant` 도 싣는지 본다.
- 지배 판정 경계 시험을 **게이트 안에 지역 사본으로** 써 놨다. 소스를 바꿔도 사본은 그대로라
  그냥 통과했다(이 저장소가 `check-trainer-budget` 에서 이미 당한 실수다)
  → **소스에서 판정식을 떼어 내 `new Function` 으로 돌린다.** 문턱을 0.9 → 0.99 로 슬쩍
  올리는 변이도 이제 잡힌다.

### AJ-1(치명) — "검증받은 모델" 과 "워커가 돌리는 모델" 이 같다는 보장이 없었다
GBDT·단타·SEQ·MEMO 는 업로드 때 **probe** 로 *"워커 추론이 트레이너 확률을 재현하는가"* 를
확인하고, 못 넘기면 승격을 막는다. **DNN 만 그 검사가 없었다.**
그런데 DNN 은 **변환이 제일 많은 모델**이다:

```
① BatchNorm 접기   relu(BN(Wx+b)) → relu(W'x+b'),  a = gamma/sqrt(var+eps)
② τ* 를 마지막 층 bias 에 굽기
③ 접힌 계수를 ★소수 5자리로 반올림★ 해서 전송
```
**③이 특히 위험하다** — `gamma` 가 작아 `a` 가 작으면 `W'` 가 1e-6 수준이 되어 **0 으로 반올림**된다.
그 채널은 **조용히 사라지고**, 아무도 재지 않았다. 즉 "검증 47.3%" 가 이 워커가 실제로
계산하는 값이라는 보장이 **어디에도 없었다.** 성능 논의의 바닥이 확인 안 된 상태였다.

**고친 것** — seq/memo 와 **같은 자**:
- 트레이너가 검증행 64개로 `probe:[{x, p}]` 를 싣는다. **표준화 ★전★ 원본 x** 라
  워커의 `mean/std`·클리핑 경로까지 같이 검증된다.
- 트레이너가 **접힌·반올림된 가중치로 직접 재현**해 오차를 찍는다 — 업로드 전에 원인을 안다:
  ```
  [변환정합] 접기+반올림 후 최대 확률차 0.______ (probe 64행)
             · 반올림으로 0 이 된 가중치 __/__ (_.___%)
  ```
- 워커가 커밋 후 **`mlDNNScore`(라이브와 같은 함수)** 로 재현을 확인한다.
  **★거부가 아니라 억제★** — 이 경로는 37MB 를 스트리밍으로 이미 저장했고(객체로 파싱하면
  OOM 이라 그렇게 설계돼 있다) 되돌리면 학습 회차가 통째로 날아간다. **저장은 하되 투표를 막고**
  `ERROR` 로 남긴다(정확도 미달과 같은 처리다).
- 결과가 `dnn_trust.probeMaxDiff` 로 남고 **전체 구조 화면이 그 값을 적는다**.
  probe 가 없던 회차는 **"정합 미확인"** 이라고 적는다 — **없는 검사를 통과로 위장하지 않는다.**

**게이트** `check-dnn-fold-probe`: 접기 수식을 **직접 돌려** BN 경로와 평면 경로가 같은지
검산하고(오차 4.4e-16), 트레이너가 그 식을 쓰는지, 반올림 자릿수에서 **|w| < 5e-6 이 0 이 된다**는
사실을 못 박고, 워커 판정을 **문턱 경계에서** 돌린다. 돌연변이 **12/12**.

**★게이트가 내 실수를 두 번 잡았다★**
- `check-eval-cost` ⑧: 내가 새로 만든 정합-실패 `dnn_trust` 기록에 **`trainedAt` 이 없었다**
  → 화면 나이가 `null` 이 된다. `source:"external"` 기록 13건을 전수 검사하는 가드다.
- 내 게이트 자신의 돌연변이 D6: `"probe": probe}` 를 **파일 전체**에서 찾는 바람에
  SEQ·GBDT 의 같은 줄에 걸려 **DNN 에서 빼도 통과**했다 → **DNN meta 블록 안에서만** 보게 고쳤다.

### AI-1(치명·배포차단) — 벽시계 시간을 계약으로 삼은 게이트
**V33.391 의 Cloudflare 배포가 실패했다**(run 35417376999). 원인은 코드가 아니라 **측정**이다:

```
CI:    옛 22ms → 새 16ms = 1.4배  → FAIL("캐시가 안 먹고 있다") → 배포 중단
로컬:  옛 35ms → 새 14ms = 2.5배  → 통과
```
같은 코드다. `check-flow-peer-cpu` ②가 **1회·무예열·20ms 규모**의 벽시계 비율을
`ratio >= 1.5` 로 단언하고 있었다 — ±3ms 스케줄러 잡음이 배수를 1.4~2.6 사이로 흔든다.
게다가 CI 러너가 막 **Node 24** 로 올라갔다.

**계약("캐시가 실제로 값을 한다")은 옳다. 문턱을 낮추지 않고 측정을 고쳤다:**
- **별도 캐시로 양쪽을 예열**한다 — 측정 대상 메모를 미리 채우면 새 경로를 과대평가한다
- 종목 **60 → 200**, 측정을 잡음보다 훨씬 크게
- **3회 재고 최솟값**을 쓴다(최솟값은 스케줄러 잡음에 로버스트한 추정량이다)
- 새 경로는 **매 회 새 객체**로 잰다 — **메모 구축 비용을 항상 포함**시킨다(공짜로 안 준다)

```
옛 102ms → 새 26ms (3.9배) · 회차별 옛/새 105/27 102/27 101/26      ← 5회 연속 재현
메모를 무력화하면 옛 101ms → 새 105ms (1.0배) → 여전히 잡는다      ← 민감도는 그대로
```
예열이 없어서 새 경로가 JIT 비용을 뒤집어쓰고 있었다 — 그래서 배수가 2.5 에서 **3.9** 로 올랐다.

**같은 부류를 전부 훑었다**: `check-bandit-memo` 의 시간은 **출력 전용**(단언 아님),
`check-enrich-budget` 은 **차단이 실제로 걸리는가**를 500ms·900ms 여유로 본다(잡음 대비 충분).
**벽시계 비율을 계약으로 삼은 곳은 이 한 곳뿐이었다.**

### AH-1 — 동전보다 낮으려면 ★뭔가가 뒤집혀 있어야★ 한다
같은 표본에서 트리는 53~54%, 이 망은 **47.3%**. 단순히 약한 게 아니다.
후보는 둘이고 **둘 다 이제 매 회차 잰다**:

**① `[기저율]`** — 라벨은 10일 선행수익 부호다. 학습은 **7.4년**(대부분 상승장),
검증은 **최근 240일**. 두 구간의 기저율이 다르면 학습 구간의 사전확률을 배운 모델은
검증에서 **구조적으로 동전 아래**로 간다. τ* 도 보정구간(학습 꼬리)에서 고르므로
그 편향을 그대로 들고 검증으로 건너간다.
```
[기저율] 학습 __._% · 보정 __._% · 검증 __._% → 이동 ±_._%p
         다수클래스 기준선 학습 __._% / 검증 __._%
③ 앙상블 valAcc 47.30% … · 다수클래스 __._% → ★초과 ±_._%p★
```
**정확도는 다수클래스 대비 초과(%p)로만 뜻이 있다.** 승격 게이트의 자(lb)는 안 바꿨다.

**② `[분포이동]`** — 표준화는 학습 구간 평균·표준편차로 잡고(V33.115 누출수정) 검증에도
그 자를 쓴다. 옳다. 그런데 최근 240일 분포가 밀려 있으면 검증행 z 가 통째로 밀리고
`stdClip(±6)` 에 **포화**된다. **트리는 단조변환에 불변이라 이 이동에 강하고 신경망은 아니다**
— "같은 표본인데 트리만 잘한다" 의 표준적인 설명이다. 평균|z|·포화율·포화 상위 피처를 적는다.

### AH-2(★근본 성능 축★) — 7.4년을 ★균등하게★ 배우고 최근 240일로 채점하고 있었다
시장은 정상(stationary)이 아니다. 게다가 recency 손잡이는 규모가 커지면서 죽어
(AB-2: 표본의 **97.2%** 가 바닥값에 붙어 가중의 **95.2%** 를 차지) **사실상 균등가중**이다.
트리는 구간을 쪼개 최근 영역을 따로 설명할 수 있지만, **전역 파라미터 하나로 모든 시대를
설명하는 이 망은 그럴 수 없다.**

→ **표본 창(`win_days`)을 스윕 축으로 만들었다.** 후보: **전체 / 3년 / 1.5년**.
`0` = 전체(**종전 동작과 완전히 동일**). ★값은 내가 고르지 않았다 — 측정이 고른다.★
V33.386 의 측정→저장→전달→적용 고리를 그대로 타므로 **이긴 창이 다음 회차부터 실제로 적용된다.**

지킨 것: 창 기준점은 **검증 시작 시점**(후보 전원이 같은 자) · 표본이 과하게 줄면
(5,000건 또는 8% 미만) **전체로 되돌리고 사유를 찍는다**(창이 아니라 기근이 된다) ·
**클래스 균형 가중을 창 안에서 다시 잰다**(창이 바뀌면 기저율도 바뀐다) ·
과적합·최근성 진단도 **실제로 배운 행**을 본다.

### ★기존 계약 2건이 내 변경 때문에 틀린 이유로 실패했다 — 뜻으로 다시 적었다★
- `check-depth-sweep`: *"Xtr 도 fit_arch 밖에서 만들어야 한다"* 였다. 표본 창이 축이 되면
  **후보마다 학습행이 달라야 한다 — 그게 그 축의 목적이다.** 공정함을 지키는 것은
  학습행이 같은 것이 아니라 **① 채점행·보정행이 모든 후보에게 동일할 것**,
  **② 학습행은 선언된 창으로만 달라질 것**이다. 그렇게 다시 적고 ②를 새로 못 박았다.
- `check-fit-diagnosis`: `_mtr = mw[tr]` 를 문자열로 못 박아 뒀다. 계약의 뜻은
  *"실제로 학습에 쓴 그 가중"* 이지 `tr` 이라는 **이름**이 아니다 → **`Mtr` 을 만든 바로 그
  색인인지**를 보게 바꿨다(종전보다 좁다).

### AG-1(치명) — V33.388 을 절반만 한 것이 ★새 비대칭★ 을 만들었다
V33.388 은 Modal 학습기 8곳의 조기중단 누출을 고쳤다. **워커의 자체 폴백 학습기는 그대로였다.**
그냥 남는 문제가 아니다:

```
워커 DNN 재학습 경로:  if (외부모델.valAccLB >= 폴백.dnnLB) → 외부 모델 유지
```
이 한 줄이 **정직해진 외부 모델**과 **여전히 부푼 폴백**을 맞대어 어느 쪽을 쓸지 정한다.
→ **내 수정이 폴백 쪽으로 저울을 기울여 놓았다.** 고치다 만 것이 안 고친 것보다 나쁘다.

고친 곳(둘 다 `_mlpFit` 이 이미 쓰던 방식 그대로 — *"내부검증은 학습 꼬리 15%, 홀드아웃은 건드리지 않는다"*):
- **워커 DNN**: 학습 꼬리 15% 를 내부검증으로 떼고 **학습에서 뺀다**. 못 뗐으면 그 회차가 부풀었다고 로그가 말한다.
- **워커 GBDT**: `_gbdtFit` 에 `innerStop` 을 달아 **CV 폴드·홀드아웃 폴백 두 곳**에 켠다
  (폴드 검증으로 트리 수를 고르고 그 폴드로 OOF 를 재던 자리다).
  메타모델 경로는 **이미 제대로 된 내부셋을 넘기므로 안 건드렸다** — 켜면 두 번 뗀다.

### AG-2~4 — 모델 상태 화면의 ★조용한 빈칸★
이 화면들은 빈 값을 만나도 **터지지 않는다**. `d.inputDim!=null ? d.inputDim : '—'` 로 조용히
'—' 를 그리거나, `if(d.headDegenerate)` 로 경고를 **아예 안 그린다**. 로그도 안 남는다.

| | 무엇이 틀렸나 |
|---|---|
| **AG-2** | 전체 구조 화면이 몇 달째 **"★—차원★ · 피처 판 N"** 이라고 적고 있었다. 모델 탭들은 전부 `inputDim` 을 싣는데 **전체 구조 응답만 그 칸을 한 번도 채운 적이 없다** → `LUXML.featNames.length` 로 채운다(손으로 안 적는다) |
| **AG-3** | STACK 화면이 **"전문가 확률 8 + 참여마스크 8"** 이라고 적었다. 위원이 8명이던 시절 값이다 — MEMO(V33.92)·SEQ(V33.272) 합류로 지금은 **9명·18차원**이다. ★바로 위 줄의 주석이 "숫자를 손으로 적지 않는다" 라고 적어 두고 그 다음 줄이 8 을 손으로 적고 있었다.★ → `inputDim` 을 반으로 나눠 쓴다 |
| **AG-4** | STACK 의 **"후보 전원이 다수 클래스로 붕괴"** 경고가 **한 번도 뜬 적이 없다.** 모델은 `headDegenerate` 를 들고 있고(31255) 화면은 그릴 줄 아는데(14713) **응답이 안 넘겼다** → 헤드 선택이 퇴화했는지 사람이 알 길이 없었다 |

**게이트** `check-viz-fields`: 화면이 응답에서 읽는 이름을 **중괄호 균형으로 함수 본문을 잘라** 전부
긁어내고, 서버가 그 이름을 채우는지 **대조**한다. 전체 구조 7칸 · 모델 탭 6종 전부.
구조 숫자를 손으로 적으면(“전문가 확률 8”) 실패한다. 돌연변이 5/5 + 무해 변경 오탐 0.

### ★기존 게이트가 내 수정 때문에 틀린 이유로 실패했다 — 계약을 뜻으로 고쳐 적었다★
`check-model-evidence` 는 호출을 **문자열 통째로** 못 박아 뒀다
(`_dnnTrainOne(train, val, dims, deadline, warm, DNNW)`). 그 계약의 뜻은
*"하이퍼파라미터를 인자로 받는다"* 이지 *"둘째 인자가 val"* 이 아니다.
뜻으로 고쳐 적고 **더 좁게** 만들었다: 여섯째 인자가 `DNNW` 인지, 본문이 전역 `DNN.` 을
직접 읽지 않는지, 그리고 **둘째 인자가 채점용 홀드아웃이 아닌지**(=이번 누출)까지 본다.
고치는 중에 **정의부가 호출부보다 먼저 걸려** 언제나 `(val, hp)` 로 읽히는 것을 발견했다 —
그대로 뒀으면 **아무것도 안 지키는 검사**가 될 뻔했다. `=\s*` 로 호출부에 앵커를 옮겼다.

### AF-1(치명) — 매수와 매도가 ★다른 위원회·다른 입력★ 으로 판단하고 있었다
청산 호출부 주석은 이렇게 적혀 있었다:
> "진입을 주도하는 위원회(**GBDT/DNN/MIND**)로 보유 포지션도 재평가"

**그때 위원은 셋이었다.** 그 뒤 위원회는 아홉이 됐고 — **이 호출만 안 따라왔다.**

| | 진입(매수) | 청산(매도) |
|---|---|---|
| SEQ·FLOW·XALPHA | 참여 | **한 번도 참여 못 함** (`seqFeat`/`flowFeat`/`xaFeat` 미전달 → 조용히 불참) |
| `sectorCloses` | 실제값 | **미전달** → 미국 종목 `sectorRs20`·`sectorBeta` 가 청산 때만 중립 |
| shock·evCtx·portStats·이중헤드·techK·finalCal | 전달 | 전부 미전달 |

**같은 종목·같은 순간에 진입과 매도가 다른 값을 봤다.** 위원이 준 것만이 아니라
**입력 자체가 달랐다** — 같은 모델에 다른 벡터를 먹인 것이다.
`AI_EXIT` 로 나간 매도는 전부 이 여섯-위원 결손-입력 판단이었다.

→ 청산 경로에서 `_secX`·`_xaX`·`_seqX`·`_flowX` 를 만들어 **진입과 같은 인자 전부**를 넘긴다.
비용은 묶어 뒀다: **FLOW 는 `noFetch` 로만** 부른다(보유 종목마다 매 분 도는 경로다 —
캐시에 없으면 네트워크를 안 타고 그냥 불참한다). **SEQ 는 진입과 같은 사이클 예산**(`_enrich.seqMs`)을
나눠 쓴다 — 한쪽이 먼저 태우면 다른 쪽이 굶는다.

**★사람이 정할 자리(적어 둔다)★**: 청산 문턱 `committeeExitProb 0.42` 는
**옛 여섯-위원 분포에서 관찰된 값**이다. 위원이 늘면 결합확률 분포가 바뀌므로 이 문턱은
다시 봐야 한다. **임의로 옮기지 않았다.**

### AF-2(중) — 위원이 왜 빠졌는지 ★아무 데도 안 적혀 있었다★
위원 조립부는 위원마다 `try/catch` 로 감싸는데 **catch 가 전부 비어 있었다**(8곳).
그래서 셋을 구분할 수 없었다: **① 미승격 ② 피처 미제공 ③ 던짐.**
그리고 화면의 `voting` 은 **저장된 승격 상태**로 계산된다 — 실제로 표를 한 번도 못 던져도
**계속 "가동 중" 이라고 말한다.** AF-1 이 몇 달간 안 보였던 이유가 정확히 이것이다.

→ `_absent` 명부를 만들어 **위원 9종 전부** 사유를 적고, 결정 결과(`absent`)에 실어 보낸다.
**"피처 미제공(호출부가 …를 안 넘겼다)" 을 "미승격" 과 다른 말로 적는다** — 같은 말로 적으면
이번 버그를 또 못 본다. 정원(`STACK_SLOTS`) 대비 빈칸도 "사유 미기록" 으로 채운다.

### ★게이트가 내가 넣은 버그를 잡았다 — 이번엔 실행으로★
`_absent`/`_skip` 을 첫 위원(mind) **뒤에** 선언해 놓고 mind 블록에서 썼다 → **TDZ ReferenceError**.
`mlDeepDecide` 의 최상위 catch 가 그걸 삼켜 **위원회 전체가 null 을 반환**한다 —
V33.90 에서 이미 한 번 당한 그 버그를 내가 똑같이 재현했다.
`check-order`(TDZ 정적검사) · `check-seqformer` · `check-stack-seq`(둘 다 실제로 호출)
**3종이 동시에 잡았다.** 선언을 첫 위원 위로 올려 고쳤다.

**게이트**: `check-committee-symmetry` — 두 호출부의 **옵션 키 집합을 괄호 균형으로 잘라 비교**한다.
진입에 있고 청산에 없는 키가 하나라도 있으면 실패. 청산 호출인지 `committeeExit` 문맥으로
확인하고(순서만 믿으면 코드가 바뀔 때 엉뚱한 걸 비교한다), 피처 조립 인자도 대조하고,
불참 사유 기록·`noFetch`·사이클 예산 공유까지 못 박는다. 돌연변이 **11/11**.

### AE-4 — SEQ 형상 스윕도 검증행에서 골랐다 (V33.388 이 남겨 둔 것)
V33.388 에서 "SEQ 형상 스윕은 아직 안 고쳤다" 고 적었다. 닫았다.
후보 4개(d·헤드·층)를 `accLB`(검증행)로 줄세우고 그 값을 승격 점수로 올리던 것을,
**자는 그대로 두고 행만** 보정구간으로 옮겼다(`lbs_`). 검증행은 채점에만 쓴다.
`fit_seq` 는 이제 6-튜플을 돌려주고, 순위표에 `보정/검증` 을 **나란히** 찍는다.

같이 고친 것: SEQ 의 `_split_ts` 태그가 **`"GBDT"`** 였다 — 로그만의 문제지만,
두 학습기의 분할 크기를 비교할 때 사람이 **틀린 줄을 읽게 된다.** `"SEQ"` 로 고쳤다.

`check-seqformer` 의 계약을 *"같은 자"* 에서 *"같은 자 × 다른 행"* 으로 올렸다 —
`r_[0]` 로 고르면 **실패한다**(종전 계약은 그걸 요구했다). 돌연변이 4/4.

**이제 이 트레이너에서 선택은 전부 보정행에서 일어난다**: 에폭·트리 수(8곳) ·
A/B 승자 · 깊이·규제 스윕 · SEQ 형상 스윕 · τ*. **검증행은 채점에만 쓰인다.**
남는 낙관은 하나뿐이고 적어 뒀다 — `lbSel`/`lbs_` 는 τ* 도 보정구간에서 골랐으므로
그만큼 낙관이 남는다(후보 전체에 같은 방향이라 **순위는 보존된다**).

### AE-1(치명) — 조기중단이 ★채점표를 보고★ 멈추고 있었다 (학습기 8곳)
이 저장소는 같은 종류의 누출을 이미 두 번 막았다 — τ* 를 검증 앞 절반에서 고르던 것
(V33.341), SEQ 가 엠바고 없이 검증받던 것(B-2). **그런데 조기중단은 전부 그대로였다.**

```
XGB   num_boost_round=1000 · patience=90  · evals=[(dva,"v")]      ← 검증구간
LGB   num_boost_round=1000 · patience=90  · valid_sets=[lva]       ← 검증구간
CAT   iterations=1000      · patience=90  · eval_set=(Xva,Yva)     ← 검증구간
GBDT  MAXTREES=600         · PATIENCE=50  · vraw += apply_tree(t, Xva)
DNN   epochs≤120 × K시드   · patience≥15  · vl = BCE(net(Xva), Yva)
단타  num_boost_round=700  · patience=80  · valid_sets=[lva]
시장별 600판 + ★A/B 승자★ 도 검증 IC 로 선택
```
그리고 `acc` · `Wilson 하한` · `블록 IC` — **워커 승격 게이트가 보는 값 전부** 를
**그 같은 검증행**에서 냈다. 최대 1,000판 중 검증에 제일 잘 맞는 판을 골라
그 검증으로 채점한 것이다. **고른 자로 채점하면 그 값은 실력이 아니라 운을 포함한다.**

**고친 방향** — 학습 꼬리에서 **보정구간(cal)** 을 떼어 거기서 멈출 때를 고른다.
`_split_ts` 는 이미 그 기능을 갖고 있었다(τ* 용으로 DNN 만 쓰고 있었다). 엠바고가
검증과 갈라 놓으므로 누출이 없고, **검증은 채점에만 쓴다.**
단타는 `_split_ts` 를 안 쓰므로 학습 꼬리 10% 를 직접 떼고 **학습에서 뺐다**
(안 빼면 그 구간 예측이 부풀어 조기중단이 늦어진다).

**★이 변경은 보고되는 숫자를 낮춘다.★** 보정구간만큼 학습 표본도 10% 준다.
그래도 하는 이유: 부푼 숫자로 승격을 판정하면 **위원회가 실제보다 잘한다고 믿는
모델을 앉힌다.** 실거래 성과가 검증 수치를 못 따라가던 것의 일부가 여기 있다.
★사용자 지시("검증 하한을 낮추지 말고")는 문턱 이야기다 — 문턱은 하나도 안 건드렸다.
움직인 것은 잰 값이고, 그건 원래 그 값이었다.★

**크기를 적는다.** DNN 은 매 에폭 검증손실을 **기록만** 하고(선택엔 안 쓴다)
그 최솟값과 선택시점 값의 차이를 찍는다 — **그것이 종전 방식이 가져가던 이득**이다:
```
[조기중단] 기준=보정구간(학습 꼬리·엠바고로 검증과 분리)
          · 검증손실 선택시점 0.____ vs 전구간최저 0.____ → 종전 방식이 가져가던 이득 0.____
```

### AE-2(중) — 스윕 승자도 검증행에서 고르고 있었다
깊이·규제 후보 9개를 `lb`(검증행)로 줄세워 이긴 구성을 고르고, 그 `lb` 를 그대로
승격 점수로 올렸다. 후보 수가 9개뿐이라 에폭 선택(600~1,000판)보다는 **훨씬 작지만
같은 종류**다. **자(유효표본 Wilson 하한)는 그대로 두고 행만** 보정구간으로 옮겼다(`lbSel`).
`check-depth-sweep` 의 계약을 *"같은 자"* 에서 *"같은 자 × 다른 행"* 으로 올렸다.

**남는 것(적어 둔다)**: τ* 도 보정구간에서 고르므로 `lbSel` 에는 그만큼의 낙관이 남는다
(후보 전체에 같은 방향으로 실리므로 **순위는 보존된다**). **SEQ 형상 스윕**(후보 4개)은
아직 검증행 `accLB` 로 고른다 — 같은 병이고, 아직 안 고쳤다.

**게이트**: `check-earlystop-leak` 16단언 — 학습기 8곳의 학습부 블록을 **잘라서**
그 안의 평가집합 인자를 보고, 검증행이 섞이면 실패한다. 보정구간이 모자랄 때의 폴백은
허용하되 **다섯 곳 전부가 "이 회차 점수는 부풀어 있다" 고 말하는지** 센다.
돌연변이 **15/15**. 처음 두 개를 놓쳤다 — 하나는 게이트가 **주석**에 걸렸고(또 이 실수다),
하나는 폴백 경고를 하나 지워도 개수 하한을 넘겨서 통과했다(시장별 경로엔 **아예 없었다**).

### AD-1(중) — 드롭아웃 10층 × BatchNorm — ★재 본 적 없는 축★
같은 표본에서 **트리들은 53~54%, 이 망은 47.3%** 다. **동전보다 낮다.**
규제가 과한 것만으로는 이 모양이 잘 안 나온다. 구조를 보면:

```python
dims = 80-640-512-384-256-192-128-96-64-48-32-1     # 은닉 11층
x = lin(x); x = bn(x); x = relu(x); x = dropout(x, p=0.50)   # ★11층 전부★
```
드롭아웃과 BatchNorm 을 같이 쓰면 알려진 부작용이 있다 — **학습 중 BN 은 드롭아웃으로
흐트러진 입력의 통계를 러닝평균에 쌓는데, 평가 때는 드롭아웃이 꺼져 입력 분산이 다르다.**
쌓아 둔 통계가 틀린 분포를 가리키고, 층마다 겹치면 깊을수록 커진다.

**★단정하지 않는다.★** 두 가지를 넣었다 — 하나는 재는 장치, 하나는 고칠 수 있는 손잡이:

**① `[BN진단]` — 비용 0 의 측정.** 같은 검증행을 두 번 본다:
```
[BN진단] 러닝통계 47.32% vs 배치통계 __._%  → 차이 +_._%p (배치통계는 진단용·배포 점수 아님)
```
크게 갈리면 "규제가 과하다" 가 아니라 **"통계가 틀렸다"** 는 뜻이다. 배치통계 값은
검증집합 자신의 통계를 쓰므로 **배포 가능한 점수가 아니다** — 로그가 스스로 그렇게 말한다.
게이트는 이 값을 **안 본다**(진단이 판정을 흔들면 안 된다). 진단이 BN 러닝통계를
건드리므로 **버퍼를 떠 놓고 되돌린다** — 진단하러 들어가서 업로드될 모델을 바꾸면 안 된다.

**② `dropTail` — 드롭아웃을 ★몇 번째 층부터★ 걸지.** `0` = 전 층(**종전과 완전히 동일**),
`k>0` = 마지막 k 개 은닉층에만. 스윕 격자에 **`드롭아웃 꼬리2`** 를 넣었다 —
세기는 사다리 그대로 두고 **위치만** 바꾼 점이다. 세기만 흔드는 격자(0.5×·0.2×)로는
"전 층 p=0.5 × BN" 이 문제인지 영원히 알 수 없다.

★값을 내가 고르지 않았다 — 측정이 고른다.★ V33.386 이 이은 고리 덕에 스윕 승자는
이제 다음 회차부터 실제로 적용된다. 두 변경이 맞물려야 뜻이 있다.

**게이트**: `check-reg-measured` 32단언. `forward` 의 층 선택 조건을 **소스에서 떼어 내**
층 번호 0~10 을 흘려 `drop_tail=0 → 11층 전부`, `=2 → 마지막 두 층`, `=99 → 전 층에서 멈춤`
(음수 인덱스 금지)을 확인한다. BN 진단은 **러닝통계·`num_batches_tracked` 복원**과
`eval()` 복귀까지 못 박는다. 돌연변이 **18/18**.

### AC-1(중) — 측정-반영 고리가 ★한 축만★ 이어져 있었다
V33.204 가 구성 스윕을 만들었고, V33.260 이 "측정이 매번 버려진다" 를 고쳤다.
그런데 스윕은 **두 축**을 잰다 — 깊이와 규제. 고쳐 붙인 것은 **깊이뿐이었다.**

```
스윕: 규제 현행 / 규제 완화(0.5×) / 규제 최소(0.2×) 를 같은 표본·분할·시드로 잰다
  → 승자 _reg_win 을 그 실행의 최종 학습에 쓴다
  → POST /api/dnn-arch 본문: { featVer, hidden, lb, acc, auc, n, ranking }
                                        ↑ 규제가 없다
  → 다음 정기 회차: 사다리(Nall 기준 do=0.50 l2=1.5e-3 mix=0.25 noise=0.08)로 복귀
```
**"규제 완화가 낫다" 를 몇 번을 재도 다음 회차는 언제나 사다리로 돌아온다.**
트레이너 주석은 *"사다리를 기본값으로 두고, 스윕이 재서 이긴 구성이 있으면 그것을 쓴다"*
라고 적혀 있었다 — **절반만 참이었다.** 규제는 스윕이 끝나는 순간 사라졌다.

이게 "AI 성능이 안 난다" 와 바로 이어진다. 사다리 맨 윗칸(`dropout 0.50` × 은닉 11층
+ BatchNorm, `mixup 0.25`)은 **표본 18만 시절에 손으로 쓴 값**이고 지금 표본은 51만이다.
그 값이 과한지 아닌지를 **재는 장치는 이미 있었는데 그 답을 쓸 길이 없었다.**

**고친 것** (측정 → 저장 → 전달 → 적용, 네 구간 전부):
| 구간 | 종전 | 지금 |
|---|---|---|
| 트레이너 업로드 | `hidden` 만 | `reg{dropout,l2,mixupP,inputNoise}` 동봉 |
| 워커 저장 | 받지도 않음 | 범위검사 후 `dnn_arch.reg` 에 저장 · **부분 채택 금지** |
| 워커 판정 | `{hidden,...}` | `_dnnArchDecide` 가 `reg` 도 돌려준다(**featVer 같이 탄다**) |
| 워커 전달 | — | `_mlExportConfig` 이 `regMeasured` 로 내려보낸다(없으면 **키 자체가 없다**) |
| 트레이너 적용 | 사다리가 덮어씀 | `regMeasured` 가 유효하면 사다리 대신 쓴다 · **어느 쪽을 썼는지 로그로 말한다** |

**격자가 걸어다니던 것도 같이 고쳤다.** 스윕 배수를 `_reg_base`(=지난 승자)에 걸면
회차마다 곱해진다(0.5 → 0.25 → 0.125…) — 규제가 한 방향으로 미끄러지고 회차끼리
비교가 안 된다. 배수는 **고정된 사다리**에 걸고, 지난 승자는 `규제 현행` 으로,
사다리는 `규제 사다리` 로 **같은 격자 안에서 방어**하게 했다(값이 같으면 중복 제거).

**★게이트가 내 버그를 잡았다★** — 워커는 `mixupP` 로 보내는데 트레이너는 `mixup_p` 로
읽고 있었다. `KeyError` → `except` → 조용히 사다리로 복귀. **고치기 전과 똑같이 동작하면서
고쳤다고 보고할 뻔했다.** `check-reg-measured` 가 워커 함수를 진짜로 호출하고
트레이너 블록을 떼어 내 Python 으로 돌려서 잡았다. 같이 잡힌 것: 규제가 **전부 0**
(=`규제 최소` 가 이긴 경우)인 승자가 falsy 로 버려지는 문제.

**게이트**: `check-reg-measured` 23단언 · **돌연변이 9/9** — 잰 값 무시 · 전달 누락 ·
`_dnnArchDecide` 반환 누락 · 업로드 누락 · 격자 앵커 되돌림 · 라우트 범위검사 제거 ·
사다리 후보 제거 · 트레이너 범위 상한 무력화 · 0 규제 폐기.

### AB-1(중) — "과소적합" 판정은 ★성립하지 않는 비교★ 위에 있었다 ★내 잘못★
V33.378 에서 나는 `train 47.17% vs val 49.23% → 격차 -2.07%p` 를 보고
"과적합이 아니라 과소적합" 이라고 판정하고 규제를 낮추는 쪽으로 처방을 적었다.
**그 비교 자체가 성립하지 않는다.**

```
train_acc = ((ptr>=0.5) == (ytr>0.5)).mean()      ← ★가중 없는★ 전수 평균
loss      = BCE(ptr, ytr) * mw                     ← 학습은 mw 가중으로 한다
```
모델이 **일부러 덜 보도록 만든 표본**이 `train_acc` 를 지배한다. 검증(최근 240일)과
비교하면 **서로 다른 문제를 푼 성적표**를 나란히 놓는 것이다. 격차의 부호조차 믿을 수 없다.

→ `train_acc` 를 **학습과 같은 가중**(`mw[tr]`)으로 다시 잰다. 종전의 균등값은
`train_acc_u` 로 **같이 찍는다** — 둘이 갈리면 그 자체가 신호다.
`gap = train_acc - acc` 는 이제 같은 자로 잰 두 값의 차다.

### AB-2(중) — 반감기 45일 손잡이가 ★규모가 커지면서 조용히 죽었다★
`recency = max(rec_floor, 0.5 ** (days / hl_days))` · `hl_days=45` · `rec_floor=0.35`.
표본이 **7.4년**으로 늘어난 지금, 반감기 45일이면 1년 전 표본은 이미
`0.5**8.1 ≈ 0.004` 로 **전부 바닥값 0.35 에 붙는다**. 실측 재현:

```
학습표본의 97.2% 가 recency 바닥(0.35)에 붙어 있고
그들이 전체 손실 가중의 95.2% 를 차지한다
```
**이름은 "최근을 더 본다" 인데 실제로는 거의 균등가중이다.** 손잡이를 아무리
돌려도(45일→30일→90일) 바닥에 붙은 97% 는 꿈쩍하지 않는다 — 움직이는 것은 `rec_floor` 뿐이다.
값을 바꾸는 것은 **성능에 직접 영향**을 주므로 여기서 임의로 돌리지 않았다.
대신 **매 회차 로그에 노출**한다:
```
[최근성] 반감기 45일 · 바닥 0.35 → 학습표본의 97.2% 가 바닥에 붙어 있고
        그들이 가중의 95.2% 를 차지한다  ★이름과 달리 최근을 거의 안 당긴다★
```
`_wfloor > 0.8` 일 때만 별표가 붙는다. **사람이 보고 정할 자리다**(`rec_floor` 를 낮출지,
`hl_days` 를 표본 기간에 비례시킬지) — 근거 없이 내가 반대로 밀지 않는다(V33.260 규칙).

### AB-3 — ★가설을 세우고, 재보고, 버렸다★ (가중치 정규화는 원인이 아니다)
스윙 DNN 은 `UNIQ`(de Prado 고유도, 평균 0.033)를 **정규화 없이** 곱하고,
스칼프 트레이너는 `np.clip(uniq / uniq.mean(), 0.25, 4.0)` 로 **정규화해서** 곱한다.
"스윙 쪽 유효표본이 깎여서 과소적합" 이라는 가설을 세웠다.

**재보니 틀렸다.** Kish 유효표본 `(Σw)²/Σw²` 은 **척도불변**이다:
```
raw UNIQ        → n_eff = 44.0%
normalized UNIQ → n_eff = 45.1%     (차이 1.1%p — clip 때문일 뿐)
```
**변경을 넣지 않았다.** 실측이 가설을 기각했으므로 코드는 그대로 둔다.

**게이트**: `check-fit-diagnosis` 를 18개 단언으로 확장.
`train_acc` 가 `mw[tr]` 로 가중되는지를 **AST 로 추적**하고, 균등값이 따로 남는지,
`[최근성]` 로그가 실제 `print(` 인지(주석 처리로 죽지 않는지)를 못 박는다.
돌연변이 5종 중 처음에 **"최근성 로그 제거" 를 놓쳤다** — 게이트가 문자열만 봐서
주석 처리해도 통과했다. `print(f"   [최근성] 반감기` 를 앵커로 바꿔 잡았다.


### AA-1(치명) — 굶주림 → 자동 트리거 → 비용 폭주 ★되먹임 고리★
워커 `_luxAutoRetrainModal` 은 외부 모델이 낡으면 GitHub 워크플로를
`inputs: { run_now: "true" }` 로 디스패치한다 — **52분짜리 GPU 회차 한 판**이다.
그 조건이 **`oldestAge`(가장 오래된 외부 모델) > 14h** 였다.

그런데 Modal 회차는 예산 안에서 단계를 **회전**시키므로 가장 오래된 모델은 언제나
회전 주기(실측 24~31h)만큼 묵어 있다 → **구조적으로 영구 참**.
```
예약 크론 4회/일 + 자동 트리거(쿨다운 8h) 최대 3회/일 = 하루 최대 7회 × 52분
≈ 월 180 GPU시간 → 약 $106/월
실제 청구: 2026-09-01~18 에 $65.15 = 월 $108 환산   ★계산이 청구서와 맞는다★
```
**W-3 굶주림이 비용 폭주의 원인이었다.**

→ 판정을 **가장 신선한 모델**로 바꾼다. 둘은 전혀 다른 사건이다:
- `freshestAge` 가 낡음 → **학습 자체가 안 돈다**(진짜 사고) → 트리거
- `oldestAge` 만 낡음 → 회전이 아직 차례를 안 줬다(정상) → 트리거 안 함

**안전망은 그대로다** — 모델 없음·판 불일치·전부 낡음은 여전히 즉시 디스패치.
`check-retrain-loop` 이 판정식을 **떼어 내 9가지 경우로 돌려** 확인하고,
`oldestAge` 를 15~168h 로 흔들어도 **트리거되지 않음**을 못 박는다. 돌연변이 6/6.

### Modal 크론 4회/일 → ★2회/일★ (사용자 지시)
`10 */6 * * *` → `10 */12 * * *`. 회차 전체가 T4 박스에서 도므로 회차 수 = GPU 청구시간.
**월 $62 → 약 $31.** CPU 단계도 절반이 되지만 **한 바퀴는 오히려 빨라진다** —
굶으면 V33.377 예약이 DNN 시드를 양보시키기 때문이다:
```
시드 6 → 후속 814s × 4회 = 3,256s/일 → 한 바퀴 31시간
시드 2 → 후속 2,054s × 2회 = 4,108s/일 → 한 바퀴 ★24시간★
```

### 사이트는 ★접속 없이 돈다★ (확인 완료)
```toml
[triggers]
crons = ["*/1 * * * *"]      ← 1분마다 Cloudflare 가 워커를 깨운다
```
`scheduled()` 가 **엔진 작업 41종 전부**를 돌린다 — 매매·수확·야간학습 7단·LLM·원장감사.
접속 시 보이는 것은 대시보드가 **그 결과를 읽어오는 것**뿐이다.
멈추는 조건은 둘뿐이고 둘 다 수동/한도다: `cfg.forceLock === true`,
사용량 90% 도달(월 요청 10M / CPU 30M ms — 1분 크론으로는 월 4.3만이라 닿지 않는다).

### Z-5 — 크론은 정상. 문제는 ★회차 용량★ 이다
증거: 화면 "DNN 3.5시간 전 / GBDT 27.2시간 전". 6시간 주기인데 **DNN 만 신선**하다 =
크론은 뛰고 뒤 단계가 굶는다. 실측 회차 예산 분해:
```
3,300s = 수집 571 + DNN 6시드 1,860 + 마무리 55 + ★후속 814★
후속 8단계 총량 4,179s  →  전 단계 한 바퀴 5.1회차 = ★31시간★
```
**주기를 늘리면 더 나빠진다** — 회차 수가 줄어 한 바퀴가 더 길어진다. 고칠 곳은 셋이다:

| 시드 | DNN | 후속 예산 | 한 바퀴 |
|---:|---:|---:|---:|
| 6 | 1,860s | 814s | 31시간 |
| 4 | 1,240s | 1,434s | 17시간 |
| 3 | 930s | 1,744s | 14시간 |
| 2 | 620s | 2,054s | **12시간** |

| timeout | 후속 예산 | 한 바퀴 | GPU 비용 |
|---:|---:|---:|---:|
| 60분(지금) | 1,434s | 17시간 | **$62/월** |
| 90분 | 3,234s | 8시간 | $93/월 |
| 120분 | 5,034s | 5시간 | $124/월 |

**고친 것(무료)**: 상태 저장소가 죽으면 `last_ok` 가 안 남아 모든 단계가 "한 번도 안 돎"
으로 같아지고 순서가 **언제나 기본 순서**가 된다 — W-3 교착의 재발이다.
→ 왕복으로 저장소를 확인해 **로그가 말하게** 하고, 죽었으면 **시각 기반 회전**으로 돌린다.
그리고 회차마다 **한 바퀴가 몇 시간인지**를 숫자로 찍고, 하루가 넘으면
"주기를 늘리는 것이 답이 아니다" 라고 로그가 직접 말한다.

### Z-6 — GPU 비용 주석이 ★60배★ 틀려 있었다
적혀 있던 말: `12h마다 2분이라 월 크레딧 $1 수준(무료 $30 내)`.
실측: 크론은 **12h 가 아니라 6h**, 회차는 **2분이 아니라 52.4분**.
→ 52.4분 × 4회/일 × 30일 = **104.8 GPU시간/월 ≈ $62/월**. 무료 $30 을 **두 배 넘긴다.**
게다가 그 시간의 **약 40%는 GPU 를 안 쓴다**(수집·부스터·단타·MEMO·시장별은 CPU 연산인데
T4 가 회차 내내 붙어 있다) — 월 약 $25 가 놀고 있다.
GPU/CPU 함수 분리로 줄일 수 있지만 표본을 두 번 받아야 하고 방금 D1 이 무너진 참이라(Z-3)
**추측으로 지금 넣지 않았다. 숫자만 남긴다 — 사람이 정할 일.**

### 코드 규모(2026-09-17)
`src/index.js` 50,598 · `tools/*.mjs` 25,753 · `public/index.html` 20,075 ·
`trainer/*.py` 3,385 · workflows 2,080 · `public/*.js` 445 · `tools/*.sh` 136
→ **코드 102,472줄** (+ 문서 5,647 = 108,119)

### Z-3 — "비싸서 실패" 에 ★같은 질의를 다시 던지는★ 재시도는 안 듣는다
```
RuntimeError: export 500: {"error":"D1_ERROR: D1 DB exceeded its CPU time limit
                           and was reset."}   at fetch_all()
```
재시도는 **이미 있었다**(5회 · 5/10/20/40초). 그런데 못 넘었다 — 당연하다.
실패 원인이 "질의 하나가 너무 비싸다" 인데 **똑같이 비싼 질의**를 다시 보냈기 때문이다.
기다림은 일시적 혼잡에는 듣지만 **질의 자체의 비용**에는 안 듣는다.

→ 실패하면 **페이지를 반으로 줄인다**(20,000 → 10,000 → … → 2,000 바닥).
D1 의 CPU 한도는 질의 단위라 쪼개면 질의당 비용이 절반이 된다.
**대가**: 페이지가 20,000 의 배수를 벗어나면 워커의 R2 스냅샷 분기가 파트를 못 골라
D1 경로로 떨어져 그 회차 수집이 느려진다. **느린 수집이 죽은 50분보다 낫다.**

`check-export-resilience` 가 재시도 루프를 **떼어 내 실제로 돌려** 확인한다:
한 번에 성공하면 안 줄인다(무해성) · 실패하면 줄어든다 · 바닥이 있다 ·
끝까지 실패하면 **던진다**(반쪽 표본으로 조용히 학습하지 않는다). 돌연변이 6/6.

**※ 따로 볼 것**: 애초에 이 오류가 났다는 것은 **R2 스냅샷이 서빙하지 않고 D1 이 받고
있었다**는 뜻이기도 하다(스냅샷이 살아 있으면 D1 CPU 한도에 닿지 않는다).
I-2 고침이 `MLSNAP_SCHEMA` 를 3 으로 올렸으니 스냅샷 재생성이 필요할 수 있다 — **미확인**.

### ★Y-2 확인 — 단타가 처음으로 위원회에 앉았다★
run 35186033702 (2026-09-17 05:54) 로그 그대로:
```
valAcc 0.5637 (하한 0.5537, 유효 n=6637/26224) IC 0.1978 RankIC 0.1365 / 트리 44
blockIC 0.1935 t 7.77 (유의성 게이트용)        ← 이 줄이 _vai 로 죽던 자리다
단타모델 업로드 OK: {"ok": true, "trusted": true, "valAcc": 0.5637,
                    "valAccLB": 0.5537, "baseline": 0.5575, "n": 104897, "reason": "ok"}
```
**통과 경로는 정확도가 아니라 IC 였다.** 워커의 실제 baseline 은 0.5575 라
정확도 경로 문턱은 0.5725 인데 하한은 0.5537 — 정확도로는 미달이다.
통과시킨 것은 `blockIC 0.1935 ≥ 0.015` 와 `t 7.77`(문턱의 4.7배)이고,
**그 두 숫자를 만드는 줄이 바로 `_vai` 로 죽던 자리다.**
즉 이 모델은 실력이 모자라 못 들어온 것이 아니라 **자기 실력을 증명할 숫자를 만들다 죽어서**
못 들어왔다. (내가 앞서 "정확도 경로로 통과할 것" 이라 한 예측은 틀렸다 — baseline 을
트레이너 로그값 0.537 로 읽었는데 워커 실제값은 0.5575 였다. 결과는 통과지만 경로가 달랐다.)

### Z-2 — 단계 이름이 세 곳에 따로 적혀 있어 또 갈렸다
`target=ablate` 실행이 `ValueError: unknown training target` 으로 **시작도 못 했다**
(run 35187653387). V33.380 이 `_PLAN`·워크플로에는 넣고 **검증 튜플**에는 안 넣었다.
→ 검증을 `STAGE_COST_DEFAULT` 에서 파생시켜 **손목록을 없앴다.**
그리고 게이트가 곧바로 **두 번째 드리프트**를 잡았다 — 워크플로 목록에 `gbdt` 가 빠져 있어
GBDT 만 따로 복구할 길이 없었다. 셋이 정확히 같은지를 `check-trainer-budget` 이 지킨다.

### ★Z-1 — 구조적 문제는 모델이 아니라 라벨·지평이다(증거)★
11층 MLP 와 부스팅 트리는 다른 모델족인데 스윙에서 똑같이 작은 엣지로 수렴한다:
DNN 49.3% · 부스터 52.2~52.5% · IC 0.003~0.032 (다수클래스 50.8%).
같은 파이프라인의 단타(60분 삼중배리어)는 **57.0% · IC 0.207 · 고유도 0.333**.
두 모델족이 같은 벽에 부딪히면 그 벽은 **라벨·지평**이다.

`sign(pnl)` 에는 **데드밴드가 없다** — +0.02% 와 −0.02% 를 1/0 으로 가른다.
그 띠가 정확도 상한을 누르고, 지평 10일이 고유도 0.033 의 원인이기도 하다.

→ **바꾸지 않고 잰다.** `ablate` 단계(`_label_ablation`)가 같은 분할·같은 학습기로
운영/데드밴드/횡단면/변동성정규화 라벨을 각각 학습해 **적용률과 함께** 표로 찍는다.
**아무것도 업로드하지 않고 문턱도 안 건드린다** — `check-label-bench` 가 못 박는다.

**다음에 할 것**: `target: ablate` 로 한 회차 돌려 표를 받고, 그 숫자로 사람이 정한다.
**60% 는 약속하지 않는다** — 10일 sign 라벨에서는 이 증거로 불가능에 가깝다.
가능한 길은 (1) 데드밴드(적용률↓·정확도↑) (2) 단타 슬리브 확대(이미 57%)이고,
둘 다 문턱이 아니라 **문제 정의**를 바꾸는 것이다.

### ★Y-2 — 이 시스템에서 제일 잘하는 모델이 업로드에서 죽고 있었다★
run 35182171750 (2026-09-17 05:19) 로그 그대로:
```
⑧ 단타(장중) 학습 — 표본 104544건 / 최근 14일 … / 장중피처 v5×44
   valAcc 0.5700 (하한 0.5600, 유효 n=6588/26136) IC 0.2073 RankIC 0.1433 / 트리 170
   scalp 예외(무시): name '_vai' is not defined
```
| 모델 | valAcc | 하한 | IC |
|---|---|---|---|
| **단타(SCALP)** | **57.0%** | **56.0%** | **0.207** |
| 부스터(xgb/lgb/cat) | 52.2~52.5% | 51.3~51.6% | 0.056~0.062 |
| DNN | 49.3% | 48.4% | −0.003 |

다수클래스 기준선 53.7% 를 크게 넘고 IC 도 한 자릿수가 다르다. **그런데 마지막 업로드
줄에서 `NameError: _vai` 로 죽어 워커에 한 번도 닿은 적이 없다.**
W-4 로 이미 고쳐 커밋했지만(V33.377) — 아래 Y-1 때문에 **그 고침이 Modal 에 안 올라가 있었다.**

### ★Y-1(치명) — 고친 코드가 Modal 에 못 가고 있었다★
04:30 학습(50분)이 `modal-training` 동시성 그룹을 잡고 있는 동안 들어온 실행이 전부 취소됐다:
```
35183524091 (a865c81 · V33.377 푸시)  → cancelled
35183933828 (수동 target=memo)        → cancelled
35184243721 (a865c81)                 → cancelled
35184328375 (9425dce · V33.378 푸시)  → cancelled
```
GitHub 는 `cancel-in-progress: false` 에서도 **대기열을 하나만** 유지한다 — 뒤에 들어온
실행이 앞의 대기분을 밀어낸다. 결과: **V33.377·V33.378 의 트레이너 고침(MEMO import ·
단타 `_vai`)이 Modal 에 한 번도 배포되지 않았다.** 그리고 실행은 `cancelled` 로만 남아
실패로 보이지도 않는다. **O-1 과 완전히 같은 병이다.**

→ **배포와 학습은 같은 자원이 아니다.** `modal deploy` 는 20초짜리 멱등 연산이다.
작업을 둘로 쪼개고 동시성 그룹을 **학습 작업에만** 걸었다. 이제 `trainer/modal/**` 푸시는
학습이 도는 중에도 항상 배포된다. `check-deploy-not-blocked` 가 구조를 YAML 로 파싱해 지킨다.

### 이번 로그로 재확인된 것
- `[표본위생] 811,423/1,177,451 (68.9%) · 시간범위 2694일` — I-2 정상
- `[DNN] [홀드아웃] 240일 → 정직한 블록 12개 / 목표 12개` — U-1 정상
- `⑪ STACK 홀드아웃 경계 통지 — minTs=… (247479건)` — **성공**(경계가 앞으로 갔다. W-2 는 되감기에만 걸린다)
- `MEMO 업로드 예외: name 'requests' is not defined` — W-1 재확인(고침 미배포 상태였음)
- `⏭ mind · seq · gbdt · markets 생략` — W-3 재확인(4단계)
- `[과적합진단] … 격차 -1.85%p — 과적합 낮음(→피처 품질이 병목)` — X-1 재확인
- 부스터 t: xgb 1.10 · lgb 1.48 · cat 1.36 — **여전히 1.65 미달. 문턱은 안 내린다.**

### ★V33.378★
- **X-1** 과적합 진단 분기가 둘뿐이라 `격차 -2.07%p` 가 "과적합 낮음(→피처 품질이 병목)" 으로
  흘러들어갔다. 음수 격차는 **과소적합**이고 처방이 정반대다. 세 번째 분기를 넣고,
  τ* 를 되돌린 학습정확도를 함께 찍어 **과소적합 / 보정 전이**를 사람이 가를 수 있게 했다.
- **X-2** 고유도 수식이 de Prado 정의보다 **2.1× 보수적**임을 실측으로 확인. **고치지 않았다** —
  효과가 +0.3%p 뿐이고, 승격 게이트를 느슨하게 하는 방향이며, 현재 동작은
  `check-uniqueness` 가 명시적으로 고정한 계약이다. **사람이 정할 일**로 남긴다.

### ★V33.377 — Modal 학습 로그 실측으로 잡은 것★
근거: run 35149059451 (2026-09-16 20:52~21:40) 전체 로그. 화면의 "위원회 3/10 가동" 과 대조.

| | 무엇이었나 | 결과 |
|---|---|---|
| **W-1** | MEMO 업로더에 `requests`·`json` import 누락 | **MEMO 업로드가 한 번도 성공한 적 없음** |
| **W-2** | STACK OOF 되감기가 섀도우 자리를 안 봄 (`oldestExpertAt: 0`) | **STACK 전진표본 0 — 영원히 보류** |
| **W-3** | 단계 회전 교착 — 커서가 `mind` 에 고정 | **5단계가 매 회차 생략**(GBDT 27.2h) |
| **W-4** | 단타 IC 산출 `NameError: _vai` + 지평 10일(단타는 60분) | **굶주려서 안 드러나 있었음** |
| **W-5** | 워치독 `modal run --detach` 가 블로킹 | **3회 연속 15분 컷 cancelled** |
| **W-6** | 굶주림 게이트의 시뮬레이션 상수가 드리프트 | **초록불이 거짓이었음** |

**★고친 것이 듣는 증거(같은 로그)★**
```
[표본위생] 서로 다른 표본 811,411/1,177,439 (68.9%) · 시간범위 2693일
[DNN] [홀드아웃] 240일 · 247667건 → 정직한 블록 12개 / 목표 12개 (전체 2693일)
```
I-2(V33.365)와 U-1(V33.376) 둘 다 설계대로 작동한다.

**★그래도 아직 못 넘는 것 — 문턱은 안 내린다★**
```
xgb t=1.22 · lgb t=1.45 · cat t=1.15   (문턱 1.65, K=12)
표본 고유도 0.033 · 유효표본 8,187/247,667 · 평균동시성 30.3건
```
K 를 12로 맞춰도 t 가 거의 안 움직였다 — ICIR 자체가 작다. 그리고 `accLB` 의 진짜 병목은
**고유도 0.033**(종목당 하루 ~3건 수확 × 지평 10일 = 동시성 30)이다. → `W-7` 로 남긴다.

**다음 Modal 회차(6시간)에서 볼 줄**
```
MEMO 업로드 200 …                       ← W-1 (종전엔 '업로드 예외')
⑪ STACK 홀드아웃 경계 통지 — minTs=…     ← W-2 (종전엔 409)
굶은 순서: scalp(한 번도) · seq(…h) · …  ← W-3
[예산예약] 굶은 단계 N종 — DNN 이 …s 를 양보한다
```
워치독은 다음 예약(6시간)부터 `cancelled` 대신 1~2분 만에 초록이어야 한다.

### ★V33.376 — "왜 다 섀도우·잠정인가" 에 손을 댄다(U-1)★
G-2 실측: XGB·LGB·CAT 의 홀드아웃 t 가 **1.36 · 1.48 · 1.14** — 문턱 1.65 **바로 아래**다.
그 t 를 만든 독립 블록 K 는 **7~12개**뿐이었다. `t = ICIR × √K` 이므로 K 가 12→24 면
같은 실력으로 t 가 **1.41배**가 된다(1.36 → 1.92).

그런데 K 는 행 수가 아니라 **달력 기간**에서 나오는데, 분할은 기간을 **한 번도 안 봤다** —
행의 20% 를 떼고 끝이었다. 표본이 늘어도(I-2 고침으로 실제로 크게 는다) K 는 그대로다.

→ 홀드아웃을 **기간**으로 정한다(목표 블록 12개 = 240일). **문턱은 한 톨도 안 건드렸다.**
학습을 굶기지 않도록 상한 두 개(행 33% · 학습기간 ≥ 2×홀드아웃)를 걸고, 못 닿으면
**로그가 무엇이 막았는지 말한다.** 절대 줄이지는 않는다.

**다음 Modal 학습(6시간 주기)에서 확인할 줄**
```
[DNN]  [홀드아웃] NNN일 · NNNN건 → 정직한 블록 N개 / 목표 12개 (전체 NNNN일)
[표본위생] …            ← I-2 고침의 증거(중복 비율)
표본 고유도 … 유효 N     ← ~7,000 근처로 돌아오는지
```
블록이 12개에 닿으면 XGB·LGB·CAT 의 t 가 문턱을 넘을 수 있다 — **넘지 못하면 그건
실력이 없다는 뜻이고, 그때는 문턱이 아니라 모델을 고쳐야 한다.** 문턱은 안 내린다.

### ★V33.375 — 저장소를 공개로 돌린 뒤 남아 있던 구멍★
공개 전환으로 CI 는 살아났지만(R-1), 같이 내려간 것이 있다: **워커 주소를 알아내는 난이도**.
이 대시보드에는 로그인이 없고, `mutationGuard`(V33.193)는 **쓰기만** 막는다 —
그 주석 자체가 "읽기(GET)에는 아무 영향이 없다" 라고 적고 있었다. 즉 주소를 아는 사람은
`/api/state`·`/api/trades`·`/api/ai-positions`·`/api/download/*` 로 **보유종목·현금·체결이력을
전부 읽을 수 있었다.** 게다가 `Access-Control-Allow-Origin: *` 이라 **아무 웹사이트의 스크립트나**
방문자 브라우저를 통해 그 본문을 가져갈 수 있었다.

세 가지를 걸었다(전부 `tools/check-viewer-gate.mjs` 가 **실제로 요청을 물려** 확인한다):

| | 무엇 | 지금 상태 |
|---|---|---|
| ① | **읽기 문** — `VIEW_KEY` 를 아는 요청만 읽는다 | **꺼져 있다**(시크릿 미설정 = 완전 무동작) |
| ② | **CORS 축소** — ACAO 를 `*` → 동일 출처 | 즉시 적용(동일 출처라 화면은 영향 없음) |
| ③ | **noindex** — `X-Robots-Tag` + `/robots.txt` | 즉시 적용 |

#### ★읽기 문을 켜는 법 (사용자가 폰에서 할 수 있다)★
1. Cloudflare 대시보드 → Workers & Pages → 이 워커 → Settings → **Variables and Secrets**
2. **Add** → Type **Secret** → Name `VIEW_KEY` → Value 는 길고 무작위한 문자열(20자 이상)
3. Save → 배포 없이 곧바로 적용된다.
4. 폰에서 `https://<주소>/?k=<그 키>` 로 한 번 접속 → 쿠키가 심기고 주소에서 키가 지워진다.
   그 다음부터는 **평소처럼 주소만 열면 된다**(180일).

**끄는 법: 같은 화면에서 `VIEW_KEY` 를 지운다.** 코드를 되돌리거나 재배포할 필요가 없다 —
이 탈출구가 설계의 1번 원칙이다(사용자는 로컬 복구 수단이 없다).

- CI 진단 워크플로(`worker-logs`·`worker-probe`·`modal-watchdog`·`train-now`)는 `TRAIN_KEY` 를
  헤더로 보내도록 고쳤다 — 문을 켜도 그대로 돈다. `tools/deploy-local.sh` 도 401 을
  "배포 실패" 로 잘못 읽지 않고 **"문이 켜져 있다"** 고 정확히 말한다.
- 정직하게: 이건 **자물쇠지 금고가 아니다.** 코드가 공개라 공격자는 문의 모양을 안다.
  막는 것은 "주소를 알아낸 사람이 그냥 읽어 가는 것" 이고, 못 막는 것은 "키를 아는 사람"
  그리고 "남이 소스를 받아 **자기 복제본**을 배포하는 것"이다 — 후자는 공개 소스에서
  막을 수 있는 종류가 아니다. 이 문이 지키는 것은 **이 인스턴스와 그 안의 돈·이력**이다.
- 돌연변이 시험 **16/16**: 탈출구 삭제 · 접두사 비교 · TRAIN_KEY 길 제거 · 쿠키 길 제거 ·
  실패 계수 누락 · 주소에 키 남김 · HttpOnly 제거 · 쿠키 1시간 · API 302 · 판 번호 노출 ·
  키 되비침 · 빈 본문 · X-Robots-Tag 제거 · ACAO `*` 복귀 · 쿠키 접두사 매칭 — 전부 잡힌다.

### ★배포 성공 — 확인된 사실★
저장소 공개 전환 후 재실행에서 **모든 단계가 success**:
```
1~141  게이트 전부 success   (numpy 설치가 5단계로 올라가 68단계 통과)
142    Install wrangler      success
144    Enable R2 binding     success   ← 장중 표본 수집 경로 살아 있음
146    Deploy                success   05:12:53 → 05:12:58
```
**V33.365~373 이 한 번에 라이브가 됐다** — I-2(표본 110만이 2만의 복사본) 고침 포함.

### 피처 점검 (사용자 지시: "특히 피쳐쪽에 문제 생겼는지 확인")
**결론: 피처 쪽에 결함은 없었다.** 세 방향으로 확인했다.
1. 피처 80칸을 **운영과 같은 모양의 인자**로 120가지 흔들었다 → 상수는 7개뿐, 전부 이유가 있다.
2. 처음엔 18칸이 상수로 나왔는데 **맥락을 안 넘긴 내 하네스 탓**이었다
   (`idxCloses`·`sectorCloses`·`xsPanel`·`obsTs`). 운영 호출부는 전부 넘긴다.
3. 단타 경로는 일부러 null 을 넘기지만 **같은 벡터가 표본 수집과 라이브 판정 양쪽에** 쓰여
   스큐가 없다(20830 vs 20885 확인).

**다만 그 과정에서 둘을 고쳤다**
- **S-1** FOMC 표가 끝난 뒤 400일 유예 구간에서 '다음 회의' 를 **상한상수로 지어내며 known=1**
  이라 했다. 실측 2028-06: `to=45 since=45 known=1` — 둘 다 상한. → 모르면 모른다고 답한다.
  ★오늘 동작은 안 바뀐다★(표가 2027-12 까지). 기존 게이트는 표 **이전**만 봤다.
- **S-2** 죽은 피처 탐지가 **선언 목록 크기만** 봤다 — 선언 없이 데이터가 끊긴 칸(F-2 유형)은
  못 봤다. `check-feat-alive` 신설. 돌연변이 4종 전부 검출.

### 다음에 볼 것 (배포됐으니 이제 실측이 나온다)
1. **다음 Modal 회차 로그의 `[표본위생]`** — I-2 고침의 증거. **제일 중요하다.**
2. 그다음 `표본 고유도: … 유효 N/…` 이 **7,000대**로 돌아오는지(210 이 아니라).
3. `[EVAL-COST]` 한 줄(C-1) · 자가진단 '시세' 한 문장(A-6) — 남은 결함 둘이 여기서 갈린다.
4. 폰에서 다운로드·흰 화면·`시스템 준비 N/M` 확인(N-1·N-3·N-4).

### 앞선 판 (V33.373) — 배포가 풀렸다
사용자가 저장소를 공개로 전환했다(`"private": false` 확인). 공개 저장소는 GitHub Actions 가
무제한이라 **러너가 즉시 배정된다** — 재실행에서 67단계가 실제로 돌았다
(종전엔 `runner_id 0` · steps 없음이었다).

### R-1 — numpy 설치가 게이트보다 뒤에 있었다 (CI 에서만 나는 실패)
러너가 살아나자 **68단계 `Block-IC honesty check` 에서 배포가 멈췄다**:
```
ModuleNotFoundError: No module named 'numpy'
```
파이썬으로 수학을 재현하는 게이트가 10종인데 러너에는 numpy 가 없다.
설치 단계는 **111단계**에 있었고, V33.366 이 추가한 게이트는 **68단계**였다.
**로컬엔 numpy 가 있어 136종 전부 통과했다 — CI 에서만 나는 실패였다.**

★이 저장소는 이 함정을 두 번 밟았다.★ 처음에도 "설치를 그 검사 ★바로 앞★ 에" 두는 식으로
고쳐서, 나중에 앞쪽에 추가된 게이트가 또 걸렸다.
→ 설치를 **맨 앞(esbuild 다음)** 으로 옮기고, **순서를 `check-ci-budget` 이 강제**한다
  (설치가 첫 게이트보다 뒤면 실패 · 설치가 없어도 실패). 돌연변이 2종 전부 검출.
※ "없으면 건너뛴다" 로 고치지 않았다 — 그러면 영원히 초록인 장식이 된다.

### 배포 뒤 확인할 것 (순서대로)
1. 화면 빌드 배너가 **V33.373** 으로 바뀌는지 — "정말 올라갔다" 의 증거.
2. 폰에서 「🧠 AI 운영상태 JSON」 → 전달 시트가 뜨는지(N-1).
3. 접속 시 흰 화면 없이 `시스템 준비 N/M` 이 보이는지(N-3·N-4).
4. **다음 Modal 회차 로그의 `[표본위생]` 비율** — I-2 고침의 증거. **제일 중요하다.**
5. `[EVAL-COST]` 한 줄(C-1) · 자가진단 '시세' 한 문장(A-6) — 남은 결함 둘이 여기서 갈린다.

### 앞선 판 (V33.372) — 배포 문제 원인 확정·선택지
저장소는 **비공개**다(`"private": true` 확인). 그래서 무료 한도 2,000분이 적용되고,
그 한도가 소진돼 **러너가 배정되지 않는다**(`runner_id 0` · steps 없음).

**이력 전체(171커밋) 비밀 검사 결과**
- `.env`·`.pem`·`.key` — **한 번도 커밋된 적 없다** ✅
- 실제 워커 주소 — **이력에 없다**(`xxx`·`example` 자리표시자만) ✅
- `TRAIN_KEY: "s3cret-key-value"` — 게이트의 **시험용 가짜값** ✅
- ★`rfApiKey: "WLMMRNV8GDpmbjEcrFar"` — **진짜 Roboflow 키**★
  현재 트리 `backup/index.V32.70.html` 에도, 이력 곳곳에도 있다.
  **다만 죽은 키다** — Vision 서브시스템은 **V33.112 에서 통째로 제거**됐다.
  → **공개로 돌릴 거면 이 키를 Roboflow 에서 폐기(revoke)하는 것이 선행 조건.** 기능 손실 0.

**선택지**
| 목표 | 방법 |
|---|---|
| 비공개 유지 + 즉시 배포 | **Cloudflare Workers Builds** — 대시보드에서 저장소 연결. Actions 분을 안 쓴다. Build command 에 `bash tools/verify-gates.sh` 를 넣으면 CI 와 같은 136종을 돌리고 실패 시 배포가 중단된다 |
| 전부(트레이너·워치독까지) 복구 | 공개 전환(위 키 폐기 후) |
| 기다리기 | Actions 한도 리셋 |

★**Workers Builds 는 워커만 배포하지만 그것으로 충분하다**★ — I-2 의 근본 원인은 워커 쪽이고,
`check-export-paging` 의 네 조합 대조가 **"새 워커 + 옛 트레이너 → 표본이 온전하다"** 를 증명한다.
Modal 자체 cron 이 학습을 계속 돌리므로 트레이너는 옛 판이어도 된다.

### V33.372 — 게이트 러너를 한 벌로
`tools/verify-gates.sh` 신설. `deploy-local.sh` 와 Workers Builds 가 **같은 스크립트**를 쓴다.
★만들면서 실제로 구멍 두 개를 냈다가 잡았다★:
1. 이름만 뽑는 정규식이 `node --experimental-sqlite tools/check-d1-scan.mjs` 를 못 잡아
   **135/136 만 돌리고 "전부 통과" 라고 했다.** 실행줄을 통째로 읽고,
   **tools/ 파일 수와 실행 수가 같은지** 직접 세게 했다.
2. 게이트가 러너를 전부 돌리게 해서 **무한재귀**를 만들었다(120초 태움).
   `VERIFY_GATES_LIST_ONLY` 목록 확인 모드로 끊었다.
※ "빠진 게이트가 있으면 멈춘다" 분기는 평소 안 걸려 지워도 티가 안 났다 —
  `VERIFY_GATES_WF` 로 워크플로 사본을 물려 **그 분기를 실제로 발동**시켜 시험한다.

### 앞선 판 (V33.371) — Q-1 · 죽은 설정 손잡이 11개
이 저장소가 ★반복해서★ 당한 유형이다: `liveSrcWeight`(V33.341) · dropout/l2(V33.260) ·
`mcap_shares`(F-2). 셋 다 "값을 바꿔도 아무 일이 없는" 상태였고 셋 다 **사고가 난 뒤에** 발견됐다.
손잡이가 죽은 것보다 ★죽은 줄 모르고 돌린 기간★ 이 비싸다. 그래서 래칫을 걸었다 —
새로 죽으면 실패, 목록의 것이 살아나도 실패(목록이 낡는 것도 거짓말이다), 이유 없이 등재 금지.

★**안전 구멍은 아니다**★ — 한 거래 상한은 `RISKENG`(35% · 160% · ADV 5%)이 실제로 걸고 있다.
문제는 **주석이 활성인 것처럼 적혀 있던 것**이다. 주석을 사실로 고쳤고 **값은 안 건드렸다**
(거래 동작 변화 0).

★**하나는 성격이 다르다**★ — `RISKENG.reduceOnlyOnHalt` 는 은퇴가 아니라
**한 번도 구현된 적이 없다.** 종목별 거래정지를 감지하는 경로 자체가 없다
(`KRHALT` 는 시장 전체 서킷브레이커다). 지우지 않고 사실을 적어 뒀다 —
필요한 안전장치가 맞고, 지우면 "그런 생각을 한 적도 없다" 가 된다.
**구현하려면 종목별 정지 상태를 받아올 데이터 원천이 먼저 있어야 한다 — 사람의 결정이다.**

### 앞선 판 (V33.370) — P-1 · 같은 목록이 세 벌이었다
라이브 전용 6칸(`sigWeight`·`confluence`·전략원핫4)의 이름과 중립값이
①집합 ②익스포트 설정 ③피처 생성기 에 ★따로★ 적혀 있었다. 셋이 갈리면
**학습이 본 값과 서빙이 내는 값이 달라진다** — V33.341 이 고쳤던 스큐가 조용히 되살아난다.
→ `_LIVE_ONLY_NEUTRAL` 한 표로 접었다. **값은 그대로라 오늘의 동작은 안 바뀐다.**

### P-2 — 그 어긋남의 값을 실측했다
트레이너와 같은 조건의 모의 학습(AdamW lr 0.0025 · wd 1.5e-3 · 입력잡음 0.08):
```
                       정보칸 |W|   중립화칸 |W|   비
입력잡음 0.08 · 150k      0.345        0.377      1.09   ← 죽지 않는다
대조: 입력잡음 0          0.318        0.173      0.55   ← 잡음이 없으면 감쇠한다
```
★`input_noise` 가 중립화된 칸의 가중치를 살려 둔다.★ 그 칸에 서빙이 1.0 을 넣으면
**로짓 평균 0.14 → 확률 약 3.6%p**. 부스터가 1~2%p 를 두고 다투는 자리다.
게이트가 종전엔 "불변" 만 봐서 서빙 상수를 1→0 으로 바꿔도 통과했다 — 값 일치·
익스포트 설정 일치·**featVer 핀**을 더했다(중립 상수를 바꾸려면 재학습해야 한다).

### ★내가 틀릴 뻔한 것 — 기록해 둔다★
처음엔 `_LIVE_ONLY_FEATS` 로만 grep 해서 **"서빙엔 중립화가 없다"** 고 단정할 뻔했다.
사실은 V33.341 이 이미 고쳐 뒀고, 그 목록이 **손으로 적힌 두 번째 사본**이라 검색에
안 걸렸을 뿐이다. **없는 결함을 보고하지 않은 것은 확인했기 때문이다 — 그 사본이
진짜 문제였고, 그걸 없앤 것이 P-1 이다.**

### 앞선 판 (V33.369) — ★러너가 배정된 적이 없다★
작업 상세(run 35044684776):
```
runner_id   : 0
runner_name : ""
steps       : (없음)
```
스텝이 ★하나도★ 실행되지 않았다. 재실행(2차)도 2초 만에 같은 결과.
**깃허브가 작업 시작 자체를 거부하고 있다 — 코드로 고칠 수 있는 종류가 아니다.**

### O-2 — CI 없이 배포하는 길 (`tools/deploy-local.sh`)
고친 코드가 운영에 못 가면 안 고친 것과 같다. CI 를 기다리지 않고 배포할 수 있게 한다.
```
bash tools/deploy-local.sh      # 최초 1회만: npx wrangler login
```
① deploy.yml 에서 ★게이트 목록을 읽어★ 전부 돌린다(손으로 적으면 CI 보다 적게 돈다)
② R2 바인딩을 CI 와 ★같은 sed·같은 확인★ 으로 보장한다
③ `wrangler deploy` ④ wrangler.toml 원복(실패해도 trap 으로)

★R2 에 대해 정정★ — 처음엔 "저장소에 주석 처리돼 있어 손으로 배포하면 R2 가 빠진다"
고 적었다. **사실이 아니다.** V33.191 이 그 블록을 영구 해제해 커밋했고, CI 의 sed 는
지금 무동작이다. `check-deploy-local` 이 그 거짓 단정을 잡아 줬다.
sed·확인은 안전망으로 남긴다(누가 다시 주석 처리하는 날 두 경로가 갈리면 안 된다).

### ★사람이 해야 할 일 — 둘 중 하나★
**(A) 지금 바로 운영에 올린다** — 로컬에서 `bash tools/deploy-local.sh`.
   Cloudflare 로그인만 있으면 된다. V33.365~369 가 한 번에 올라간다.
**(B) CI 를 되살린다** — GitHub → Settings → Billing → Actions 사용량·지출 한도.
   한도가 풀리면 실패한 워크플로를 Re-run 하거나 아무 커밋이나 push.

(A) 를 먼저 하고 (B) 를 정리해도 된다. (B) 없이는 앞으로도 자동 배포가 안 된다.

### ★배포 없이 지금 뽑을 수 있는 진단 — 남은 결함 셋이 여기서 갈린다★
운영판 V33.364 에는 **V33.347·V33.361 의 계측이 이미 들어 있다.** 즉 아래 셋은
배포를 기다릴 필요 없이 **지금 화면에서** 답이 나온다. (사진 한 장이면 된다)

| 결함 | 어디서 | 무엇을 보내면 되나 |
|---|---|---|
| **C-1** 평가비용 17배·커버리지 3% | 로그 화면에서 `EVAL-COST` 검색 | 그 한 줄 전체. `decide`·`feat`·`기타` 중 **어느 버킷이 큰지**가 범인이다 |
| **C-1 보조** | 같은 화면에서 `[EVAL]` | `평가 N/M종목(x%)` — 커버리지 추이 |
| **A-6** v7 이 빈 배열을 준다 | **자가진단** 화면의 '시세' 항목 | `인증은 정상…` 인지 `★인증 악수 실패…★` 인지 **한 문장** |

`[EVAL-COST]` 는 **매 사이클** 찍힌다(조건 없음). 못 찾으면 그 자체가 신호다.

### 배포된 뒤에 볼 것 (순서대로)
1. 화면 빌드 배너가 **V33.369** 로 바뀌는지 — 이게 "정말 올라갔다" 의 증거다.
2. 폰에서 「🧠 AI 운영상태 JSON」 → 전달 시트가 뜨는지(N-1).
3. 접속 시 흰 화면이 없는지 · `시스템 준비 N/M` 이 보이는지(N-3·N-4).
4. 다음 Modal 회차 로그의 **`[표본위생]`** 비율 — **I-2 고침의 증거. 이게 제일 중요하다.**

### 앞선 판 (V33.368) — ★운영 판은 V33.364 다★
GitHub Actions 실측:
```
09-15 12:03→12:58  Modal Watchdog(예약)  성공 · ★55분★
09-15 15:20 이후   배포·Modal·예약 ★전부★ 3~5초 만에 실패
```
★커밋과 무관한 예약 실행까지 같이 죽었다★ = 계정 차원(러너 시간 고갈)이다.
그래서 **V33.365(I-2 표본 복사본 고침) · V33.366(다운로드) · V33.367(흰 화면)이
커밋만 되고 배포되지 않았다.** 사용자가 "다운로드 아직도 안 된다"·"모델 2개뿐이다"
라고 한 것은 **고친 코드가 운영에 없기 때문**이다. 코드는 맞다 — 길이 막혔다.

★내가 "배포 완료" 라고 세 번 보고한 것은 틀렸다.★ push 성공을 배포 성공으로 읽었다.
앞으로는 push 뒤 워크플로 결론까지 확인한다.

### O-1 — 원인: 예약 워치독이 GPU 학습을 붙잡고 기다렸다
`modal run` 은 ★Modal 학습이 끝날 때까지 블로킹★ 이다. 그동안 깃허브 러너는 아무 일도
안 하면서 과금된다. 6시간마다 55분 = **월 6,600분** — 무료 한도 **2,000분의 3배**다.
→ `modal run --detach` 로 바꿨다. 요청만 걸고 빠진다(학습은 Modal 에서 그대로 끝까지 돈다).
  한 회 55분 → 약 2분. **월 6,600 → 240분.**
→ 작업 8개 ★전부★ `timeout-minutes` 를 걸었다(종전 5개는 상한이 아예 없었고,
  `resample-run` 은 330분 = 한도의 16%를 한 번에 태우는 값이었다).

### ★사람이 해야 할 일 — 이것 없이는 아무것도 배포되지 않는다★
1. GitHub → Settings → Billing → **Actions 사용량·지출 한도 확인**.
   무료 한도 소진이면 결제수단/지출한도를 올리거나 **다음 결제주기까지 기다려야** 한다.
2. 한도가 풀리면 **Actions 에서 실패한 워크플로를 Re-run** 하거나 아무 커밋이나 push.
   그 순간 V33.365~368 이 한 번에 배포된다.
3. 배포 성공을 확인한 뒤에야 다음이 의미가 있다:
   - 다운로드(N-1) 가 폰에서 되는지
   - 다음 Modal 회차 로그의 `[표본위생]` 비율(I-2 고침의 증거)

### ★게이트가 왜 이걸 못 잡았나★
게이트 133종은 **코드**만 봤다. **그 코드가 배포되는 길**은 아무도 안 봤다.
`check-ci-budget` 이 그 자리를 메운다 — 작업마다 상한이 있는지, 예약 워크플로가
학습을 붙잡지 않는지, 월 러너 시간 추정이 한도 안인지. 돌연변이 6종 전부 잡는다.

### 앞선 판 (V33.367) — N-3 · 인트로 전에 흰 화면
브라우저는 `<head>` 의 렌더링 차단 자원을 다 받기 전엔 ★아무것도 못 칠한다.★ 그런데 배경색을
정하는 첫 `<style>` 이 59줄이고 그 앞에 차단 자원이 둘 있었다 — 12줄 동기 `<script>`,
58줄 외부 폰트 스타일시트. 그동안 칠해지는 건 ★기본 흰색★ 이다.
→ ⑴ 임계 CSS 를 가장 앞으로(`<html class="lux-booting">` + 배경색) ⑵ 폰트를 비차단으로
  (`media=print` → `onload`, `<noscript>` 보존) ⑶ 외부 스크립트에 `defer`.
★`.lux-booting` 은 인트로가 걷힐 때 벗긴다★ — 안 벗기면 라이트 테마 오버스크롤에 어두운 띠가 남는다.

### N-4 — 인트로가 준비를 안 기다렸다
`setTimeout(removeIntro, 4200)` 고정이었다. 그래서 인트로가 사라진 화면에 `로딩 중…` 이 남았다.
→ `LUXBOOT` 추적기. ★애니메이션 최소시간(4.2초)★ 과 ★준비 완료★ 를 둘 다 만족해야 걷는다.
  등록: 데이터·파이프라인·뉴스·원자재·국채·시장심리·두뇌·자원(window load)·글꼴(fonts.ready).
  인트로에 `시스템 준비 N/M` 표시.

★**제1 규칙: 걸려서 안 열리는 일은 없어야 한다.**★
- 실패해도 `done` 을 부른다(성공이 아니라 ★끝남★ 을 센다) · 약속이 아닌 반환값도 즉시 끝으로 센다
- 상한 12초에서 연다 · 최후 보루 15초 · 같은 이름은 한 번만(10초 폴링이 다시 붙잡지 않는다)
- 탭하면 즉시 건너뛴다

### ★배포 뒤 볼 것 (V33.367)★
1. 폰에서 접속 — **흰 화면 없이** 바로 어두운 인트로가 뜨는지.
2. 인트로 아래 `시스템 준비 N/M` 이 올라가다 `준비 완료` 가 되는지.
3. 인트로가 걷힌 화면에 **`로딩 중…` 이 남아 있지 않은지.**
4. 네트워크가 나쁠 때 **12초 안에는 반드시 열리는지**(걸려서 못 들어가면 그게 더 나쁜 회귀다).
5. 라이트 테마에서 위아래로 세게 스크롤할 때 **어두운 띠가 안 보이는지**(배경 고정을 푸는 자리).

### 앞선 판 (V33.366) — N-1 · 다운로드가 폰에서는 한 번도 된 적이 없다
저장 경로가 `a[download]` 하나뿐인데 **iOS WKWebView 인앱은 그 속성을 무시한다.**
이 화면을 실제로 쓰는 자리가 iPhone·카카오톡 인앱이라는 사실은 **이 저장소가 이미 적어 뒀다**
(`public/index.html:13994`). ★그리고 화면은 '다운로드 완료' 토스트를 띄웠다 — 되지도 않은 일을
됐다고 말했다.★
→ 수집이 끝나면 **전달 시트**를 띄우고, 그 버튼의 탭(=새 제스처) 안에서 공유·복사·새창을 한다.
  (`navigator.share` 는 제스처 안에서만 되는데 수집은 5~15초 비동기라 그 자리에서 못 부른다.)
  데스크톱은 종전대로 즉시 내려받는다. 로그 CSV 도 같은 경로로 통일.

### N-2 — 블록 IC 의 t 가 부풀려져 있다 (★판정은 안 바꿨다★)
`_calc_ic_blocks` 의 블록 수는 `max(K, min(12, n//200))` — ★표본 수만 보고 시간을 안 본다.★
홀드아웃 70일에 K=12 면 블록 5.8일인데 라벨 지평은 10일이다. 인접 블록이 같은 라벨을
나눠 가져 블록 IC 끼리 상관이 생기고 t 가 부푼다. **실력 0 모의에서 오통과율 1.63배.**

★방향을 분명히★ — 이건 게이트를 **더 엄격하게** 만드는 발견이다. "성능 미달이 게이트 탓" 이
아니다. 지금 자가 관대한 쪽으로 틀렸는데도 부스터가 t 1.14~1.48 로 떨어졌다는 건
**참 유의성이 그보다 낮다**는 뜻이다.
→ 시간 기준 블록(2×지평, 경계 버림)의 `valICtHonest` 를 **나란히 기록**하고 `valICtGap` 을 남긴다.
  위원 **6곳 전부** 같은 자. **승격 판정은 그대로** — 위원을 빼는 건 사람의 결정(G-2).

★**핵심: 자를 고쳐도 기간이 짧으면 검정력이 없다.**★ 70일 홀드아웃이면 정직한 블록이 **3개**뿐이고
df=2 에서 t 1.65 의 명목값은 12% 다. **70일로는 잘 보정된 IC 검정 자체가 불가능하다.**
진짜 해법은 **관측 기간**이다. 그래서 분할이 한 줄로 답한다:
`[홀드아웃] N일 · 검증 M건 → 정직한 블록 K개 … 약 D일 필요(valFrac 을 X 쯤으로)`

### ★배포 뒤 볼 것 (V33.366)★
1. 폰에서 「🧠 AI 운영상태 JSON」 → **전달 시트가 뜨는지**, 「파일로 저장·보내기」로 파일이 실제로 남는지.
2. 다음 Modal 로그의 **`[표본위생]`**(V33.365) → 비율이 100% 에 가까워야 한다. **이게 제일 중요하다.**
3. 그다음 **`[홀드아웃]`** 줄 — 며칠인지, 정직한 블록이 몇 개인지. valFrac 조정은 **사람이 결정**.
4. **`[정직한IC]`** 줄의 `차 ±X` — 크게 벌어지는 위원이 있으면 그 승격 근거를 다시 볼 것.

### ★실력·수익률에 대해 정직하게★
- **제일 큰 지렛대는 이미 나갔다 — I-2(V33.365).** 모델들이 2만 행의 복사본으로 학습하고 있었다.
  이제 110만 행으로 학습한다. 그 효과는 **다음 회차에 처음 보인다.**
- **문턱을 낮추는 방식은 쓰지 않았다**(B-6). 이번 측정은 오히려 지금 자가 **관대**하다고 말한다.
- 남은 정직한 길은 둘뿐이다: **관측 기간을 늘리거나(valFrac — 사람 결정), 진짜 실력을 늘리거나.**
  진짜 실력 쪽은 I-2 이후의 수치를 보고 판단해야 한다 — **지금 숫자로는 아무 말도 할 수 없다.**

### 앞선 판 (V33.365) — ★I-2: 표본 22만이 '2만건의 복사본' 이었다★
사용자 지시: "자체 탑재 ai 문제도 전부 해결해 **성능이 안 나오는 이유 찾아서** 수정해".
그 답이다. **고유도 문제가 아니었다 — 표본이 22만이 아니었다.**

`/api/ml-export` 의 두 경로가 페이지 주소를 ★서로 다른 말★ 로 한다:
```
R2 스냅샷 분기 : offset  → _part = Math.floor(offset / MLSNAP_PART)
D1 분기        : cursorTs · cursorId (keyset)
```
트레이너는 커서를 한 번 받으면 ★offset 을 영영 안 보낸다★(modal_train.py:174 의 if/else).
수확 스냅샷 빌더는 장외에 ★점진적★ 으로 돌다가 마지막 파트에서 done 을 찍는다 —
즉 ★수집 도중에 뒤집히는 게 정상★ 이다. 그 순간부터 모든 페이지가
`offset 없음 → 0 → _part=0` → ★파트0(최근 2만행)만 계속★ 이 된다.
트레이너는 `off += len(got)` 로 세므로 **total 을 채우고 정상 종료**한다 —
로그도 `내려받음 1,123,768/1,123,768` 로 **완벽히 정상으로 보였다.**

★재현이 실측과 맞는다★ (게이트가 매번 실제로 돌린다)
```
수집 도중 신선해짐 : 받은 1,140,000건 · ★서로 다른 행 60,000★ · 시간범위 41.7일
고유도            : 동시성 = 종목당 19건 × 복제 56배 = 1,064 → 유효표본 ★204★
실측              :                                              ★210★ (동시성 1,047.6)
정상 배치 대조     : 시뮬 7,947(28.3) vs GBDT 실측 7,377(29.4)  ← 모델이 맞다는 증거
```

★**하한만의 문제가 아니다 — 검증표본이 학습표본의 복사본이었다.** valAcc 자체가 의미를 잃는다.★
"성능 미달"·"잡음과 구별이 안 된다" 가 한 뿌리로 모인다.

### 고침 — 양쪽 다, ★어느 한쪽만으로도★ 막히게
1. **워커** 빌더가 파트마다 마지막 `(ts,id)` 를 `bounds` 로 적는다. R2 분기가 **커서를 읽어**
   다음 파트를 찾고, **못 찾으면 파트0 대신 D1 로 떨어진다.** R2 응답도 `nextCursorTs` 를 실어
   중간에 D1 으로 떨어져도 같은 자리에서 이어진다. `MLSNAP_SCHEMA` 2→3(경계 없는 옛 사진 차단).
2. **트레이너** `offset` 을 **언제나** 보낸다 · **같은 페이지가 두 번 오면 멈춘다**(오탐 없음) ·
   수집 직후 `[표본위생] 서로 다른 표본 N/M` 을 **먼저** 찍고 중복 과반이면 **학습하지 않는다.**

### ★배포 뒤 볼 것 (V33.365) — 순서대로★
1. **`[표본위생]` 줄의 비율이 100% 에 가까운지.** 그게 이 고침의 증거다. 아니면 아직 새는 것이다.
2. `표본 고유도: … 유효 N/…` 이 **7,000대로** 돌아오는지(210 이 아니라).
3. 그러면 accLB 가 약 **5.5%p** 올라 SEQ·부스터가 다시 들어올 수 있다.
   ★다만 그건 **하한이 정직해진 것**이지 실력이 오른 게 아니다★ — 그 다음은 G-2 가 볼 문제다.
4. 배포 직후 한 회차는 스냅샷이 다시 뜨는 동안 **D1 에서 읽어 느릴 수 있다**(정상이다).

### 남은 것 — 정직하게
- **G-2 2단계**(문턱이 두 벌) — 읽기 쪽을 0.523 으로 맞추면 GBDT 가 빠져 위원회가 빈다. **사람의 결정.**
- `A-3`·`A-4`(정책) · `A-6` 근본원인 · `B-5`·`B-6`·`B-8`(재수확 시점) · `C-1` · `BK`·`SATS` 티커(사람 확인).
- 옛 스냅샷 키 `ml/v*s2/part-*` 가 R2 에 남는다(정리 경로 없음 — 용량 문제 시).

### 앞선 판 (V33.364) — H-2 · 문턱이 12~19시간 전 값이었다
실측(스냅샷 2026-09-15 01:49):
```
US  지금 계산 floor 0.4500 / ★적용된 값 0.4614 — 12.0시간 전★
KR  지금 계산 floor 0.4665 / ★적용된 값 0.4643 — 19.3시간 전★
```
원인: 기록이 `if (_newThr < _thrAI) { … }` ★안에서만★ 이뤄졌다(`src/index.js:22034`).
남는 건 "마지막 사이클" 이 아니라 ★"백분위가 고정문턱을 마지막으로 이긴 때"★ 다.
그 뒤로 고정 쪽이 계속 이기면 기록이 갱신되지 않고, 화면은 반나절 전 값을 '지금' 이라 부른다.
화면은 그 필드를 `applied` 라 했고 주석은 "마지막 사이클에 실제로 쓴 값" 이라 했다 — ★둘 다 거짓★.
→ 기록을 `if` **밖으로** 꺼내 **매 사이클** 실제 쓰는 값을 남기고, `path`(pct/fixed)와
  **두 후보값(pctThr·fixedThr)** 을 함께 적는다. 읽는 쪽은 `ageH` 와, 1시간 초과 시 문장을 낸다.
★**적용 규칙은 안 건드렸다 — 문턱을 올리지 않는 규칙 그대로다. 바뀐 건 기록뿐이다.**★

### H-3 — 모델 열화가 "언제부터" 를 말 못 했다 (+ 내 규율 위반 자수)
감지될 때마다 `ts` 를 덮어써 ★처음 나빠진 시각이 매번 지워졌다★ — 하루를 방치해도 화면은
갓 생긴 문제와 똑같이 보인다. → `since` 를 물려받아 지속시간을 말하고(`3.0일째`),
**24시간 넘으면 ERROR 로 승격**, 회복 시 `model_drift` 삭제.
★이 고침은 V33.363 에 **게이트 없이** 나갔다 — 저장소 규율 위반이다. 스스로 찾아 여기서 메웠다.★

### ★배포 뒤 볼 것 (V33.364)★
1. 문턱 카드의 `적용된 값` 옆에 **나이**가 보이는지. **여전히 12~19시간이면 그건 표기가 아니라
   ★스케줄러★ 문제다** — 지금까지는 낡았는지조차 알 수 없었다.
2. `path` 가 `fixed` 로 나오는지 — 나오면 "백분위가 안 먹은 것" 이지 "기록이 안 된 것" 이 아니다.
3. 열화가 있다면 `N일째` 가 누적되는지(매번 0분으로 돌아가지 않는지).

### ★이번 판이 안 고친 것 — 그대로 남아 있다★
- **I-2 유효표본 붕괴(7,379→210)** — 부스터가 위원회에 못 드는 ★직접 원인★. 다음 회차
  트레이너 로그 `[고유도내역]` 과 워커 `poolWindow` 를 봐야 갈린다. **추측으로 안 고쳤다.**
- **G-2 2단계**(문턱 두 벌) — 읽기 쪽을 0.523 으로 맞추면 GBDT 가 빠져 위원회가 빈다. **사람의 결정.**
- `A-3`·`A-4`(정책) · `A-6` 근본원인 · `B-5`·`B-6`·`B-8` · `C-1` · `BK`·`SATS` 티커(사람 확인).

### 앞선 판 (V33.363) — 판 불일치·성능 미달
스크린샷에서 ★같은 카드 안의 두 문장이 정반대★ 였다:
```
뱃지 : 「모델 판 불일치」 ×0.00
본문 : "재학습은 돌고 있다 — featVer 17 모델이 47분 전 도착했으나 승격 거절:
        IC 경로 — 정확도 하한 42.61% < 49% …"
```
처방이 정반대다 — 「판 불일치」는 "기다리면 된다" 로 읽히는데 사실은 "기다려도 안 된다" 다.

### M-1 — 원인이 ★둘★ 이었다
① 뱃지를 가르는 `featVerOk` 가 ★낡은 승격기록(featVer 15)★ 에서 나왔다.
   V33.356 이 `why` 문장은 고쳤지만 ★뱃지를 가르는 필드는 안 고쳤다★ — 글자만 고치고 값은 뒀다.
   → 현재 판 수신분이 있으면 그걸 **실효 판**으로 보고. 낡은 쪽은 `activeFeatVer` 로 따로 남긴다.
② `public/model-evidence.js` ★만★ `r.tier` 로 갈렸다. 나머지 화면은 `r.state`(V33.301 단일 출처).
   tier 로 갈리면 `tier:"full"` + `mult:0` 이 ★"정식 합류 ×0.00"★ 으로 나온다.
   **V33.301 이 없애려던 '판정이 여러 곳' 이 그 뒤에 생긴 파일에서 되살아났다.**

### M-2 — MIND: 사이드바 "모델 없음" ↔ 본문 "정식 합류 ×1.00"
`diag` 는 `mfm && mmeta`(fm 요구), `buildRoster` 는 `mmeta && (mfm || trees)`.
실측 `experts:["tree"]` — 지금 MIND 는 트리 코어다. 그리고 채점기가 답을 준다:
`const _isTree = !mind.fm && trees.length > 0` → ★트리만 있어도 실제로 채점된다★.
**roster 가 맞고 diag 가 틀렸다**(V33.249 가 트리 코어를 넣으며 이 자리를 안 고쳤다).
→ `_mindStored(probe)` 한 벌(정의 1 + diag + roster = 3곳이 같은 함수).

### M-3 — "성능 미달" 이 스스로를 설명하지 않았다
점추정 vs 하한 간격이 셋 다 ★같다★: XGB 48.21→42.61 · LGB 51.01→45.37 · CAT 41.65→36.21.
Wilson 에서 그 폭(약 5.5%p)은 ★유효표본 n≈222★ 를 뜻한다 — 원시는 22만 건인데.
즉 막힌 직접 원인은 '실력' 이 아니라 ★유효표본 붕괴(I-2)★ 다.
화면이 "성능 미달" 이라고만 하면 사람은 모델을 탓하고 문턱을 만지러 간다.
→ 사유에 `유효표본 222/220000(점추정 48.21%) ★…하한이 눌린 것이지 점추정이 나쁜 게 아니다(I-2)★`.

### ★정직하게 — 이번 판이 고친 것과 못 고친 것★
- **고친 것**: 화면이 사실을 말한다(뱃지·MIND·사유). **거래 동작은 한 톨도 안 바뀐다.**
- **못 고친 것**: 부스터가 ★실제로 위원회에 들어오려면 I-2 가 풀려야 한다.★
  그 답은 다음 회차의 `[고유도내역]`(트레이너)과 `poolWindow`(워커)에 있다.
- 무실력 기준이 0.505 → **0.6303** 으로 뛴 것도 확인했다. **버그가 아니라 측정값**이다 —
  시장별 라벨 쏠림(KR 하락장)이 커지면 '상수만 찍어도 나오는 정확도' 가 올라간다.
  **문턱은 낮추지 않았다**(B-6).

### ★배포 뒤 볼 것 (V33.363)★
1. XGB/LGB/CAT 뱃지가 **「성능 미달」** 로 바뀌고, 사유에 **유효표본 222/220000** 이 보이는지.
2. MIND 가 두 화면에서 **같은 말**을 하는지(사이드바·본문).
3. 카드 measures 에 `수신 판 v17 · 승격돼 있는 판 v15(투표 안 함)` 가 보이는지.

### 앞선 판 (V33.362)

### L-1 — 429 를 받고도 30종목을 끝까지 두드렸다
실측: `stocktwits http 429 · done 12 · fail ★18★` · `reddit http 403`.
원인은 `if (!r.ok) { … break; }` 가 ★안쪽 페이지 루프만★ 끊는다는 것 —
바깥 30종목 루프는 그대로 돌아 막힌 뒤에도 18번을 더 두드렸다.
그 18회가 ★전부 fetch 예산을 태웠다★(A-5 가 바로 그 굶주림 문제였다).
그리고 429 를 계속 두드리는 건 차단을 푸는 방법이 아니라 늘리는 방법이다.
→ 429·403 이면 회차를 즉시 접고, `Retry-After`(초·HTTP-date) 만큼 쉰다.
  없으면 20분, 상한 120분(영원히 안 쉼), 최소 1분. ★소스별로 따로★ 센다.

**게이트로 잰 효과**: 429 상황에서 **30회 → 1회**, 백오프 중 **0회**, 만료 후 **스스로 재개**,
정상일 땐 **30회 그대로(회귀 없음)**. 돌연변이 9종 전부 검출 —
그중 하나는 ★내가 실제로 냈던 버그★ 다(쓰기는 `h[id].until` 인데 읽기를 최상위로 했다).

### ★배포 뒤 볼 것 (V33.362)★
1. `[SOCIAL] … HTTP 429 → N분 백오프, M종목 건너뜀` 로그가 뜨는지 — 뜨면 예산이 그만큼 남는다.
2. **Reddit 403 이 며칠 이어지면 20분 백오프로는 안 풀린다** — 영구 차단(User-Agent 정책 등)일 수
   있으니 그때는 차단 사유 자체를 볼 것.
3. 이 고침은 **예산을 아끼는 것**이지 소셜 점수를 살리는 게 아니다(`social.k` 는 여전히 관측 0/300).

### 앞선 판 (V33.361)

### A-6 — ★마지막으로 안 보고 있던 곳: cookie·crumb 악수★
v7 quote 는 2023년부터 cookie+crumb 를 요구한다. 두 경로 모두 `if (auth && auth.crumb)` 라
**crumb 이 없으면 그냥 빼고 나가고**, 야후는 crumb 없는 요청에 **오류 없이 빈 배열**을 준다 —
실측 `result=0 keys=quoteResponse` **그대로다.**
그런데 `getYahooAuth` 는 `catch (e) { /* v8 폴백이 처리 */ }` 로 **아무 것도 안 남겼다.**
그래서 이 자리를 의심할 근거 자체가 없었다(그 주석의 'v8 폴백' 이 바로 1종목/1회 저하다).

**쿠키 파싱에 진짜 위험도 있었다** — Workers 의 `get("set-cookie")` 는 여러 Set-Cookie 를
`", "` 로 이어 붙여 준다. `.split(";")[0]` 만 하면 **첫 쿠키만** 남는다(야후는 A1·A3 를 함께 준다):
```
종전  "A1=d=AQ"            ← A3 소실
고침  "A1=d=AQ; A3=d=AQB"   (Expires 안의 쉼표에서도 안 쪼개진다)
```
→ 표준 `getSetCookie()` 우선, 없으면 `", "` 를 되돌려 잇는다.
→ **성공이든 실패든** `yahoo_auth_probe` 에 남긴다. 자가진단의 v7 ERROR 가 **같은 줄에** 붙인다 —
  정상이면 *"인증은 정상 — 원인은 인증이 아니다"* 로 **후보를 줄여 준다.**
★단정하지 않는다★ — 야후로 나갈 수 없어 실제 응답을 못 봤다. **다음 사이클 로그가 답한다.**

### K-1 — 원장 드리프트가 "1건" 만 말했다
이틀 연속 같은 ERROR: `유령매도 0건 / 포지션 드리프트 1건 · /api/audit 로 상세 확인`.
**어느 종목·얼마나·어느 쪽이 큰지가 한 글자도 없다.** 원인은 `_top` 이 **유령매도만** 훑는 것.
드리프트만 있으면 빈 문자열이 된다. 감사는 `{symbol, ledgerQty, tableQty, diff}` 를 이미 갖고 있었다.
→ 이름으로 적고 **방향을 말로 푼다**: `diff>0` = 표가 더 많다(**그대로 팔면 유령매도**),
  `diff<0` = 표에서 사라진 보유(**관리 밖 포지션**).

### K-2 — 죽은 상류가 야간 단계를 며칠째 멈춰 세웠다 (사슬을 끝까지 따라갔다)
```
① v7 사망(A-6) → ② 컨센서스는 v7 전용 → rows 0 → okCount 0
③ if (okCount === 0) return cached;   ← ★조용히 return. 로그 한 줄 없다★
④ analystRevTrack 미호출 → analyst_rev 원장 미생성
⑤ 야간 anlrevk "개정 원장 없음 — 대기" (419분째) → ⑥ 자가진단 매일 경고
```
**다섯 줄 어디에도 `v7` 이라는 말이 없었다.**
→ ③ 이 WARN 을 남기고 **v7 이름·결함번호(A-6)·멈추는 하류(anlrevk)** 까지 적는다.
→ ⑤ 의 대기 사유가 **상류를 지목한다**(컨센서스 몇 종목·몇 시간 전·v7 사망 여부).

### ★배포 뒤 반드시 볼 것 (V33.361)★
1. ★**`yahoo_auth_probe`**★ — A-6 의 답이 여기 있다.
   `ok:false` 면 **crumb 이 원인**이다(HTTP 상태·쿠키 개수·사유가 함께 남는다).
   `ok:true` 인데도 v7 이 0건이면 **인증은 범인이 아니다** — 후보가 줄어든다.
2. 원장감사 ERROR 에 **종목명이 나오는지**. 나오면 그 종목의 `positions` vs 원장을 직접 대조할 것.
3. `[ANALYST] 컨센서스 수집 0종목` WARN 이 뜨는지 — 뜨면 K-2 사슬이 살아 있다는 뜻이다.

### 앞선 판 (V33.360)

### J-1 — "파일이 깨진다" 의 정체: 문장이 300자에서 잘려 ★결론만★ 사라졌다
파일 자체는 **유효한 JSON 이었다**(업로드된 5개 전부 파싱 확인). 깨진 건 **내용**이다.
`note` 4개가 정확히 300자에서 잘렸다 — flow "…잡음과 **구별**"(원문 "구별되지 않는다"),
stack "…실측 70일 ·", memo "…→ 합류 ". 이 문장은 **맨 끝에 결론**을 적는 구조라
뒤에서 자르면 **정확히 결론만** 없어진다.
→ `_clipMid` — 상한 900, 넘으면 **가운데를 접어** 머리(무엇을)와 꼬리(결론)를 둘 다 남긴다.

### J-2 — 상태 구체화 (추가 D1 쿼리 ★0★)
- **`d1Headroom`**: rows·mb·cap·leftRows·**daysLeft**(7일 평균 유입 기준)·note. 못 재면 null.
- **`poolWindow`**: windowDays·nSym·perSym·maxPerSym·conc·why —
  `pooluniq` 가 **이미 뽑은 같은 20,000행**으로 계산해 비용 0. **I-2 의 답이 여기서 나온다.**
  고유도 계산이 **실패한 상태도 말한다**(종전엔 `uBar=1` 로 조용히 떨어져 보정이 꺼진 걸 몰랐다).

### J-3 — 피처판 불일치: 옛 판 기록이 ★영원히★ 남았다
`xgb/lgb/cat_trust` 가 featVer **15** 에 얼어붙어 `activeFeatVer: 15` 로 보고됐다.
featVer 가 다르면 **다시는 투표할 수 없는 모델**인데도 자리를 지켰다.
→ 야간 `retirefv`(학습기들 **앞**)가 `featVer < 현재판` 기록·본문(R2·D1청크·메타)을 지운다.
  **거래 동작은 한 톨도 안 바뀐다** — 이미 막혀 있던 것이다.
  안 건드리는 것: 현재판 · **판 미기록**(모르면 안 지운다) · **섀도우**(다음 승격 후보).

### J-4 — "D1 한계면 R2 로" — ★정직한 답부터★
- **용량은 한계가 아니었다**(935MB). 실제 한계는 **쿼리당 CPU** 였고 I-1 에서 고쳤다.
- 학습 읽기는 **이미 R2 스냅샷**으로 간다(V33.27·176) — 새로 옮길 게 아니다.
- 깊은 `OFFSET` 도 재 봤으나 **평탄**(파트 0→19: 38→28ms) — 문제 아님.
- ★**그런데 R2 가 정말 필요한 곳이 있었다**★: 총 1,123,768 / 상한 1,200,000 — **곧 닿는다.**
  닿으면 종전엔 그냥 DELETE 였고, 수확 표본은 **다시 만들 수 없다.**
  → `ml/archive/v17/<날짜>-<id>.json` 로 **붓고 나서** 지운다. **R2 실패 시 안 지운다.**

### ★배포 뒤 반드시 볼 것 (V33.360)★
1. `note` 에 결론(`→ 합류 보류 — …`)이 **끝까지** 나오는지.
2. **`poolWindow.windowDays` / `perSym`** — I-2(유효표본 210) 의 답이 여기 있다.
   창이 20일 미만이거나 종목당 40건 초과면 `why` 가 지적한다.
3. **`d1Headroom.daysLeft`** — 2주 미만이면 곧 보관이 시작된다.
   첫 보관 시 `[표본상한] … R2 로 N건 보관 후 …` 로그. **ERROR 면 R2 문제다.**
4. `retirefv` 가 돌아 `activeFeatVer` 가 15 → 17(또는 "모델 없음")로 바뀌는지.

### 앞선 판 (V33.359)

### 사용자 보고 "아직도 해결 안됐다" — ★새 스냅샷은 더 나빠졌고, 이유가 있었다★
01:49 스냅샷에서 **SEQ 가 위원회에서 빠졌고**(mult 0.656 → 0) 부스터 3종의 valAcc 가
**0.5194/0.5199/0.5194 → 0.4821/0.5101/0.4165** 로 떨어졌다. 위원 수 1명(GBDT).
그리고 **새 ERROR 4건이 전부 같은 문구**였다: `D1 DB exceeded its CPU time limit`.

### I-1 — 전수 정렬 쿼리 3건이 D1 을 무너뜨렸다 (치명, 고침)
`ml_samples` 가 **1,123,768행 / 895.9MB** 가 됐다. 이 규모에서 한 쿼리가 D1 CPU 예산을
통째로 먹으면 **다른 요청까지 큐에 적체돼** 무관한 곳(BDKR·CM)이 함께 쓰러진다.
★진짜 SQLite 에 데이터를 넣고 ANALYZE 한 뒤 EXPLAIN QUERY PLAN 으로 재서★ 범인을 특정했다:

| 쿼리 | 종전 계획 | 종전 | 고침 |
|---|---|---|---|
| pooluniq `ORDER BY id DESC` | `TEMP B-TREE` | 199ms | `ts DESC` → **47ms** |
| 구판정리 `featver != ?` | **`SCAN`**(전수) | 11ms | `featver < ?` → **0ms** |
| 총량폐기 `ORDER BY RANDOM()` | `TEMP B-TREE` | 37ms | `ts ASC` → **1ms** |

인덱스는 `(featver, ts)` 인데 정렬을 **`id`** 로 걸어 112만 행을 전부 읽어 정렬하고 2만 건만 취했다.
`featver != ?` 는 인덱스를 못 탄다 — featVer 는 되감기지 않으니 `featver < ?` 와 같은 집합이고
그쪽은 커버링 인덱스를 탄다(지금은 **한 건도 안 지우면서 매일 밤 전수 스캔**만 했다).
★`ORDER BY RANDOM()` 은 아직 안 터졌지만 곧 터진다★ — 상한 1,200,000 에 현재 1,123,768.
→ 오래된 것부터 지운다. **이건 정책 변화다** — 풀이 최근으로 기운다.
  원치 않으면 `HARVEST.maxTotal` 을 올려 폐기를 미룰 것.

### I-2 — 유효표본 7,379 → ★210★ 붕괴 (원인 미확정, 계측만 심음)
| 모델 | 학습 | valAcc | 유효표본/원시 |
|---|---|---|---|
| GBDT | 09-14 19:02 | 0.5226 | **7,377**/217,230 |
| DNN | 09-15 00:43 | 0.5248 | **210**/220,000 |

accLB 가 그 탓에 무너져 SEQ·부스터가 전부 거절됐다.
**트레이너 코드는 그 사이 한 줄도 안 바뀌었다**(git 확인) — 바뀐 건 데이터(+37,615건)다.
시뮬레이션으로 범위는 좁혔지만(정상 10,868 · 종목구분 상실 11 · ts 몰림 1,018 · **실측 210**)
★어느 가정도 210 을 정확히 주지 않아 단정하지 않았다.★
→ 추측으로 고치는 대신 **다음 회차가 답하게** 했다. 트레이너가 찍는다:
  `[고유도내역] 평가창 N일 · 종목 M개 · 종목당 K건 · 평균동시성 C건`
→ **다음 Modal 회차 로그의 그 한 줄을 읽으면 갈린다**: 평가창이 짧다 / 종목당 건수가 많다 /
  종목 수가 적다. **그 전까지는 원인 미상이고, 문턱을 낮춰 통과시키는 것은 답이 아니다(B-6).**

### ★배포 뒤 반드시 볼 것 (V33.359)★
1. `D1_ERROR … CPU time limit` 가 **사라졌는지.** 남으면 범인이 `ml_samples` 밖에 있다.
2. `[STAGE:pooluniq]` 가 성공하는지 — 실패하면 `uBar=1` 로 떨어져 **고유도 보정이 통째로 꺼진다.**
3. **다음 Modal 회차의 `[고유도내역]` 한 줄** — I-2 의 답이 거기 있다.

### 앞선 판 (V33.358)

### H-1 — R2 집계가 ★23일 전★ 값인데 "오늘" 이라 부르고 있었다
실측: `ageSec 2,008,262`(=23.2일) · `today.day "2026-08-23"` · `files 2` · `pending 432`.
화면은 `"2,008,262초 전 집계"` 로 찍고(사람이 23일로 못 읽는다), 그 값을 **"오늘 단타 수집"**
이라 부르며 **초록**으로 칠했다 — 멈춘 수집이 도는 것처럼 보인다.
원인: `r2_status_cache` 는 **`/api/r2-status` 를 누가 열 때만** 갱신된다(`R2.list` 가 Class A
과금이라 주기 호출을 일부러 안 한다 — 그 결정은 옳다). 화면이 그걸 현재값처럼 믿은 게 문제다.
→ **과금은 안 늘린다. 표기만 정직하게.** 나이는 `extAgeTxt` 로 `"23일 전 집계"`,
  6시간 넘으면 '옛 집계' 로 보고 **'오늘' 이라 안 부르고 초록으로도 안 칠한다.** 신선하면 종전 그대로.
→ 같이 잡힌 것: `<script>` 가 IIFE 라 `extAgeTxt` 가 스코프 밖이었다(`check-html-js` 가 잡음).
  로직을 복제하면 두 표기가 갈라지므로 `window.extAgeTxt` 로 **한 벌만** 내보냈다.

### F-3 — 닫았다(증거 두 갈래)
① 나스닥 2026-06-26 공시가 `Nasdaq: SPCX` 를 지목(Codex 검증).
② **우리 자신의 데이터** — F-3 이 "같이 만들 것" 이라 적은 관측은 **이미 있었다**(`univHealthNightly`).
   스냅샷: `us n 558 · ok 558 · neverN 0` → **558종 전부 일봉을 받는다. SPCX 도 받는다.**
→ "죽은 티커가 예산만 태운다" 걱정은 해당 없음.
★대신 실명 후보가 나왔다: **`BK`(51일 뒤처짐) · `SATS`(16일 뒤처짐)**.★
  **조사자는 이 둘의 현재 상태(상장폐지·티커변경·합병)를 확인할 수단이 없다** — 이 환경에서
  외부 시세·공시 접근이 막혀 있다. **추측으로 유니버스를 건드리지 않았다.**
  확인되면 `DEFAULT_US`·`NAME_MAP`·`MCAP_RANK` **세 곳**을 함께 고칠 것.

### A-6 · G-2 (V33.357 — 아래 그대로)

### A-6 — ★계측이 답을 줬고, 원인과 무관한 구조 결함을 고쳤다★
V33.347 이 심어 둔 응답모양이 운영 스냅샷에서 나왔다: **`result=0 keys=quoteResponse`**.
봉투는 멀쩡하고 `error` 도 없는데 배열만 비었다 → **스키마 변경도 인증 실패도 아니다.**
남는 건 요청 형태나 **심볼 목록** 쪽이다.
(※ 야후로의 아웃바운드가 이 환경에서 막혀 **어느 심볼이 범인인지는 실측 못 했다 — 단정하지 않는다.**)

**원인과 무관하게 확실한 것**: 종전엔 **첫 배치 하나**가 558종의 운명을 정했다.
`slices[0]` 가 0건이면 `v7Dead` → 전 종목이 v8 개별 폴백. `fields` 를 뒤집어 한 번 더 보긴
하지만 **같은 배치라 같은 심볼을 또 물어본 것뿐**이라, "v7 이 죽었다" 와 "이 배치에 못 알아먹는
심볼이 하나 있다" 를 구분할 길이 아예 없었다. 자가진단의 **"50종목/1회 → 1종목/1회"** 가 이것이다.
→ 죽었다고 선언하기 전에 **다른 배치**를 한 번 물어본다. 오면 v7 은 살아 있고,
  그 배치만 폴백에 맡기고 나머지는 정상 속도로 받는다.

**게이트로 잰 실제 효과**(`fetchBatchQuotes` 를 실제로 돌린다):
| 상황 | 종전 | 고친 뒤 |
|---|---|---|
| 300종 중 첫 배치만 0건 | v7 사망 · **개별 폴백 300회** | v7 생존 · **개별 폴백 50회** |
| 전 배치 0건(진짜 사망) | 300회 | 300회(회귀 없음) |
| 정상 | 배치 6회 | 배치 6회(군더더기 0) |

**의심 배치를 이름으로 남긴다** — F-3 이 요청한 관측이 이것이다. 자가진단이
`의심 구간 앞 10종: …` 을 찍는다. 돌연변이 7종 전부 잡는다.

### G-2 1단계 — 문턱이 두 벌인 것을 고칠 수 있게 ★기록★ 을 남겼다
수신 시 `_accFloor(0.505, 무실력)` = **0.523**, 읽기 시 `GBDT.trustFloor` = **0.505**.
옛 규칙으로 승격된 모델이 낮은 바에서 계속 투표한다. **지금 맞추면 GBDT(51.12%)가 빠져
보조 위원이 0명**이 되는데, 그 모델이 어느 경로로 들어왔는지 기록이 없어 추측 없이 못 가른다.
→ 승격 기록에 `accBase`·`accFloorUsed`·`icTMinUsed`·`passedBy` 를 남긴다. **판정은 안 바꿨다.**
게이트가 "두 문턱이 아직 다르다" 를 함께 지킨다 — **같아지는 날 그 절이 실패하고,
그때가 읽기 쪽을 맞출 수 있게 된 시점이다.**

### ★배포 뒤 반드시 볼 것 (V33.357)★
1. 자가진단에 **`야후 v7 은 살아 있는데 배치 하나만 0건`** 이 뜨는지. 뜨면 **의심 구간 목록**을 보고
   그 안에 상장폐지·티커변경이 있는지 확인할 것. 이미 잡혀 있는 후보:
   **`BK`(51일 뒤처짐) · `SATS`(16일 뒤처짐)**, 그리고 미검증인 **`SPCX`(F-3, 유니버스 index 5 — 첫 배치)**.
2. `[EVAL-COST]`·사이클 시간이 줄었는지(폴백 300→50 이면 예산이 크게 남는다 → A-5 시간외 보강도 산다).

### 앞선 판 (V33.356)

### 사용자 보고 "또 모델들 대부분이 작동 안하고 있고" — ★학습은 멀쩡했다★
상태 스냅샷(2026-09-14 23:09) 확인: 외부 모델 **6/6 수신**, 나이 0.4~16h
(Cat/LGB/XGB 0.4h · DNN 0.5h · GBDT 4.1h · MIND 16.1h). V33.350 예산 고침은 먹었다.
**고장난 건 학습이 아니라 진단 문장이었다.**

### G-1 — "재학습 대기" 가 거짓말이었다
같은 응답 안에서 같은 모델을 두고 두 문장이 반대였다:
- `latestReceiptReason`: "IC 경로 — 블록 유의성 t 1.36 < 1.65" (featVer **17** · **0.4시간 전**)
- `reason`: "판 불일치(featVer **15** ≠ 17) — **재학습 대기**"  ←★거짓★

원인은 `_boosterAdmit` 첫 줄 `const t = live || ext`. 승격기록(featVer 15)을 **언제나 먼저** 봐서,
**이미 도착해 거절된 모델**을 두고 "아직 기다린다" 고 말했다. accLB 도 낡은 것(52.25%)을 찍어
갓 온 값(51.04%)과 달랐다. → **판정은 그대로 두고 사유만** 최신 수신분으로. 두 기록 숫자를 섞지 않는다.
★합류 판정 252조합 전수 대조 0건 변화★ — 거래 동작은 한 톨도 안 바뀐다.

### F-2 — 동점을 파고들었더니 ★전략 하나가 죽어 있었다★
`mcap_shares` 는 `{ sh, mc }` 로 저장되는데(V33.217) 순위 블록만 **`.shares`** 를 찾고 있었다
(V33.250 — 이 전략을 처음 넣은 판). 그 키는 **존재한 적이 없다** → `rank` 가 늘 `{}` →
★**`XR_FLOW` 는 추가된 날부터 0건 발동**★. 화면 시총 박스는 `ms.sh` 로 바르게 읽어 증상이 안 보였다.
→ ① 이름 `sh` ② `prev` 도 **같은 무리 안**의 순위로(종전엔 모집단이 달라 "빠진 종목 수" 를 쟀다)
  ③ 동점이면 **기권**(임의의 답으로 돈을 걸지 않는다).

### ★배포 뒤 반드시 볼 것 (V33.356)★
1. ★**죽어 있던 진입 전략 `XR_FLOW` 가 살아난다**★ — 실전 이력 **0건**이다.
   `[RV] … 시총순위 N` 이 0 이 아닌지 먼저 보고, **XR_FLOW 체결이 나기 시작하는지** 볼 것.
   원치 않으면 `rvStrat.xr.enabled: false`. 밴드(180~260)는 `now` 가 **무리 안 순위**라
   무리가 작으면 아무도 안 걸린다 — 패널의 `cohort` 를 보고 판단할 것.
2. 부스터 3종 사유가 "재학습 대기" 가 아니라 **거절 사유**로 바뀌는지.

### G-2 — 위원 대부분이 못 드는 ★진짜★ 이유 (안 고침, 고의)
명부 15명 중 투표 4명. XGB/LGB/CAT 은 블록 IC t **1.36·1.48·1.14** < 1.65,
DNN accLB **48.88%** < 50.5%, FLOW/XALPHA/STACK 홀드아웃 t **음수**, MEMO t 0.02.
**게이트가 제 일을 하는 것이다** — 무실력 기준선이 **~52.3%** 인데 부스터 정확도 하한은 51% 대고,
겹치지 않는 관측이 **7~12개**뿐(고유도 가중 217,230행 → 유효 7,377행)이라 유의성이 안 선다.
**문턱을 낮추지 말 것**(B-6).
★다만 잣대가 두 벌인 것은 찾았다★ — 수신 시 `_accFloor(0.505, 무실력)` = **0.523**,
읽기 시 `GBDT.trustFloor` = **0.505**. 옛 규칙으로 승격된 모델이 낮은 바에서 계속 투표한다.
**지금 맞추면 GBDT(51.12%)가 빠져 보조 위원이 0명이 된다.** 승격 기록에 `accBase` 가 없어
GBDT 가 어느 경로로 들어왔는지 추측 없이 못 가른다 → 먼저 `trust.accBase` 를 기록하고,
쌓인 뒤에 맞추되 **위원회가 비는 것을 감수할지는 사람이 정한다.**

### 앞선 판 (V33.355)

### B-3 — 잠정합류 경사로가 전진 IC 의 ★부호를 안 읽었다★
문서엔 "표본 미달이면 음수를 무시한다" 고만 적혀 있었는데, 실행으로 재 보니 더 나빴다.
고치기 전 실측 — 전진 IC **−0.039** 인 채로 표본만 늘렸을 때
`n=0 ×0.25 → n=200 ×0.425 → n=399 ×0.5991`, 그리고 `n=400` 에서 제외.
같은 표본에서 **양수 +0.039 도 ×0.5991** 이었다. 즉 부호를 아예 안 본다.
두 가지가 더 나온다 — ① **반대 증거가 쌓일수록 가중이 커진다** ② 399→400 에 **절벽**이 있다.
→ 부호가 이미 측정돼 있으면 경사로의 **방향**을 그 부호가 정한다:
  `_fwdNeg ? fwdBase*(1-frac) : fwdBase + fwdSpan*frac`.
  음수는 표본이 찰수록 0 으로 내려가 `minForward` 의 하드 리젝과 **이어진다**(절벽 제거).
  실측: −0.039 는 n≈209 에서 `minAdmitMult` 아래로 떨어져 제외된다.
→ ★전진 IC 8종 × 표본 58점 전수 대조: 종전보다 배수가 오른 경우 0건.★ 없던 합류를 만들지 않는다.
  부호 미측정(`fwdIC == null`)은 진짜 '부족한 증거' 라 **종전과 완전히 동일** —
  "못 쟀다" 를 "반대다" 로 바꿔 읽지 않는다.

### B-4 — 죽은 입력 6칸: 못 고치니 ★덫★ 을 놨다
`liveCtxNeutral` 이 `sigWeight`·`confluence`·전략원핫4 를 모든 행에서 같은 값으로 덮어써
80차원 중 6칸이 상수다. featNames **중간**이라 지금 빼면 98만 표본의 인덱스가 밀린다 → 못 뺀다.
미룬 일은 잊힌다(V12.47 의 원핫 4칸이 여러 판을 그렇게 지났다).
→ `check-live-only-feats` 가 `LUXML.featVer > 17` 인 순간 **배포를 막는다.** featVer 상향은
  표본을 어차피 재각인하므로 인덱스가 밀려도 되는 **유일한 시점**이다.
→ **덫에 걸리면 `B4_FEATVER` 를 올리지 말 것** — featNames 6칸 제거 · `_LIVE_ONLY_FEATS` 비우기 ·
  중립화 블록 제거 · 그 상수 삭제가 정답이다.

### 배포 뒤 볼 것 (V33.355)
1. **위원 수가 줄었는지.** B-3 은 가중을 깎는 변경이라, 전진 IC 가 음수인 채 대기하던 위원
   (XALPHA 류)은 화면에서 사라지거나 배수가 내려간다. 그게 의도다 — 다만 B-6(위원회가 사실상
   GBDT 한 명)과 겹치므로 **몇 명이 남았는지** 같이 볼 것.
2. `forwardFloor` 는 0 이고 비교가 `<=` 다 — IC 가 **정확히 0** 이면 음수 취급이다. 의도한 것이나 재확인.

### 앞선 판 (V33.354)

### D-1 — 비용 함수의 "데이터" 변경이 원장을 소급해서 다시 썼다
`computeCashFromTrades` 는 과거 체결을 **매번 재생**한다. 규칙 변경은 시행일 상수로 막아 뒀는데
(`ETF_TAX_EXEMPT_FROM`·`EXT_SLIP_FROM`) ★집합 멤버십 `ETF_SYMBOLS` 에는 날짜가 없었다★ —
한국 ETF 를 나중에 넣으면 **시행일 이후 과거 매도 전부**가 비과세로 재계산되어 현금이 소급해 바뀐다.
**git 200커밋을 봤다: 집합 내용은 한 번도 안 바뀌었다** → 아직 안 터진 지뢰다(그래서 "중").
→ `ETF_TAX_MEMBER_FROM` + `_etfTaxExemptAt(symbol, ts)`. 기존 28종은 표에 없어 `from == null` →
  **과거 현금을 1원도 안 바꾼다.** 앞으로 추가할 때 날짜를 같이 적으면 그 전 매도는 과거대로 남는다.
→ `cash_ckpt` 이전 구간은 여전히 얼어 있다. **이건 "앞으로 소급이 안 생기게" 막은 것이지
  얼어 있는 체크포인트를 검증한 게 아니다.**

### E-2 — 조용히 삼키는 catch 중 ★실패가 성공처럼 보이는★ 4곳
빈 catch 는 **1,045곳**(문서의 922 는 `catch (e) {}` 한 철자만 센 과소집계였다).
전수로 훑어 **돈 경로 13곳**을 하나씩 읽고, 그중 ★삼키면 거짓말이 되는★ 4곳만 고쳤다 —
`verifyAfterTrade`(대조 실패가 "대조 통과" 로 보임) · `auditAccounting` 자가치유(실패해도
**"자가치유 성공"** 을 찍고 있었다 → `_healed` 플래그) · `LEDGER_DUP`(검사 실패 = "중복 없음") ·
본전 잠금(실패 = "잠금 걸림").
**숫자는 안 줄었다** — 로그 자체가 실패할 수 있어 4곳 모두 `catch (e2) {}` 로 한 겹 감쌌다.
줄어든 건 개수가 아니라 **거짓말**이다. 나머지 9곳은 실패해도 아무 주장을 안 해서 그대로 뒀다.

### 게이트 `check-ledger-retro` (신설)
28종 기준 명단을 박아 두고 ① 집합이 늘었는데 날짜가 없으면 실패 ② 날짜 정합성 ③ 시행일 전후
세율이 갈리는지 **실행으로** ④ 같은 원장 두 번 재생 = 같은 현금(회계 결정성) ⑤ 감시자 4곳이
실제로 로그를 남기는지. 돌연변이 8종 전부 잡는다.

### 배포 뒤 볼 것 (V33.354)
1. **새 문구 4개가 실제로 뜨는지** — `체결 후 대조 자체가 실패` · `원장 중복 검사 실패` ·
   `본전 잠금 실패` · `[AUDIT-FIX]`. 뜬다면 그동안 조용히 실패하고 있었다는 뜻이다.
2. 한국 ETF 를 새로 넣을 때 게이트가 막으면 **우회하지 말고 `ETF_TAX_MEMBER_FROM` 에 날짜를 적을 것.**

### 앞선 판 (V33.352)

### E-1 — 같은 문장 43회가 로그를 덮어 새 사고를 묻었다
실측: TIME-CAP 43회 · MLOPS 23회 · 기타 13회. 로그 보존은 전체 1,500행 + ERROR/WARN 1,000행이라
한 문장이 43줄을 먹으면 그만큼 다른 사건이 사라진다. 선례 — 09-10 에 `[FETCH] 평가가능 0종목` 이
21회 쌓이는 동안 진짜 원인은 전혀 다른 것(시간외 가드)이었다.
→ `log()` 가 같은 문장을 10분 창으로 묶는다. **`ts` 는 첫 발생 그대로** 둔다 — 갱신하면
  그 줄이 계속 맨 위를 차지해 고치려던 일이 도로 난다. 본문에 `(×43 · 최근 12초 전)`.
  **새 문장은 언제나 새 줄**, 통계(`__engineErrCount`)는 줄 수가 아니라 사건 수를 그대로 센다.

### C-2 — 자가진단이 98만 행을 요청당 세 번 훑었다
총량 1회 + 24h 1회 + 7d 1회. 뒤 둘은 `COALESCE(ins_ts, ts)` 라 인덱스를 못 탄다.
**그런데 `_mlCountsCached` 가 이미 있었다** — `GROUP BY` 한 방 + 60초 캐시. V12.130b 가
`/api/ml-status` 100초 지연을 고치며 만든 그 함수다. 자가진단만 그걸 안 쓰고 있었다
(C-3 FLOW 일봉과 같은 모양 — 처방은 있는데 새 코드가 그걸 모른다).
→ 유입 집계를 그 캐시 안으로 옮겨 `CASE` 합 한 번으로. 자가진단의 무거운 스캔 3회 → 0회.
→ 재학습 디스패치의 COUNT 는 **되돌렸다**. 그 한 줄이 selfcheck 비용이라는 읽기가 틀렸고,
  게이트가 그걸 잡아 줬다.

### A-5 — v7 이 죽으면 시간외 보강이 0종목이었다(24가 아니라)
흐름은 `v7 배치 → 분봉 폴백 → 시간외 보강` 인데, 폴백이 예산을 0 까지 다 썼다.
v7 이 죽으면 전 종목이 폴백 대상이라 예산 200 을 통째로 먹고, 뒤의 보강은 한 종목도 못 돈다.
**그리고 v7 이 죽었을 때가 바로 보강이 가장 필요한 때다.** 게이트로 재현: 고치기 전 0회 → 고친 뒤 28회.
→ 시간외 창이면 40 을 먼저 떼어 두고 폴백을 멈춘다(예산 자체는 안 늘림). 상한 24 → 120.
→ **같이 발견**: 회전 커서가 '하려던 수' 만큼 밀려, 예산에 막혀 **안 해 본 종목이 영영 빠졌다.**
  실제 시도 수로 민다.

### 배포 뒤 볼 것
1. **`/api/selfcheck` 실측이 줄었는지.** 안 줄면 병목은 `ml_samples` 가 아니라
   `logs` 400행 조회 2건 또는 `pipe` 의 `ai_stage:` 범위 스캔이다.
2. **로그 화면에 `(×N)` 이 보이는지, 새 ERROR 가 반복 줄 위에 뜨는지.**
3. 앞선 판 확인 대기분(그대로): 2026-11-19·11-27·12-24 세션 전환(A-8) ·
   17~20시 KST 미국 장전 시세(A-9) · `LEDGER_DUP` 로그(D-3) · Modal 회차 요약의
   `상태 저장소 없음`(B-9) · `[CF]` 편입 건수(B-7) · 실거래 표본 35건.

## Previous handoff — V33.326

- Owner: Claude
- Last commit: V33.326 (resolve with `git log`)
- Base: 9bb942b / V33.325. 작업 전 `git fetch origin main` 확인(뒤처짐 0).
- Scope: 사용자 지적 — "또 신규 위원들 작동 안하는데 원인 분석해서 수정해라".

### Claude V33.326 — 2026-09-09

- **원인(운영 실측으로 확정)**: worker-probe 로 프로덕션 `/api/ai-mode` 를 떠 보니 공용
  트레이너(`_miniLogisticTrain`)를 쓰는 위원이 ★전원★ 같은 자리에 걸려 있었다.
    FLOW 21일=관측 2개(t 5.46) · XALPHA 24일=2개(t 2.94) · STACK 38일=3개(t -0.88)
    · 이중헤드 강세 25일=2개(t 2.57) · 약세 25일=2개(t 4.92)  → 전부 tier=pending, mult=0
  t 가 5.46·4.92 로 충분히 유의한데도 합류를 못 한다 — 문턱 미달이 아니라 ★기간이 모자라
  판정 자체가 보류★ 됐다(expertAdmit 의 minBlocks=5 게이트).
  기간이 짧았던 이유는 홀드아웃을 '행의 마지막 20%' 로 잘랐기 때문이다. 수확이 한 날짜에
  전 종목을 쌓으므로 8,000행이 21일밖에 안 되고, 라벨 지평 10일 기준 겹치지 않는 관측이
  2개뿐이다. ★표에는 재료가 있는데 안 읽은 것★ — 창(40,000행)은 100일 넘게 덮는다.
- **왜 '또' 인가 — 반쪽만 고쳐져 있었다**: V33.303 이 MEMO 에 대해 정확히 같은 진단을 하고
  달력 고정으로 고쳤는데, 그 수정이 `MEMOML` 안에만 들어가 같은 병을 앓는 flow·xalpha·
  stack·이중헤드는 그대로 남았다. MEMO 는 지금 이 게이트에 안 걸린다(t -1.37 로 '못 미침'
  판정을 받는다) — 그게 대조군이다.
- **고침** (`src/index.js`): 공용 트레이너에도 같은 달력 홀드아웃을 넣었다. 홀드아웃 =
  가장 최근 `MINIHOLD.days()` 일을 12칸으로 나눠 균등추출(기간은 지키고 행은 holdCap 9000 으로
  묶음 — CPU·메모리 예산 불변), 학습 = 홀드아웃 시작 − 엠바고(지평 1회) 이전에서 최근
  window 행. 분할 경계는 행 비율이 아니라 ★시각(ts)★ 으로 잡는다(파싱 탈락이 있어 행 수로는
  못 맞춘다). 이력이 짧으면 종전 20% 로 물러서고 로그에 이유를 적는다.
- **기간을 상수로 박지 않았다**: `MINIHOLD.days()` 가 `minBlocks × 지평` 에서 계산한다.
  지평이 10→15 로 바뀌는 날 상수 60 은 조용히 부족해지고 위원들은 다시 대기로 돌아간다 —
  방금 겪은 그 사고다. 여유는 40% 로 잡았다: 칸별 균등추출이 ★가장 오래된 칸에서 한 칸 폭만큼
  기간을 깎기★ 때문에 20%(60일)로는 실측 55~58일, 관측이 정확히 5개로 문턱에 걸친다
  (행/일 380·1500·4000 세 밀도로 시뮬레이션해 확인). 70일이면 65~68일 → 관측 6개로 여유 1개.
  대가는 숨기지 않는다 — 배포 모델이 홀드아웃+엠바고(80일)만큼 오래된 구간에서 적합된다.
- **새 게이트** `tools/check-mini-holdout.mjs` (deploy.yml 배선 완료): 설정이 정말 minBlocks 를
  넘기는지 ★산수로★ 검증한다. ① 목표 기간 ② 칸 손실을 뺀 최악 기간에 ★여유 1개★ ③ 학습 몫
  ④ 공용 트레이너가 실제로 달력 분할을 하는지 ⑤ MEMO 와 설정이 갈라지지 않았는지.
  세 가지 되돌림(여유 1.2 로 축소 · 지평 15 로 변경 · 시각경계 삭제)을 실제로 넣어 ★전부
  잡히는 것을 확인★ 한 뒤 원복했다.
- **기존 게이트 강화** (`tools/check-holdout-anchor.mjs`): 이 판으로 `_calWhy = "이력 "` 이 두
  곳이 되면서, 변이 시험의 `String.replace`(첫 번째만 치환)가 헛돌게 됐다 — 계약을 다 지워도
  통과하는 상태였다. ★그 검사가 스스로 실패해서 알려줬다.★ `replaceAll` 로 바꿔 전부 지워야
  잡히도록 되돌렸다.
- 검증: `node --check` 통과, 88종 게이트 전체 통과, `git diff --check` 통과.
- 배포 후 확인할 것: 다음 야간 학습이 돌고 나서 worker-probe 로 `/api/ai-mode` 를 다시 떠,
  flow·xalpha·stack·이중헤드의 `valICspanD` 가 60일대로 오르고 `valICeff ≥ 5` 가 되는지,
  그리고 tier 가 pending 을 벗어나는지 본다. ★학습이 한 번 돌기 전에는 옛 레코드 그대로다.★
- 학습·주문 트리거 없음, 정책 값(requireTrustedModel 등) 변경 없음, 시크릿 없음.

## Previous handoff — V33.325

- Owner: Claude
- Last commit: V33.325 (resolve with `git log`)
- Base: b7b61a8 / V33.324 (Codex). 작업 전 `git fetch origin main` → 3커밋 뒤져 있어
  `git pull --ff-only` 로 맞춘 뒤 시작했다(CLAUDE.md 규칙).
- Scope: 사용자 지적 — "AI 두뇌 작동 로그가 오류난거 같은데 글씨가 세로로 쓰인다".

### Claude V33.325 — 2026-09-08

- **원인: 매달린 선택자(dangling selector) 하나.** `public/index.html` 인라인 CSS 에
  '스크롤 경계 페이드' 규칙이 있었고 선택자가 `#page-nnviz .nlv-feed, #page-nnviz .nlv-toplist`
  였다. 언젠가 `.nlv-toplist` 를 이 페이드에서 빼면서 ★선언 블록을 통째로 지우고
  `#page-nnviz .nlv-feed,` 만 쉼표째 남겼다.★ CSS 는 그 쉼표를 다음 규칙까지 이어 읽으므로,
  판정 로그가 바로 아래 "최우선 후보 카드" 규칙
  (`display:grid; grid-template-columns:repeat(3,1fr)`)을 그대로 물려받았다.
- **증상이 왜 '세로 글씨'였나(실측)**: `.nlv-feed` 가 3열 그리드가 되면서 로그 줄 하나하나가
  칸에 갇혀 `.nlv-line` 이 141×300px 이 됐고, 줄 안의 `58px 1fr` 에서 본문 칸이 36px 로
  짜부라져 글자가 한 자씩 세로로 쌓였다. 고친 뒤 같은 화면에서 줄 435×56px,
  본문 칸 330px, 트랙 `58px 329.5px` — 정상 가로 흐름.
- **고친 방법**: 매달린 선택자 한 줄을 지웠다. 페이드 마스크는 이미 선언이 사라진
  유물이고 현재 두뇌 콘솔은 평면·무채색 디자인이라 되살리지 않았다. 사고 경위를 그 자리에
  주석으로 남겼다.
- **재발 방지 게이트** (`tools/check-ai-ops-ui.mjs`): 인라인 `<style>` 을 중괄호 깊이를 세며
  훑어(@media 안쪽 포함) `.nlv-feed` 가 들어간 선택자 목록 중 `display:grid` 나
  `grid-template-columns` 를 선언하는 규칙이 있으면 실패시킨다. ★버그를 일부러 되살려
  게이트가 실제로 잡는 것을 확인한 뒤★ 원복했다(FAIL 메시지에 붙어버린 선택자를 그대로 출력).
- 검증: `node --check` 통과, 87종 게이트 전체 통과, `git diff --check` 통과.
  1366px·390px 두 폭에서 로그가 정상 가로 흐름이고 가로 오버플로 없음.
- 학습·주문 트리거 없음, 정책 값 변경 없음, 시크릿 없음.

## Previous handoff — V33.324

- Status: complete; V33.324 deployed and verified in the production browser.
- Owner: Codex
- Branch: main (direct main and live deployment explicitly requested)
- Last commit: HEAD / V33.324 (resolve with git log -1)
- Base: a5d8e14 / V33.323; origin/main was fetched and matched before work.
- Scope: delete existing DNN-family and SEQ visualization designs and implement new responsive living neural scenes.

### Codex V33.324 — 2026-09-09

- Deleted old SVG neural renderer, node tooltip/hit-test renderer, SEQ card geometry, animation/gesture renderer, and their dedicated styles (~1,100 HTML lines). Model payload/status/banner and admission logic remain in existing adapters.
- New `public/neural-observatory.js` + `.css`: independent Canvas engine. DNN has golden-angle neuron bodies and moving axon signals; SEQ has 3D helical time/stage bundles with perspective, camera orbit, pinch/drag/keyboard, zoom, fit, head/block/time and individual node selectors.
- Every DNN neuron retains its source index/value (live payload: 2,428). SEQ overview samples non-selected time slices for speed; selected time includes every dimension, and full-detail mode exposes all 2,737 nodes. Only measured attention carries numeric edge weights. Animation is explicitly marked illustrative, not live activation.
- DNN static raster cache; <=100 moving signals, 30fps scheduling, device pixel ratio capped at 1.5. Intersection/visibility observers stop hidden scenes; replacement mount disposes the prior renderer. Reduced-motion disables motion by default. Full-detail selection stops automatic rotation.
- New engine executable tests replace old SVG-shape assertions; retained server-side SEQ training/weight/probe checks. The new gate is wired into deploy.yml. Browser probe tests detail nodes, actual inspector data, head/block/time, zoom, keyboard fit, pause/resume and hidden-scene pause/resume.
- Browser: 390x844, 820x1180, 1366x900 observed without document horizontal overflow. SEQ overview average Canvas draw ~2.4ms; mixed full-detail interaction probe ~5.5ms in this browser (not a physical-device frame-rate guarantee). Screenshots in workspace `outputs/neural324-*`.
- Final validation: all 87 `tools/check-*.mjs` gates passed; after final mobile layout/cache adjustments, neural engine, inline JS, AI operations checks and `git diff --check` passed again. DNN cached average draw ~0.47ms. Narrow DNN uses a 3-column serpentine arrangement rather than shrinking all 12 layers into one tiny row.
- Live evidence: implementation commit `b5b5fa530969cdc5604364a3a8bef9405ef9f92a`; GitHub deployment run `34269735567` completed successfully. Production returned `lux-build=V33.324`. Production SEQ browser probe at 1366x900 passed detail/block/head/time/zoom/pause/hidden-resume; DNN at 390x844 rendered 2,428 neurons / 2,992 structural edges without horizontal overflow, average cached draw 0.64ms. No browser JS errors. Final screenshots: `outputs/neural324-live-seq.png`, `outputs/neural324-live-dnn-mobile.png`. This follow-up handoff commit changes documentation only.
- No training/order actions, model algorithm, binding or cron changes. Old shared status panels/sidebar remain outside this renderer scope.

## Previous handoff — V33.323

- Status: complete locally; deployment must be checked against this exact HEAD after push.
- Owner: Codex
- Branch: main (direct main authorized; no PR)
- Last commit: HEAD / V33.323 (resolve with git log -1)
- Base: abece41 / V33.322 from origin/main. User said they had edited brain observatory separately, so Codex fast-forwarded before applying this patch.
- Scope: 사용자 지시 — "최근 스캔에서 반사실 후보 보다가 갱신될 때 화면이 위로 올라가는 문제 해결, 모델 구조 인공신경망 디자인 완전 변경, SEQ 3D 구조관측 3D 모델링 변경, 렉 없이 살아움직이는 느낌".

### Codex V33.323 — 2026-09-08

- 최신 GitHub 코드 우선: 로컬 수정분을 stash 한 뒤 origin/main V33.322로 fast-forward, 충돌은 upstream V33.322 로직을 유지하고 이 UI 패치만 다시 얹었다.
- `public/workspace-ui.js`: 두뇌관측 모델 뷰 재렌더가 더 이상 `brain.scrollTop = 0`을 강제하지 않는다. 현재 window/page scroll과 page-local scroll을 저장했다가 기존 `openNnViz()` 재호출 후 복원해, 최근 스캔/반사실 후보를 보는 중 갱신이 화면을 맨 위로 끌어올리지 않게 했다.
- `public/index.html` + `public/workspace-layout.css`: DNN 신경망 관측은 기존 데이터, 노드 hit-test, 툴팁, 포커스 하이라이트, 4,500 edge cap을 유지하면서 흑백 slice deck, square layer plates, CSS-only signal flow로 다시 디자인했다.
- SEQ 3D 관측은 기존 projection/control/pick/data mapping은 유지하고 `.sq3-neural` wireframe stage와 가벼운 CSS pulse/flow로 다시 모델링했다. `prefers-reduced-motion`에서는 애니메이션이 꺼진다.
- `tools/check-ai-ops-ui.mjs`: 스크롤 보존 계약과 새 DNN/SEQ 렌더 훅을 회귀 검사에 추가했다.
- 검증: V33.323 기준 모든 `tools/check-*.mjs` 통과, `node --check src/index.js` 통과, `git diff --check --cached` 통과. 로컬 preview `127.0.0.1:8766`에서 V33.323 로드, DNN 2,428 nodes / 4,500 capped flow edges, SEQ 3D SVG stage 생성, document horizontal overflow 없음.
- 스크린샷: `outputs/brain-dnn-v33.316.png`, `outputs/brain-seq-v33.316.png`는 같은 시각 패치의 rebase 전 캡처다(최종 버전 번호만 V33.323으로 올라감).
- 학습·주문 트리거 없음, 정책 값/바인딩/크론 변경 없음, 다른 페이지 디자인 변경 없음.

## Previous handoff — V33.322

### Claude V33.322 — 2026-09-08

- **판정 깔때기만 한 단계 촘촘하게** (`public/workspace-layout.css`): 글자 크기는 그대로 두고
  여백·막대 두께만 줄였다 — `.nlv-funnel` 패딩 8px→5/6px, 행 간격 6px→3px, 막대 9px→8px,
  KPI 칸 패딩 5px→4px. 카드 머리글도 이 카드에서만 10px→7px 로 낮췄다(다른 카드 머리글은
  건드리지 않았다 — 실측으로 "실시간 스캔 종목" 머리글이 35px 로 유지되는 것 확인).
- 실측: `.nlv-funnel-wrap` 287px → 262px(-25px, 약 9%). 머리글 35→29, 본문 174→157,
  KPI 46→44. "아주 조금"이라는 요청에 맞춘 폭이며, 5개 행·수치·퍼센트는 그대로 읽힌다.
- 검증: `node --check` 통과, 86종 게이트 전체 통과, `git diff --check` 통과, 1366px 화면
  스크린샷으로 가독성 확인.
- 학습·주문 트리거 없음, 정책 값 변경 없음, 시크릿 없음.

## Previous handoff — V33.321

- Owner: Claude
- Last commit: V33.321 (resolve with `git log`)
- Base: V33.320 (같은 세션, 직전 커밋), 배포 run 34178419604 성공 확인 후 시작.
- Scope: 사용자가 실제 화면 캡처와 함께 "이걸 비율이 맞다 하는거냐? 다시 디자인해". V33.320 의
  가로 배치가 사용자 화면 폭에서 그대로 접혀 있었다 — 추측 말고 실측으로 원인을 잡고 재설계.

### Claude V33.321 — 2026-09-08

- **왜 V33.320 이 사용자 화면에서 안 먹혔나(실측)**: 헤드리스로 사용자 화면 폭(1366px)을
  재보니 `.nlv-core` 가 ★477px★ 이었다. V33.320 은 지표(156px)+근거(300px)=456px 를
  기준으로 나란히 두게 했는데, 패딩까지 더하면 477px 를 넘겨 ★그대로 세로로 접혔다★.
  접힌 카드는 722px 까지 길어지고, 옆 "판정 깔때기"는 내용이 287px 뿐인데 grid stretch 로
  같은 722px 로 늘어나 절반 이상이 검은 여백이 됐다 — 사용자가 캡처로 보여준 그 화면이다.
- **구조를 바꿨다(뉴스·환율 화면과 같은 원칙)**: 높이가 크게 다른 블록을 나란히 세우면
  반드시 한쪽이 빈다. `.nlv-hero` 를 1열로 바꿔 "실시간 스캔 종목"과 "판정 깔때기"가 각자
  제 줄에서 전체 폭을 쓰게 하고, 넓어진 폭 ★안에서★ 차트·지표·근거를 가로로 나눴다
  (새 래퍼 `.nlv-body`). 카드 높이 722px → 417px, 세 칸 높이는 서로 정확히 같다.
- **flex-grow 를 1 미만으로 두면 안 된다(실측으로 두 번 걸렀다)**: 지표 레일에 `flex:0 1`
  을 주니 좁은 화면에서 접힌 줄이 168px 에 머물러 135px 가 비었고, `0.5` 로 고쳐도 남는
  폭의 절반만 먹어 67px 가 비었다 — grow 합이 1 미만이면 남은 공간이 전부 분배되지 않는다.
  `flex:1` 로 두고, 넓은 화면에서 레일이 과하게 벌어지는 건 `@media (min-width:1200px)`
  에서만 `max-width:300px` 로 잡았다(접히는 폭에서 상한을 걸면 그 상한이 곧 빈칸이 된다).
- **반사실 후보 그리드 auto-fill → auto-fit**: auto-fill 은 항목이 모자라도 빈 트랙을 만들어
  4건만 있을 때 오른쪽이 배경색 그대로 남았다.
- 검증(실측): 390 / 768 / 1366 / 1920 네 폭에서 세 칸 폭 합이 줄 폭과 정확히 일치 —
  390 스택(303/303/303), 768(333+188 → 521), 1366(385+221+323=929),
  1920(560+300+623=1483). 전 폭 가로 오버플로 없음. 86종 게이트 전체 통과.
- 학습·주문 트리거 없음, 정책 값 변경 없음, 시크릿 없음.

## Previous handoff — V33.320

- Owner: Claude
- Last commit: V33.320 (resolve with `git log`)
- Base: V33.319 (같은 세션, 직전 커밋), 배포 run 34175414914 성공 확인 후 시작.
- Scope: 사용자 지시 — "카드 비율 다시 맞춰 그리고 작은 글씨로 해서 지표 옆에 쓰이게하고
  그래프 크기를 줄여". 실시간 스캔 종목 카드가 세로로만 길어진 것을 바로잡았다.

### Claude V33.320 — 2026-09-08

- **그래프 축소** (`public/index.html`): `drawLineChart` 의 viewBox 를 460×190(2.4:1) →
  460×124(3.7:1) 로 눕혔다. svg 가 `width:100%; height:auto` 라 그려지는 높이는 전적으로
  viewBox 비율이 정한다 — 카드 폭 550px 기준 약 227px → 약 148px. 폭은 그대로 다 쓰고
  높이만 줄어든다. 축 여백(padT 12→9, padB 18→15)도 함께 줄여 그래프가 눌리지 않게 했다.
  자리도 같이 줄였다: `.nlv-tp-chart` 의 `min-height:172px` → `0`(컴팩트 뷰의 150px 도 해제)
  — 안 줄이면 낮아진 그래프 아래에 빈 공간만 남는다.
- **지표를 세로 레일로, 판단 근거를 그 옆에** (`public/index.html`, `public/brain-console.css`):
  새 래퍼 `.nlv-detail`(flex)로 지표와 판단 근거를 나란히 놓고, 글자를 한 단계씩 줄였다.
  ★가로 타일을 세로 목록으로 바꾼 이유★: 9개를 가로 3열로 깔면 3줄(≈80px)에서 끝나 옆
  근거 칸(≈200px)보다 훨씬 짧고, 그 아래가 빈 검은 덩어리로 남았다(첫 시도에서 실제로 그랬다).
  한 줄에 하나씩 세우니 높이가 근거 칸과 맞아 빈 칸이 사라졌다.
  구분선도 '간격으로 컨테이너 배경 비추기'에서 셀 테두리로 바꿨다 — 남는 자리가 어두운
  선 색 그대로 드러나지 않게. 좁은 화면에서는 flex-wrap 으로 위아래로 돌아간다(모바일 확인).
- **주석 경계표 보존** (`public/index.html`): `tools/check-ai-ops-ui.mjs` 가 `// 종가 선 그래프`
  를 코드 구간 잘라내기 경계로 쓴다. 처음에 이 주석을 블록 주석으로 바꿨다가 게이트가
  함수를 못 찾아 실패했다 — 첫 줄을 원래 문구 그대로 되돌리고 설명은 아래에 덧붙였으며,
  그 사실을 주석에 적어 두어 다음 사람이 같은 데 걸리지 않게 했다.
- 검증: `node --check` 통과, 86종 게이트 전체 통과, `git diff --check` 통과. 헤드리스
  크로미움 확인 — 데스크톱(1600×1000)에서 지표 레일과 근거 칸 높이가 맞아 빈 칸 없음,
  모바일(390×844)에서 두 칸이 정상적으로 위아래로 쌓임(각 303px)이고 가로 오버플로 없음
  (scrollWidth == clientWidth), 그래프는 데스크톱 ≈148px · 모바일 75px 로 축소 확인.
- 학습·주문 트리거 없음, 정책 값 변경 없음, 시크릿 없음.

## Previous handoff — V33.319

- Owner: Claude
- Last commit: V33.319 (resolve with `git log`)
- Base: V33.318 (같은 세션, 직전 커밋), 배포 run 34174328087 성공 확인 후 시작.
- Scope: 사용자 지적 2건 — ① V33.318 로 넣은 스캔 지표가 화면에 안 보인다 ② 확률의 근거가
  무엇인지도 표시해 달라.

### Claude V33.319 — 2026-09-08

- **지표가 안 보이던 진짜 이유**: V33.318 은 야간 스캔이 픽에 `ta` 를 실어 보내게 했는데,
  ★스캔은 하루 한 번 돈다★. 배포 직후엔 저장된 픽이 전부 옛 코드가 만든 것이라 `ta` 가 없고,
  화면은 다음 밤까지 계속 비어 있었다. "다음 스캔부터 보입니다"라고 안내만 하고 끝낼 일이
  아니었다 — 기능을 넣고 아무것도 안 보이는 상태로 둔 것이다.
- **`/api/ta-explain?symbol=` 신규(읽기 전용)** (`src/index.js`): 지금 보고 있는 ★그 한
  종목만★ 즉석 계산한다. 새로 받아오지 않는다 — 이미 있는 일봉 캐시(`daily:<sym>`)와 지수
  캐시만 읽고, 없으면 없다고 답한다(지어내지 않는다). 숫자는 스캔이 쓰는 것과 같은 함수
  (`mlBuildFeatures` → `luxTaFromFeat`)로 만들어, 나중에 스캔이 채워 넣는 값과 어긋날 수 없다.
  응답: `ta`(9종) + `reasons`(taPredictDirection 이 점수를 쌓으며 남긴 근거 문자열) +
  `tf`(다기간 컨센서스 now/week/month/year) + `upProb`/`confidence`. SWR 60초 캐시.
- **지표 목록 단일화** (`src/index.js`): `LUX_TA_KEYS` + `luxTaFromFeat()` 를 만들어 스캔과
  즉석 조회가 ★같은 목록·같은 단위★ 를 쓰게 했다. 각자 목록을 들고 있으면 언젠가 갈라져
  같은 종목인데 두 화면 숫자가 달라진다.
- **판단 근거 패널** (`public/index.html`, `public/brain-console.css`): 사용자 요청("확률의
  근거가 된것이 뭔지도"). ★두 가지를 반드시 구분해 적는다★ —
    · 확률 `p` 는 위원회(신경망 합의)가 100여개 피처를 보고 낸 값이다. 어떤 한 지표가 p 를
      만들었다고 말할 수 없어서, "모델 합의 — 단일 지표가 아니라 피처 전체를 본 값"이라 적는다.
    · 랭크는 산수 그대로 분해된다: `p + 기술가중×tech + 대형주가중×blue` → `×(1+evTilt)`.
      가중치는 하드코딩하지 않고 서버가 `/api/ai-picks` 의 `rankWeights` 로 내려준 실제 엔진
      값을 쓴다(엔진 설정이 바뀌는 날 화면만 옛 수로 남지 않게).
    · `reasons` 는 ★기술 점수★ 의 근거이지 p 의 근거가 아니라서, 제목을 "기술 근거"로 못박았다.
  가산 항목은 `rankP` 가 있는 픽(= 엔진이 실제로 그 산수를 한 픽)에만 적는다 — 진입루프가
  만든 픽에는 랭크가 없으므로 합계 없는 숫자를 띄우지 않는다.
- 이미 저장된 픽도 `p`/`tech`/`blue`/`rankP` 를 갖고 있어, 랭크 분해는 ★야간 스캔을 기다리지
  않고 즉시★ 보인다. 지표는 ta-explain 이 채운다.
- 검증: `node --check` 통과, 86종 게이트 전체 통과, `git diff --check` 통과. 헤드리스
  크로미움에 ★ta 가 없는 픽★(배포 직후 실제 상황)을 주입해 확인 — 지표 9종이 즉석 조회로
  채워지고, 판단 근거가 p·기술가산·대형주가산·최종랭크·기술근거·다기간추세까지 렌더됨.
  일봉 캐시가 없는 경우(available:false)도 확인 — 지표 칸은 사유를 적고, 랭크 분해는 그대로.
- 학습·주문 트리거 없음, 정책 값 변경 없음, 시크릿 없음.

## Previous handoff — V33.318

- Owner: Claude
- Last commit: V33.318 (resolve with `git log`)
- Base: V33.317 (같은 세션, 직전 커밋), 배포 run 34172044453 성공 확인 후 시작.
- Scope: 사용자 지시 5건 — ① 지표&환율 화면 이름을 "뉴스·환율"로 ② 뉴스를 보려고 내리면
  반쪽이 비는 비율 문제 재설계 ③ 작동 화면에서 "위원회 구성" 제거 ④ 잘려 있던 "판정 깔때기"·
  "실시간 스캔 종목"을 완전하게 복구 ⑤ 실시간 스캔에 MACD 등 종목 지표 추가.

### Claude V33.318 — 2026-09-08

- **화면 이름 통일** (`public/index.html`): 좌측 네비·모바일 드로어·페이지 헤더를 전부
  "뉴스·환율"로 맞추고, 탭 순서도 전체 브리핑 → 뉴스 → 환율 → 경제지표로 바꿨다.
  V33.315에서 뉴스가 이 화면에 흡수됐는데 이름만 옛 이름이라 뉴스를 찾는 사람이 지나쳤다.
- **뉴스·환율 레이아웃 재설계** (`public/index.html`, `public/workspace-layout.css`):
  종전 `.market-grid` 는 2열이라 짧은 지표/환율 열과 긴 뉴스 열이 나란히 섰고, 뉴스를 읽으려
  내려가면 반대쪽 절반이 통째로 비었다. 세 블록(뉴스·경제지표·환율)은 길이 차가 커서 무엇을
  옆에 세워도 한쪽이 빈다 — 전부 제 줄에서 전체 폭을 쓰도록 세로로 쌓고, 남는 폭은 블록
  ★안에서★ 채운다(뉴스는 다단 그리드, 표는 5열/3열이 넓게 펴진다). 뉴스 카드는 DOM 순서도
  맨 앞으로 옮겨 화면 순서와 탭 순서를 맞췄다.
- **≥1600px 에서 페이지가 쪼그라들던 진짜 원인** (`public/workspace-layout.css`):
  `.page` 는 세로 flex 컨테이너(`.content`)의 아이템인데 `@media(min-width:1600px)` 에서
  `margin:0 auto` 가 걸린다. ★교차축 auto 마진은 flex 의 stretch 를 끈다★ — 그 순간 페이지는
  내용 폭(측정값 572px)으로 줄어든 뒤 가운데 정렬돼, 뉴스만 보는 뷰에서 양옆이 통째로 비었다.
  전체 브리핑에선 경제지표 표(min-width:520px)가 폭을 벌려 증상이 가려져 있었다.
  `#page-macro` 에 `width:100%` 를 주어 1720px 까지 채우고 그 이상에서만 가운데 정렬되는
  원래 의도로 되돌렸다(이 페이지에만 한정 — 다른 화면의 전역 레이아웃은 건드리지 않았다).
- **작동 화면에서 "위원회 구성" 제거** (`public/workspace-layout.css`): DOM 을 지우지 않고
  `data-brain-view="operations"` 에서만 감춘다 — `#nlvCommittee` 를 찾는 renderCommittee 가
  조용히 죽지 않게. 명부는 "02 모델 구조" 뷰에 그대로 있고, 맨 위 요약의 COMMITTEE 지표
  (`#opsCommittee`)도 그대로라 가동 인원은 계속 보인다.
- **잘린 패널 복구** (`public/workspace-layout.css`): V33.315~316 은 작동 화면을 100dvh 에
  맞추려고 `.nnviz-content` 에 높이를 고정하고 자식마다 max-height + overflow 를 걸었다.
  그래서 스캔 필름스트립은 `max-height:150px; overflow:hidden` 에 잘려 카드 아랫단이 사라졌고,
  판정 깔때기는 `minmax(170px,1fr)` 칸 안에서 스크롤 막대에 갇혔다. 높이 고정과 클램프를
  전부 걷어내 자연 높이로 흐르게 했다(페이지가 그냥 스크롤된다). 밀도(패딩·글자 크기)는 유지.
- **실시간 스캔 종목 지표** (`src/index.js`, `public/index.html`, `public/brain-console.css`):
  야간 스캔이 이미 `mlBuildFeatures` 로 구해 놓는 값을 픽에 실어 보낸다 — ★추가 계산·추가
  fetch 0★. 반환이 featNames 순서의 숫자 배열이라, 이름→위치 색인을 루프 밖에서 한 번 만들고
  픽마다 그 자리 값만 뽑아 `pk.ta` 로 붙였다(9종: rsi14·macdH·volSurge·ret5·ret20·atrPct·
  rs20·maStack·taUpProb, 40종목만 저장되므로 크기 부담 없음). 화면은 ★피처 정의 그대로의
  단위★ 로 적는다 — macdH 는 MACD 히스토그램을 가격 %로 정규화한 값이라 "원 MACD 값"이라
  적지 않고, volSurge 는 20일 평균 대비 배수로 적는다. 스캔 카드엔 MACD·RSI·거래량 3종,
  히어로엔 9종 전부를 타일로. 방향은 ★글자색★ 으로만 말한다(배경은 무채색 유지).
  옛 스캔 결과엔 `ta` 가 없으므로 "지표는 다음 야간 스캔부터 표시됩니다"로 정직하게 비운다.
- 검증: `node --check` 통과, 86종 게이트 전체 통과, `git diff --check` 통과. 헤드리스
  크로미움에 API 목업을 주입해(1600×1000 / 390×844) 다섯 건 모두 눈으로 확인 — 뉴스 3단
  전체 폭, 지표 타일 마지막 줄까지 채움, 필름스트립·깔때기 잘림 없음, 위원회 구성 사라짐,
  모바일 가로 오버플로 없음(scrollWidth == clientWidth).
- 학습·주문 트리거 없음, 정책 값 변경 없음, 시크릿 없음.

## Previous handoff — V33.317

- Owner: Claude
- Last commit: V33.317 (resolve with `git log`)
- Base: V33.316 (같은 세션, 직전 커밋), deployment verified live before this round started.
- Scope: V33.316 을 배포한 직후 사용자가 재지적 — "배경색 푸른색은 빼라 두뇌 관측에서 글자만
  색 넣고 다시 모노크롬화시켜 배경이랑 창 색은 그리고 최우선 후보 말고 실시간으로 스캔하는
  종목 그래프랑 확률 ai가 판단하는 정보 띄우라고." V33.316 은 배경 토큰(`--ops-bg0~3`,
  `--ops-line`, `--ops-panel`)까지 시안 색조로 물들여 화면 전체가 파랗게 보였다 — 이번엔
  표면(배경·테두리·창) 토큰만 순수 무채색으로 되돌리고, 상태를 나타내는 글자색(강조 토큰)은
  남겼다. 또한 히어로 카드("최우선 후보")가 위원회 랭크1위에 고정돼 있던 것을, 아래 필름스트립
  (AI 스캔 순회)이 지금 스포트라이트하는 실시간 스캔 대상을 그대로 따라가도록 다시 연결했다.

### Claude V33.317 — 2026-09-08

- **배경 모노크롬화** (`public/brain-console.css`): `--ops-bg0/1/2/3`, `--ops-line`,
  `--ops-line2`, `--ops-panel`, `--ops-panel-hi`, `--ops-ink*` 토큰을 순수 무채색(R=G=B에
  가까운 회색/검정/흰색)으로 재정의. 강조 토큰(`--ops-cyan/green/amber/red/purple`)은 그대로
  두되, 이걸 큰 배경 면에 칠하던 곳만 골라 고쳤다: `.nnv-tab.active` 는 시안으로 꽉 채우던
  배경 대신 회색 배경 + 시안 글자로, `.nlv-scancard.pass/watch/reject .stamp` 는 색이 채워진
  배지 대신 투명 배경 + 테두리(회색) + 색 있는 글자로 바꿔 "글자만 색, 배경은 무채색" 원칙을
  일관되게 적용했다. 계산된 배경색을 헤드리스 크로미움으로 확인(`.rail`/`.nlv-core`/
  `.nnviz-content` 모두 R≈G≈B).
- **히어로 = 실시간 스캔 대상** (`public/index.html`): "최우선 후보 <small>위원회 랭크 1위</small>"
  로 고정 표시하던 카드를 "실시간 스캔 종목"으로 바꾸고, `renderCore(d)`(committee 랭크1위를
  `rankedLivePicks(d)`로 뽑아 그리던 함수)를 `renderCoreFromScan()`으로 교체 — 필름스트립이
  관리하는 `SCAN.list`/`SCAN.idx`(지금 스포트라이트 중인 스캔 대상)를 그대로 그린다. 필름스트립
  스크롤이 자동으로 넘어갈 때마다(`scanHighlight()`), 스캔 카드를 클릭할 때마다 히어로도 함께
  갱신된다. `scanVerdict()`(통과/관찰/탈락)를 재사용해 필름스트립과 같은 어휘로 표시하므로,
  기권(탈락) 종목이 지금 스캔 대상이어도 감추지 않고 "탈락"이라 보여준다(첫 요청부터의 요구사항).
  committee 랭크1위 자체를 보여주는 자리는 이제 없다 — `opsTopName`/`opsTopSignal`(맨 위 요약
  배너)은 원래 로직 그대로 committee 기준을 유지한다(사용자가 지적한 건 히어로 카드였다).
- **회귀 테스트 갱신** (`tools/check-ai-ops-ui.mjs`): 옛 계약("요약과 차트가 같은 최상위 후보를
  고른다")을 새 계약("히어로는 위원회 랭크1위가 아니라 지금 스포트라이트 중인 스캔 대상을
  따라간다" + "기권 종목도 지금 스캔 중이면 탈락이라 표시하며 숨기지 않는다" + "스캔 결과가
  비어 있으면 억지로 종목을 지어내지 않는다")로 다시 썼다 — 옛 테스트를 지우기만 한 게 아니라
  새 동작을 검증하는 assert 로 교체했다.
- `_BUILD_VER`/`lux-build`/CSS·JS 쿼리스트링을 V33.317 로 함께 올림.
- 검증: `node --check src/index.js` 통과, 86종 게이트 전체 통과(`check-ai-ops-ui.mjs` 포함),
  헤드리스 크로미움 1600×1100/390×844 스크린샷으로 배경 무채색·레이아웃 정상·히어로 라벨
  변경 확인.
- 학습·주문 트리거 없음, 정책 값(`AI_PARAMS.requireTrustedModel` 등) 변경 없음, 시크릿 없음.

## Previous handoff — V33.316

- Owner: Claude
- Last commit: V33.316 (resolve with `git log`)
- Base: V33.315 (Codex, pulled via fast-forward `git pull --ff-only origin main`), deployment verified live.
- Scope: 사용자가 "코덱스로 고친 AI 두뇌가 물빠진색"이라 지적 — brain-console.css 의 의도적
  `filter:grayscale(1)` + 무채색 토큰을 되돌려 채도 있는 미션컨트롤 팔레트로 복원. 동시에
  "다른 퀀트 AI 처럼 스캔중인 종목을 순차적으로 차트+확률로 보여주고 탈락이면 탈락이라 표시,
  AI 반사실 후보도 보여달라"는 요청에 맞춰 두 개의 새 패널(AI 스캔 순회 필름스트립, AI 반사실
  후보 목록)을 추가. 마지막으로 "지표환율/뉴스 통합이 제대로 안 된 것 같다"는 지적을
  코드로 확인해 NEWS 내비게이션 중복 항목을 제거(진짜 원인은 통합 로직이 아니라 `showPage()`가
  `news`→`macro` 로 id 를 바꿔치기 하면서 내비 하이라이트가 어긋난 것이었음).

### Claude V33.316 — 2026-09-07

- **색 복원** (`public/brain-console.css`): 두 곳의 `filter:grayscale(1)` 을 제거하고, 다크/라이트
  토큰을 시안(`#3ecbff`)/그린(`#33e6a8`)/앰버(`#ffbe3d`)/레드(`#ff5c74`)/퍼플(`#a78bff`) 기반의
  채도 있는 팔레트로 재정의. `.nnv-health-source.ok/.error`, `.nlv-cm .cm-dot` 상태색, `.nlv-line
  .buy/.warn em`, `.nnv-tab.active` 를 잉크 반전 대신 시안 강조로 교체.
- **AI 스캔 순회** (`#nlvScanWrap`/`#nlvScanRail`): `/api/ai-picks` 의 전체 유니버스 스캔 배열을
  가로 필름스트립으로 순차 하이라이트(2.2초 간격, hover/reduced-motion 시 정지)하며 종목별
  미니 차트(`pickChart`)와 랭크 점수, `abstain`/랭크 임계값 기반 통과·관찰·탈락 스탬프를 표시.
  `rankP` 는 위원회 순위 점수이지 보정된 확률이 아니므로 "확률" 대신 "랭크"로 표기(Codex
  V33.314 의 구분을 그대로 따름).
- **AI 반사실 후보** (`#nlvCfWrap`): 신규 읽기 전용 엔드포인트 `/api/cf-candidates` (`src/index.js`)
  가 `ml_candidates` 테이블에서 아직 라벨링 안 된 표본 최근 16건 + 대기 총건수를 반환(30초/1시간
  SWR 캐시, feat 벡터는 응답에 포함하지 않음). 프런트는 종목·전략·진입가·경과시간을 카드로 표시.
- **NEWS 내비 중복 제거**: 데스크톱 `data-page="news"` 항목에 `report`/`whatif` 와 동일한 방식으로
  `display:none`, 모바일 `NAVS` 배열에서 `news` 항목 삭제. `지표&환율` 탭이 이미 뉴스를 흡수하고
  있어 두 항목이 공존하면 `showPage()`의 id 치환 때문에 NEWS 클릭 시 하이라이트가 어긋났음.
- **레이아웃**: `.nlv-scanwrap` 이 `#nnvLive` 의 2번째 자식으로 추가되며 컴팩트(768px+/650px+)
  operations 그리드의 3-트랙 가정이 깨졌던 것을 `grid-template-rows`에 트랙을 하나 추가해 수정,
  `.nnviz-content`의 `overflow:hidden`→`overflow-y:auto` 로 방어.
- 검증: `node --check src/index.js` 통과, 86종 게이트 전체 통과, 헤드리스 크로미움(CDP)으로
  1600×1000/800×700 스크린샷 확인 — `.rail` computed `filter:none`(그레이스케일 해제 확인),
  스캔/반사실 패널 DOM 존재·display 정상, 컴팩트 그리드 레이아웃 깨짐 없음. 실 백엔드가 없는
  로컬 정적 서버라 `/api/ai-picks`/`/api/cf-candidates` 응답은 404 로 처리되었고(정상적인 에러
  UI 로 표시됨), 실 데이터가 있는 프로덕션에서 카드 렌더링을 재확인할 필요는 남아 있음.
- 학습·주문 트리거 없음, `AI_PARAMS.requireTrustedModel`/`EXTERNAL_LLM_DISABLED`/
  `EXTERNAL_AI_API_DISABLED` 등 정책 값 변경 없음, 시크릿 없음.
- Push 후 GitHub Actions 배포 확인 필요 — 정확한 실행 run 과 실서비스 `lux-build`/`/api/selfcheck`
  의 `build` 필드가 V33.316 인지 확인할 것 (`tools/*` 워크플로 프로브 사용, 직접 HTTPS 불가 환경).

## Previous handoff — V33.315

- Owner: Codex
- Last commit: V33.315 (resolve with `git log`)
- Base: 4da5c84 / V33.314; its deployment run 34146231149 succeeded and live version was verified.
- Scope: compact brain workspace, rebuilt responsive model topology, unified indicators/FX/news.

### Codex V33.315 — 2026-09-08

- Added page-scoped workspace-layout.css and UI-only workspace-ui.js. Operations, models and research
  views retain existing DOM/actions and all 14 model tabs. Workstation operation view fits viewport;
  dense panel contents may scroll internally. Mobile uses a natural vertical flow.
- Replaced the fixed-width overview SVG with keyboard-operable model cards, actual dimensions/slots,
  server roster states and correct provisional STACK blending. Original model evidence remains in details.
- Removed brain-theme overrides affecting the left sidebar/global theme tokens. Other page designs untouched.
- Moved existing news controls into the macro page; original news and FX routes alias the integrated view.
  Added view and sector filters, preserving refresh controls, tables and source links.
- Fixed FX polling checking a nonexistent page-fx, avoided duplicate initial macro/FX refreshes,
  added active-page news refresh, and retained last good data with an error stamp on request failure.
- Restricted external news links to HTTP(S), added noopener/noreferrer, escaped fallback publication text.
- Added production-function regression cases to the existing wired check-ai-ops-ui.mjs.
- All 86 tools/check-*.mjs passed (Node 24, PYTHONUTF8=1); worker syntax and git diff --check passed.
- Browser: 390x844, 768x1024, 1024x768, 1440x900 without horizontal document overflow.
  Workstation operation container bottom stays inside viewport; DNN graph navigation and TECH filter tested.
  Read-only preview proxies selected live APIs; unrelated API 503 toasts in preview are intentional.
- No training/orders triggered, predictive-accuracy claim, policy change, bindings/cron change or secrets saved.
- Push auto-deploys through GitHub Actions; verify exact commit run and live lux-build V33.315,
  workspace-layout.css and workspace-ui.js. Actual live host is ai-trader-app.blauenacht08.workers.dev.
- Earlier trainer/data-history limitations below remain open. Comments tagged Codex identify changed code.

## Previous handoff — V33.314

- Status: complete (implementation and local validation; verify the deployment run for this commit)
- Owner: Codex
- Branch: `main` — user explicitly requested direct main work, no PR.
- Last commit: HEAD (V33.314; resolve with `git log -1 --oneline`)
- Base: `6c87c2a` / V33.313, fresh clone and `git fetch origin main` reconfirmed before commit.
- Scope: V33.314 — monochrome AI brain redesign, model loading recovery, truthful UI and portable validation.

### Codex changes — 2026-09-08

- `public/brain-console.css`: dedicated page-scoped black/white design, large mission header, section navigation,
  inverse decision panel, responsive summary and content grids, square model controls, readable committee tiles,
  restored model roles/weights, explicit connection status symbols, light theme and reduced-motion support.
  Existing renderer surfaces use grayscale to remove hard-coded blue from SVG/canvas without rewriting
  numerical renderers. Other pages retain their theme. Existing legacy CSS remains as the compatibility base;
  the versioned stylesheet is the final authority for the brain page.
- Preserved 14 model tabs, training/GitHub/refresh actions, candidate charts, log pause, pipeline folding,
  model visualizers, and sidebar asset/budget/open-symbol functions. Header actions now remain visible on mobile.
- `rankedLivePicks`: summary and main chart select the same highest-rank non-abstaining candidate without
  mutating server data. All-abstaining scans no longer invent a top candidate. `rankP` is a rank score, not a
  calibrated probability: display it as a decimal with an explicit label, separately from the chart's `p`.
- Committee summary now understands actual `buildRoster` states `on`/`prov` (old `live`/`provisional`
  comparison always counted zero), and still updates when no candidate is available.
- Chart request sequence guards cover late success, empty responses, errors, cached switches and cleared
  candidates. A previous request can no longer erase a newer chart. Non-2xx responses display an error.
- `getState` gained opt-in strict reads; legacy callers retain their fallback behavior. Large model metadata
  failures now record `meta_read_error`, D1 chunk failures `chunk_read_error`, and invalid chunk counts are
  rejected before allocation. Valid raw reads clear stale health errors; JSON corruption remains distinguished.
- SEQ trust/model I/O failures no longer cache null for five minutes. Next request can recover; successful
  loads and explicitly untrusted models retain caching, and no stale model is used to bypass admission.
- Regression cases were added to the already-wired `check-ai-ops-ui.mjs` and `check-big-load-health.mjs`.
  No CI gate was removed and no workflow wiring changed.
- `.gitattributes` standardizes source LF for Windows/CI source-extraction checks, excludes the legacy UTF-16
  `src/index 50.js` backup. `check-syntax.mjs` uses esbuild's API (no Windows `npx.cmd` or `/dev/null` issue).
  Python verification uses native paths and explicit UTF-8 for the affected fixtures. Ignore local node_modules.
- Implementation comments carry `[Codex V33.314]`; no credentials are saved in tracked files or git remotes.

### Verification and deployment

- All 86 `tools/check-*.mjs` gates passed locally with Node 24, Python 3.11 (`PYTHONUTF8=1`), esbuild 0.28.1;
  `node --check src/index.js` also passed. CI repeats its configured checks on Node 22.
- Reproduced storage failures/recovery, SEQ negative-cache recovery, rank selection, roster states, and chart
  races using the production functions. Existing SEQ inference, MEMO cross-language, holdout, model evidence,
  accounting and risk-policy contracts remain in the full suite.
- Chromium preview: 1440px desktop and 390px mobile, no horizontal document overflow, 14 model controls
  retained. Read-only production API preview verified matching summary/chart symbol, on/prov counts,
  MEMO structure render and pipeline folding. Also exercised connection-failure UI and light theme.
- Main push triggers `Deploy to Cloudflare Workers`. Check the run associated with this exact commit before
  assuming production is current; the live HTML meta `lux-build` and worker build should both be V33.314.
- No retraining or order was triggered during verification. No claim of increased predictive accuracy.
- No changes to `wrangler.toml`, D1/R2 bindings, cron, model upload paths, external-AI policy, or
  `AI_PARAMS.requireTrustedModel` (remains user-authorized `false`).
- Earlier data-history limitations and external-trainer holdout-calendar work described below remain open;
  this change fixes proven code defects, not statistical evidence that has not yet accumulated.

## Previous handoff — V33.313 (historical context)

- Status: complete
- Owner: Claude Code
- Branch: `main` (직접 작업 — 2026-09-07 사용자 지시 "항상 main에 작업")
- Last commit: HEAD (this change; verify with `git log -1 --oneline`)
- Scope: V33.313 — 작동 화면과 구조 관측을 한 창으로 합쳤다(사용자 지시: "아직도 기존의
  모습이 많이 보여서 수정하고 작동화면과 구조 관측을 합쳐줘 창을 완전히 새롭게 디자인해줘
  오른쪽 사이드바 필요하면 없애도 됨"). V33.312 는 작동 화면(.nlv-*)만 새로 그렸는데,
  구조 관측(#nnvStruct — DNN·트리·선형·MEMO·SEQ 렌더러)은 손 안 대서 여전히 옛 "Apple
  재질" 파랑 톤이었다 — 전환 모드일 땐 안 보이니 몰랐는데 이제 그게 사용자가 본 "옛 모습"
  이었다.
- Base: `main`(V33.310). V33.305~309 는 여전히 `codex/find-issues-with-embedded-ai-models`
  브랜치에만 있다 — 병합하지 않고 필요한 부분만 옮겨 왔다(아래 "가져온 것" 참고). 그 브랜치를
  그대로 rebase/merge 하지 않는다 — V33.306·307·308·310 이 없는 바닥이라 게이트가 80종뿐이고,
  얹으면 `check-stale-base.mjs` 가 막는다.

### V33.305~309 에서 가져온 것 / 가져오지 않은 것

- **가져옴**: `AGENTS.md`(도구 공통 규약), `CLAUDE.md`의 인계 순서 문구,
  `tools/check-big-load-health.mjs`(대형모델 로딩 실패를 '미학습'과 구분 — `_bigLoadMark`/
  `_bigLoadStatus`, `/api/r2-status`·`/api/scalp-status` 응답에 `bigLoads` 필드),
  `tools/check-ai-ops-ui.mjs`와 그 배선(연결 텔레메트리·NOW 브리핑·HTTP 오류 가시화 —
  `liveHealth`/`liveFetchJSON`/`renderOpsBrief`, `loadPicks`의 `r.ok` 확인).
- **가져오되 값은 바꿈**: `tools/check-ai-handoff.mjs` — 원본은 `requireTrustedModel:true` 를
  강제했다. 그 값은 2026-07-22 사용자 지시로 `false` 로 완화된 실매매 리스크 정책이라
  2026-09-07 재확인(사용자: "false 유지") 후 강제 조건을 뺐다. 대신 그 값 옆에 날짜 있는
  근거 주석이 있는지만 본다 — 조용한 값 뒤집기 자체를 막는다.
- **가져오지 않음**: `src/index.js`의 `requireTrustedModel: true` 자체(위 이유로 `false` 유지),
  `tools/check-model-evidence.mjs`의 `requireTrustedModel:true` 단정 추가분.

### AI 작동 관제실 재설계 (미션컨트롤 방향 — 사용자 요청 "완전히 새롭고 세련되게 spacex 느낌")

- 이 화면(`.nlv-*`)은 이미 짙은 남색(#060910~#090e1a) + 시안 발광(#38bdf8) + IBM Plex Mono
  계기 숫자로 그려져 있었다 — 그 색·글꼴을 그대로 물려받아 완성했다(전면 교체가 아니라
  기존 언어를 끝까지 미는 방향 — 이미 있던 걸 갈아엎는 게 더 위험하고 저렴했다).
- 헤더: eyebrow 라벨("LUX INTELLIGENCE · OPERATIONS/STRUCTURE")·제목·부제가 작동/구조
  화면 전환에 맞춰 텍스트를 바꾼다(`setBrainView`).
- 연결 텔레메트리 바(`#nnvHealthbar`) — PICKS/COMMITTEE/PIPELINE 세 소스를 독립 상태로,
  LED 점 + 몬 라벨. NOW/DECISION 브리핑(`.ops-brief`) — 판정·최상위 신호·위원회·스캔
  최신성을 코너 브래킷 카드로. 둘 다 작동 화면에서만 보이고 구조 관측에선 숨는다.
  격자 대기층(`.nnviz-content::after`)은 아래로 갈수록 옅어져 내용을 가리지 않는다.
  `prefers-reduced-motion` 대응 포함. 좁은 화면 전용 `@media` 는 두지 않았다
  (`check-mobile.mjs` 가 "폰 블록 뒤 좁은폭 규칙 금지"를 강제 — grid auto-fit로 대신 해결).
- 헤드리스 Chromium(CDP)으로 실제 렌더를 두 화면 다 확인했다: 라이브 뷰(연결 오류 상태 —
  백엔드 없는 오프라인 시험 환경이라 의도된 것)와 구조 뷰(제목·부제 전환, 코너 브래킷)
  모두 정상.

### V33.313 추가분 — 작동 화면 · 구조 관측을 한 창으로

- **왜 옛 모습이 남아 보였나**: V33.312 는 `.nlv-*`(작동 화면)만 새로 그렸다. 구조 관측
  (`#nnvStruct`)은 `--ap-tint`(파랑 #4a9eff) + 유리질감 블러 — "Apple 재질" 토큰 체계로
  그려져 있었다. 종전엔 BVIEW 로 둘을 전환해서 한쪽이 항상 안 보였으니 몰랐는데, 합치고
  보니 한 화면 안에 ★서로 다른 브랜드의 UI 둘★ 이 붙어 있는 게 드러났다.
- **합침**: `BVIEW` 전환을 없앴다. `#nnvLive`·`#nnvStruct` 를 늘 함께 보인다.
  `setBrainView()` 는 이제 인자 없이 부팅 1회만 불려 작동·구조 데이터를 같이 로드한다.
  `openNnViz()` 를 "구조 관측 모드일 때만" 통과시키던 게이트(`__luxViewGated` 래퍼)를
  없애고, 대신 헤더 새로고침이 둘 다 새로고침하도록 바꿨다(`__luxRefreshesBoth`).
  사이드바·모바일 바의 "작동 화면/구조 관측" 전환 버튼과 사이드바의 "지금 보는 모델"
  중복 목록(`railModelsSec`)을 없앴다 — 모델 선택은 화면 안 신경망 지도(`#nnvTabs`)
  하나로, 늘 보이니 전환할 이유가 없다.
- **재테마 — 규칙을 다시 쓰지 않고 토큰 값만 바꿨다**: 구조 관측 쪽 수백 개 규칙이 이미
  읽고 있던 공용 토큰(`--ap-tint`·`--ap-mat-*`·`--ap-hair`·`--ap-ink` 등)의 값을
  미션컨트롤 팔레트로 재정의했다 — 렌더러 로직은 한 줄도 안 건드리고 색만 바뀐다.
  사이드바(`.rail`)는 `#page-nnviz` 의 형제 요소라 그 토큰을 못 읽으므로, `.rail` 이
  실제로 쓰는 전역 토큰(`--s1`·`--s2`·`--bd`·`--t1~3`·`--acc`)을 `body:has(#page-nnviz.active)
  .rail{...}` 스코프 안에서만 다시 정의했다 — 대시보드 등 다른 화면은 전혀 안 바뀐다.
- **오른쪽 사이드바**: 완전히 없애지 않았다 — 열린 종목창·ALT ASSETS·BUDGET ALLOCATION 은
  대시보드에서 옮겨온 실데이터라, 지우면 그 화면 두 곳에서 조용히 사라진다. 대신 뜻을
  잃은 부분(뷰 전환 버튼·모델 중복 목록)만 걷어내고 나머지는 같은 팔레트로 재테마했다 —
  사용자가 "필요하면"(조건부)이라 했고, 데이터 손실 없이 "완전히 새롭게"를 더 안전하게
  만족하는 쪽을 골랐다.
- `tools/check-html-js.mjs` 의 V33.153 검사("모델 목록은 구조 관측 뷰에서만 보인다")는
  전제 자체가 사라져 업데이트했다 — 이제 `railModelsSec`·`#railViews` 가 ★없어야★ 통과한다.
- 헤드리스 Chromium(CDP)으로 데스크톱·모바일 두 폭에서 실제 렌더를 확인했다 — 구조 관측
  탭이 작동 화면과 같은 시안 톤으로 통일됐고, 사이드바도 같은 팔레트를 쓴다.

### V33.312 추가분 — AI 작동 관제실 전면 재설계

- 사용자 지시: "지금 화면 완전히 폐기하고 spacex 스타일로 전체 디자인 다시해." V33.311 은
  헤더·텔레메트리·NOW 브리핑만 새로 얹고 기존 `.nlv-*` 배색을 물려받아, 사용자 눈엔
  "장식만 추가된 옛 화면"이었다.
- ★안전 경계★ 실거래 화면이라 DOM 은 지우지 않았다 — id·class 는 그대로 두어
  렌더 함수(renderCore·renderFunnel·renderCommittee·renderKpi·renderPicksGraph 등)는
  손대지 않았다. "폐기"는 캐스케이드 마지막 위치(`</body>` 직전 `<style>`)에서 이 화면에
  걸리는 시각 규칙 전부를 다시 정의해, 위쪽 12000~13900행대의 옛 규칙이 눈에 보이는 효과를
  하나도 못 내게 만드는 방식으로 했다.
- 새로 그린 것: 배경(성근 별 + 공학 격자 + 수평선 발광, 이미지 없이 CSS 만), 헤더·텔레메트리·
  브리핑(코너 브래킷 카드), 히어로(최우선 후보·판정 깔때기 — 전 패널 4모서리 브래킷),
  위원회 구성(승무원 상태 타일 그리드로 재편), 엔진 판정 로그(터미널 스타일), 엔진 파이프라인
  (LED 시스템 체크리스트), AI 후보 종목 그리드. 색·글꼴 토큰(`--ops-*`)을 `#page-nnviz` 에
  선언해 전부 한 곳에서 통제한다.
- `tools/check-html-js.mjs` 가 실측으로 잡은 것: 위원회 카드에 리터럴 hex 색 1건
  (`.nlv-cm.prov .cs{color:#7fc4f0}`) — "재질 토큰으로만 칠해야 라이트 테마가 따로 안
  필요하다"는 기존 계약을 어겼다. 토큰(`--ops-cyan-soft`)으로 바꾸고, 라이트 전용
  `.nlv-cm` 덧칠도 제거해 토큰 재정의만으로 따라오게 고쳤다.
- 헤드리스 Chromium(CDP)으로 다크·라이트·구조 관측 세 상태를 실제로 렌더해 확인했다.
  라이트 테마에서 스크린샷이 계속 다크로 나오는 문제를 만났는데, `getComputedStyle` 로는
  매번 올바른 값(`rgba(255,255,255,.88)` 등)이 잡혀 코드가 아니라 ★헤드리스 SwiftShader
  소프트웨어 합성의 리페인트 무효화 버그★ 로 판단했다 — DOM 에 진단 배지 하나를 추가해
  강제로 리플로우를 일으키자 그 즉시 올바른 라이트 화면이 그려졌다(실사용 브라우저는
  속성 변경만으로 정상 리페인트한다). 원인이 우리 CSS가 아니라 테스트 도구 쪽임을 확인한
  뒤 방어적으로 넣었던 `isolation:isolate` 만 제거했다(용도가 불분명했던 선언이라 제거 자체가
  안전하다).

## Completed

- V33.301 위원 명부(buildRoster) 단일출처 — 사이드바·구조 관측·신경망 지도가 같은 배열을 읽는다.
- V33.302 tools/probe-pick.py — 워커 응답에서 키 이름으로 필드를 뽑는 진단 도구.
- V33.303 MEMO 홀드아웃을 달력(60일 = minBlocks 5 × 라벨지평 10일 + 여유)에 고정, 학습은
  엠바고 이전만. 전진표본 0의 이유(고를 행 없음/배치하한/라벨/NaN)를 구분해 기록.
- V33.304 평가 루프 FLOW 비용 — 피어 로그수익 사이클 캐시(WeakMap) + flowpos/flowopt를
  사이클당 질의 1회로 프리로드(D1 왕복 248회 → 1회). 값은 옛 구현과 비트 단위로 동일.
- V33.306 MEMO 외부학습 — trainer/modal/modal_train.py 에 `_memo_fit`/`_memo_score`/
  `_train_and_upload_memo` 추가, 워커에 `POST /api/memo-import` 추가.
  · 승격 조건: 판(luxFeatVer) 일치 + 형상 검사 + ★정합 probe★(트레이너 확률을 워커
    memoScore 가 재현) + 신뢰 판정은 워커 expertAdmit 이 한다.
  · 유효표본수는 `_importedValN`(고유도 보정)으로 받는다 — 외부 모델만 관대한 자로
    심사받지 않게 한다.
  · `memoTrainNightly` 는 그대로 남아 있고, 외부 모델이 30시간 이내로 신선할 때만 적합을
    건너뛴다. ★전진검증은 건너뛰기보다 앞에서 항상 돈다.★

### V33.310 추가분

- ★뒤진 바닥 사고.★ 다른 도구(Codex)가 `codex/find-issues-with-embedded-ai-models`
  (V33.305, V33.304 에서 분기) 위에서 V33.309 를 만들었다. 그 바닥엔 V33.306·307·308 이
  통째로 없다 — 게이트가 80종이고 지금 main 은 82종이다.
- ★왜 위험한가:★ 게이트는 deploy.yml 에 손으로 배선된다. 뒤진 deploy.yml 이 얹히면
  새 게이트가 목록에서 사라지는데, 그 사라짐을 알아챌 사람이 바로 사라진 그 게이트다.
  파일은 저장소에 남아 있어 `ls tools/` 로도 티가 안 난다.
- `tools/check-stale-base.mjs` — ① `tools/check-*.mjs` 전부가 deploy.yml 에 배선돼 있는가
  (반대로 없는 파일을 부르지 않는가도 함께 본다) ② `_BUILD_VER` 가 직전 커밋보다
  뒤로 가지 않았는가. 워크플로 체크아웃을 `fetch-depth: 2` 로 올려 ②가 CI 에서 실제로 돈다.
- CLAUDE.md 에 "작업 시작 전 최신 `main` 을 바닥으로 삼는다" 를 도구 공통 규칙으로 적었다.

#### 아직 사용자 판단이 필요한 것

- `AI_PARAMS.requireTrustedModel` — main 은 `false`(2026-07-22 사용자 지시로 완화),
  V33.305 브랜치는 `true`. 매수 여부를 가르는 운용 정책이라 임의로 바꾸지 않았다.
- V33.305 의 `AGENTS.md` · `tools/check-ai-handoff.mjs` 는 아직 main 에 없다.
  가져올 때 그 안의 CLAUDE.md 문구("main 에서 직접 편집하지 않는다")는
  2026-09-07 지시("항상 main 에 작업")와 충돌하므로 그대로 옮기면 안 된다.

### V33.308 추가분

- ★두뇌 관측에서 탭을 옮기면 다른 모델 그림이 나왔다(주로 DNN).★ 원인이 셋이었다:
  · openNnViz 를 부르는 자리가 다섯(탭 클릭 · 화면 전환 · showPage · resize · 새로고침)인데
    ★도착 순서대로 무조건 그렸다★. 제일 무거운 DNN 응답(21MB 청크)이 뒤늦게 와서 덮었다.
  · `NNV_render` 분기가 kind 를 보다가 아무 데도 안 걸리면 ★말없이 DNN 렌더러로 떨어졌다★.
    `mlDNNVizData` 만 kind 를 안 달고 있어서, 늦은 응답·옛 캐시·모르는 모델키 셋이 전부
    "DNN" 이라는 한 증상으로 나왔다.
  · `NNV.curKey` 를 응답이 온 뒤에 바꿔서, 캐시로 먼저 그린 첫 화면이 ★이전 모델의 합류
    상태★ 를 이름표로 달았다(V33.306 이후).
- 고침: 응답에 `reqModel`·`kind`(DNN 포함 전 경로) 를 새기고, 화면은 보낸 순서(`NNV.seq`)를
  세어 최신 요청의 응답만 그린다. 모델이 어긋나면 `NNV_render` 가 `false` 를 돌려주고
  아무것도 안 그린다 — 캐시에도 안 남긴다. `curKey` 는 요청을 보내는 순간 맞춘다.
- `tools/check-nnviz-switch.mjs` — 가짜 DOM·가짜 fetch 로 그 경합을 ★실제로 재현해★ 본다
  (느린 DNN → 빠른 MEMO). 변이(순서 표 제거 / kind 확인 제거)를 넣으면 실패한다.
  탭 목록 14개 키가 전부 서버 분기에 있는지도 본다 — 모르는 키는 기본값 DNN 으로 샌다.

### V33.307 추가분

- 야간 파이프라인 `_stg` 가 단계별 소요시간을 잰다. 느린 단계(≥500ms)만 `ai_stage_cost` 에
  ★오늘 기록과 합쳐★ 즉시 쓴다 — 파이프라인이 중간에 죽어도 남는다(그게 제일 보고 싶은
  경우다). 완주 시 `[STAGE-COST]` 한 줄, `/api/ai-mode` 의 `alt.stageCost` 로도 읽는다.
  → 다음 Modal 이관 대상은 이 표를 하룻밤 받아 보고 고른다. 추측으로 고르지 않는다.
- `deploy.yml` 이 `pull_request` 에서도 검사 80종을 돌린다. ★배포 성격 단계 5개는
  `github.event_name == 'push'` 로 막았다★ — PR 이 프로덕션에 배포하면 안 된다.
  주의: 이미 `if` 를 갖고 있던 단계(`Enable R2 binding`, `R2 status summary`)는 조건을
  ★합쳐서★ 걸어야 한다. 처음에 `if` 를 한 줄 더 붙였다가 YAML 중복 키로 push 가드가
  조용히 죽었고(뒤엣것이 이긴다), `tools/check-stage-cost.mjs` ④가 그것을 잡았다.

## Remaining work

- FLOW·XALPHA·STACK·DUAL 은 아직 워커 학습이다. ★V33.307 의 `alt.stageCost` 를 하룻밤
  받아 보고 대상을 고른다.★ 현재까지 기록으로 확인된 것:
  · DUAL — DUALHEAD 주석에 "요청당 128MB 를 넘겨 워커가 죽었다 · train-now dual 이 HTTP 503
    4회 연속" 이 남아 있다. 그래서 창을 60,000 → 20,000 으로 줄였다. 밖으로 옮기면 그 제약이
    풀린다. ★단, `DUALHEAD.nonlinear: true` 라 비선형 헤드 경합(lin/gbdt/mlp/blend)까지
    옮겨야 성능이 안 깎인다★ — 선형만 옮기면 그건 성능 저하다. 추측으로 하지 않는다.
  · STACK·FLOW·XALPHA — 각자 자기 표(stack/flow/xalpha_samples)로 학습하는데 Modal 은 지금
    `ml_samples` 만 내려받는다(`/api/ml-export`). 옮기려면 export 경로부터 필요하다.
- 외부(Modal) 업로드 모델(SEQ·DNN·GBDT·부스터)은 V33.300 의 "홀드아웃 기간이 모자라면
  못 쟀다" 가드를 거치지 않는다. 트레이너가 홀드아웃 달력기간을 함께 올려야 닫힌다.
- FLOW·XALPHA 는 표본 표 자체가 21일치라 판정 불가 상태다(코드 문제가 아니라 시간 문제).

## Validation

- 새 담당자는 아래 명령을 다시 실행한다.
  - `node tools/check-model-evidence.mjs`
  - `node tools/check-syntax.mjs src/index.js`
  - `node tools/check-memo-modal.mjs`   (두 언어 정합 — 파이썬을 실제로 돌린다)
  - `node tools/check-stage-cost.mjs`   (PR 이 배포하지 않는지 — YAML 을 실제로 파싱해 본다)
  - `node tools/check-flow-peer-cpu.mjs` (옛 구현과 값 대조)
  - `node tools/check-holdout-anchor.mjs`
  - `for f in tools/check-*.mjs; do node "$f" >/dev/null || echo "FAIL $f"; done`
- 이번 세션 실행 결과: 위 전부 통과(게이트 81종), `node --check src/index.js` 통과,
  `python3 -c compile(modal_train.py)` 통과.
- 실측 대조값: MEMO 정합 probe 40건 최대 차 1.11e-16 (허용 1e-6).

## Recovery notes

- `/api/memo-import` 는 probe 가 없거나 오차가 `MEMOML.probeMaxDiff`(1e-6)를 넘으면 저장하지
  않는다. Modal 쪽 식을 고치면 반드시 probe 오차를 다시 확인한다.
- 외부 MEMO 를 끄려면 `MEMOML.externalMaxAgeH` 를 0 으로 두면 된다 — 워커가 종전대로 학습한다.
- V33.303 이후 MEMO 원형책은 홀드아웃(60일)+엠바고(10일) 이전 구간에서 만들어진다.
  즉 배포되는 원형책이 그만큼 과거다. 잰 것과 배포한 것을 같게 하려는 의도적 선택이고,
  신선도까지 되찾으려면 측정용·배포용 두 번 적합해야 한다(예산 재설계 필요).
- 비밀키·토큰·Cloudflare secret 은 이 문서에 기록하지 않는다.

## Current handoff — V33.328 (구조·설계 감사: 게이트가 헛도는 경로와 규칙 사본 제거)

사용자 요청: "다른 문제도 전부 해결해 찾아서 설계상의 문제점도 전부 찾아 구조적인 문제랑".
증상 하나를 더 고치는 대신, ★이번 세션에 두 번 반복된 실패 부류★ 를 구조로 막았다.

### ① 게이트가 조용히 헛도는 경로 (tools/check-gate-anchors.mjs 신설)
같은 날 두 번 났다.
- `check-holdout-anchor` — 소스에 같은 문구가 두 곳이 되자 `String.replace` 가 첫 곳만 지웠다.
  돌연변이 시험이 아무것도 시험하지 않은 채 ★통과★ 했다.
- `check-eval-cost` — 꺼내 돌리던 함수가 `_num` 을 쓰기 시작하자 하네스가 ReferenceError 로
  죽고 문자열 대조로 조용히 주저앉았다.

대다수 게이트는 `indexOf` 앵커로 소스를 잘라 본다. 앵커가 리팩터링으로 사라지면
`indexOf` 가 -1 → `slice(-1, …)` 이 거의 빈 문자열 → ★부정 단언이 전부 참★ 이 된다.
초록불인데 아무것도 안 지킨다. 그 경로를 메타검사로 먼저 깬다.

- 대상: 게이트 44개 · 자르기 앵커 192개(직접 `indexOf` + `slice(a,b)` 도우미 경유 모두).
- 존재 여부를 묻는 형태(`indexOf(x) < 0` / `=== -1`)는 제외한다 — "없어야 한다" 는 정상 쓰임이다.
- 도우미 판정은 좁게: 본문이 ①제 매개변수를 그대로 `indexOf` 에 넣고 ②`slice/substring` 을 할 때만.
  느슨하게 잡았더니 `check(cond,'통과문구','실패문구')` 의 ★메시지★ 86건을 앵커로 착각했다.
- 이 메타검사 자신도 썩을 수 있어 ★자가시험★ 을 넣었다(가짜 게이트 원문으로 잡을 것/안 잡을 것 확인).
- 검증: `_phaseRun` 앵커를 지우자 메타검사가 정확히 그 게이트를 지목하고 실패했다. 복원 후 통과.

감사 중 나온 오탐 2건은 ★메타검사 쪽이 틀린 것★ 이었고 고쳤다:
`new URL('…')` 홑따옴표 미인식, 한글 앵커(`/[A-Za-z]/` 요구) 누락.

### ② 같은 규칙의 사본이 둘 (tools/check-single-source.mjs 신설)
"★또★ 신규 위원들 작동 안 한다" 의 뿌리는 V33.303 이 달력 홀드아웃을 MEMO 한 곳만 고친 것이었다.
같은 함정을 소스 전체에서 찾았다(10줄 이상 동일 블록 탐지).

- **SEC Form 4 파서** — 야간 갱신(`updateInsiderFeedNow`)과 `/api/insider` 의 `__buildInsider` 가
  같은 정규식·같은 묶음 규칙을 각자 한 벌씩 갖고 ★같은 키 `insider_feed`★ 에 썼다.
  그 키는 화면용이 아니라 PEAD·사이징(`sig.insiderNote`)과 AI 피처(`insiderBuy`)가 읽는 거래 입력이다.
  SEC 제목 형식이 바뀌면 고친 쪽이 채운 값을 못 고친 쪽이 곧바로 덮어써, 원인이 안 보이는 채로 피처가 0 이 된다.
  → `_secForm4Parse(xml, rev)` 한 곳으로 합쳤다.
  **동작 동일 확인**: 옛 파서를 git HEAD 에서 꺼내 새 파서와 5개 입력(묶음·4/A 정정·엔티티 디코드·
  빈 응답·55건→40 상한)에서 돌려 ★출력이 바이트까지 같음★ 을 확인했다.
- **시세 병합** — `saveQuoteCM` 과 `saveQuoteAlt` 가 필드 19개까지 똑같은 사본이었다
  (주석에도 "saveQuoteCM 일반화" 라고 적혀 있었다). 새 일봉지표를 한쪽에만 추가하면
  다른 슬리브만 그 지표를 영영 못 받는다. → `saveQuoteCM` 은 `saveQuoteAlt` 로 넘기게 했다.
  CM 호출자는 `q` 에 `symbol` 을 안 싣기에 여기서 채워 넘긴다(저장 키 종전과 동일).
  **동작 동일 확인**: 주석 제거 후 병합 규칙이 `market` 키 말고는 완전히 일치.

### ③ 게이트가 판 올릴 때마다 무관하게 깨지던 것 (check-ai-ops-ui.mjs)
`?v=33.327` 을 리터럴로 박아 둬서 판을 올릴 때마다 이 게이트가 깨졌다.
그렇게 깨지는 검사는 "게이트를 숫자만 고쳐 통과시키는" 습관을 만들고, 그 습관이 진짜 계약까지 무디게 만든다.
지켜야 할 것은 특정 숫자가 아니라 ★빌드 판과 캐시무효화가 함께 움직인다★ 는 것이라, `lux-build` 에서 읽게 했다.

### 검증
- `node --check src/index.js` 통과.
- 게이트 91개 전부 통과(신설 2개 포함, 실패 0).
- 새 게이트 2개를 `deploy.yml` 에 배선(`check-stale-base.mjs` 가 배선 누락을 잡는다).

### 일부러 손대지 않은 것 — ★사용자 판단이 필요하다★
**XALPHA 의 전진 IC 가 음수(-0.039)인데 조기 기각에 안 걸린다.** `fwdReady=false` 라
"아직 판단 못 함" 으로 분류돼, V33.326 이후엔 ×0.60 가중으로 ★방향이 반대인 신호★ 가
위원회에 들어올 수 있다. 이건 문턱 설계의 판단이고 실제 돈이 걸린 가중치라 임의로 안 바꿨다.
선택지: (ㄱ) `fwdReady` 와 무관하게 전진 IC 가 음수면 기각, (ㄴ) 표본이 찰 때까지 가중 0 으로 보류,
(ㄷ) 지금처럼 둔다. 지시를 주면 그대로 반영한다.

### V33.328 추가 — 표본 표 4종의 '구 판 정리' 누락
`ml_samples` 에만 구 featVer 정리가 있었다. `flow_samples`·`xalpha_samples`·`stack_samples`·
`ml_samples_st` 는 판이 올라가도 옛 행이 영영 남았다. 모든 조회가 `WHERE featver = 현재판` 이라
★학습에는 안 읽히지만★, 그 죽은 행이 D1 과 (featver, ts) 인덱스를 채워 새 표본이 자랄 자리를 잠식한다.
야간 수확 끝에 표당 5만행×2배치 정리를 추가했다 — 읽히지 않는 행만 지우므로 모델 동작은 안 바뀐다.
게이트는 ★표 목록에서★ 확인한다(새 표본 표를 만들며 정리를 빠뜨리면 걸린다).
`_altPrune` 에서 stack_samples 를 빼 보고 게이트가 지목하는지 확인했다.

### 확인했으나 손대지 않은 것(문제가 아니었다)
- `/api/reset` 이 `deposits` 를 `{us:0,kr:0}` 로만 심어 `cm` 이 빠지지만, 읽는 쪽이
  `typeof deposits[market] === "number" ? … : 0` 이라 리셋 직후 정답(0)과 같다. `twr:cm` 도
  `applyCashflowToTWR` 이 없으면 스스로 초기화한다. 증상이 없어 그대로 뒀다.
- 매도 원장 원자성은 V33.129 에서 이미 해결돼 있었다(`stmtRecordTradeIfPos` + 배치, 게이트 있음).
  매도 3경로 모두 이 경로를 쓴다 — 다시 손대지 않았다.

### V33.328 추가 — 슬리브별 초기자본 표가 세 벌이었고 ★둘이 틀렸다★
같은 표를 세 곳이 각자 갖고 있었다.
- `computeCashFromTrades` — 5종 전부 맞음(유일하게 옳았다).
- `applyCashflowToTWR` — `us/kr/cm` 삼항이라 **bdus·bdkr 이 cm($100,000)으로 떨어졌다**.
  bdkr 은 원화 ₩100,000,000 이다 — 통화가 다른데 1,000배 어긋난 값으로 TWR 이 시작된다.
- `auditAccounting` — 숫자를 통째로 박아(100000 / 100000000) `cfg` 를 아예 안 봤다.
  설정에서 초기자본을 바꾸면 `ASSET_INFLATE` 문턱이 같이 안 움직인다. 문턱을 넘으면
  자가치유가 현금 체크포인트를 지워 원장 전체를 다시 합산한다 — 매 사이클 그러면 CPU 를 태운다.

**지금은 둘 다 us/kr 로만 불려 증상이 없다.** 그래서 더 합쳐 둘 값어치가 있다 —
채권 슬리브가 그 경로에 들어오는 날 조용히 틀린 값이 나온다. `_initialCashFor(cfg, market)`
한 곳으로 합치고, `auditAccounting` 에 `cfg` 를 넘겼다(안 넘어오면 종전 값으로 폴백 — 동작 불변).
게이트는 함수를 실제로 돌려 슬리브 5종이 각자 제 금액을 받는지 확인하고,
TWR 초기화가 다시 삼항으로 돌아가면 실패한다.

## Current handoff — V33.330 (프리·애프터마켓 실측 점검)

사용자 요청: "주식들 등락율 정규장 끝나고 에프터마켓이랑 프리마켓이랑 제대로 작동하고 있는지 살펴봐라".

점검 시각이 마침 ★두 시간외가 동시에 열린 창★ 이었다 — 2026-09-09 19:45 ET(미국 애프터마켓)
= 2026-09-10 08:45 KST(한국 프리마켓). 프로덕션 `/api/state`·`/api/watchlist` 를 떠서 실측했다.

### 한국 프리마켓 — 정상
`mstate=PRE`, `prePct` 가 실제 체결이 있는 종목에 채워지고 `dayPct` 가 프리 값으로 덮인다.
`regPct=0`(정규장 미개장)도 맞다. 예: OCI −0.87 · NHN −1.21 · 넥스틴 +1.90 · 동화기업 −1.38.
체결이 없는 종목은 `prePct=None`·`dayPct=0` — 지어내지 않는다. 네이버 경로는 제대로 돈다.

### 미국 애프터마켓 — ★값이 안 들어온다★
같은 시각 미국 종목은 이랬다(quote ts 19:46 ET — 갱신은 2분 전, 즉 죽은 게 아니다):
```
GOOGL  mstate "POST"   post null   postPct null
       dispPct = regPct = -2.2786   ← 정규장 종가 등락 그대로
GOOG   mstate "POST"   post null   postPct null
```
세션 판정(POST)은 맞는데 가격이 없다. 표시 계층은 정상 동작해 정규장 값으로 떨어지는데,
그 결과 ★시간외에 크게 움직여도 "아무 일 없었다" 로 읽힌다★. 틀린 수치가 아니라 틀린 인상이라
눈으로 못 잡는다. 한국이 같은 시각 정상인 것이 대조군이다 — 표시가 아니라 미국 수집 경로 문제다.

**어느 경로가 값을 넣는지**: 미국 시간외는 v7 quote 의 `preMarket*`/`postMarket*` 에서만 온다.
v8 폴백(`fetchQuoteViaChart`)은 `meta.regularMarketPrice`(장 마감 후엔 정규장 종가)와
일봉 마지막 종가를 비교하는데 장후엔 두 값이 같아 ★구조적으로 시간외를 만들 수 없다★.
그런데 지금 mstate 가 "POST" 로 갱신돼 있다 = v7 이 응답은 하고 있다는 뜻이다
(v8 경로였다면 장중에 찍힌 "REGULAR" 가 그대로 남아 있어야 한다).
즉 **야후 v7 이 marketState 는 주면서 postMarketPrice 를 안 준다.**

### 고친 것
1. **`saveQuote` 가 시간외 5필드를 통째로 날렸다** — `setState` 로 quote 를 전부 덮어쓰는데
   필드 목록에 `mstate/pre/prePct/post/postPct` 가 없었다. 가격 샤드가 COALESCE 로 지켜 온
   값이 이 경로 한 번에 사라진다. 지금은 수동 `POST /api/refresh_quotes` 에서만 불려 증상이
   드물지만, 한 번 불리면 그 시장 전 종목의 시간외 표시가 다음 샤드 갱신까지 빈다.
   → 새 값이 있으면 쓰고 없으면 기존값을 이어받게 했다.
2. **`refreshQuotesOnly` 가 계산해 둔 값을 버렸다** — `fetchQuoteViaChart` 는 미국 시간외를
   이미 계산해 돌려주는데, 호출부가 손으로 필드를 골라 담느라 그게 빠졌다. → 그대로 넘긴다.
3. **감지기 신설(핵심)** — 이걸 오래 못 잡은 진짜 이유는 감지기가 없어서다.
   `v7Dead` 는 계산만 하고 ★한 번도 안 읽는다★ — 미국 수집원이 죽어도 아무 신호가 없다.
   자가진단에 "세션은 PRE/POST 라는데 그 값이 비었다" 를 넣었다(미국 20종목 이상이 같은
   세션인데 값 있는 종목이 10% 미만일 때만 warn — 표본이 적으면 우연으로 보고 넘긴다).

### 게이트
`tools/check-overmarket.mjs` 신설(deploy.yml 배선). 문자열 대조가 아니라 함수를 꺼내 돌린다:
표시 규칙 4종(프리/애프터/값없는POST/정규장) · 저장 경로 보존과 갱신 · 감지기 판정식 ·
한국 시간외 창과 네이버 하락부호(4·5). 되돌림 두 가지(보존 삭제·감지기 무력화)를 실제로
넣어 게이트가 지목하는 것을 확인한 뒤 원복했다. 게이트 93개 전부 통과.

### ★사용자 판단이 필요하다★ — 미국 시간외 가격을 실제로 되찾는 방법
지금 고친 것은 "값이 비면 지어내지 않고, 비었다는 사실을 소리 낸다" 까지다.
값 자체를 되찾으려면 수집원을 손대야 하고 그건 subrequest 예산이 든다:
- (ㄱ) v7 호출에 `fields=` 를 명시해 preMarket*/postMarket* 를 직접 요구한다 — 추가 호출 0.
  야후가 기본 필드셋을 줄인 것이라면 이걸로 해결된다. 가장 싸다.
- (ㄴ) 시간외에만 `interval=1m&range=1d&includePrePost=true` 차트를 보유·관심 종목에 한해
  돌린다 — 확실하지만 종목당 1 subrequest.
- (ㄷ) 그대로 두고 화면에 "시간외 시세 없음" 을 명시한다.
(ㄱ)을 먼저 시도하고 안 되면 (ㄴ)을 보유종목으로 좁히는 것을 권한다. 지시를 주면 그대로 한다.

## Current handoff — V33.331 (시간외 시세 복구 + 서머타임 자동화 + 시간외 거래)

사용자 지시: "실시간으로 에프터랑 프리 가격 들어오게 하고, 서머타임 적용 같은거도 자동으로
고려하게 하고, 거래도 정규장 뿐만 아니라 에프터랑 프리에서도 하게 만들어".

### ① 시간외 가격 — 두 겹으로 복구
V33.330 실측에서 미국은 `mstate=POST` 인데 `post` 가 전부 null 이었다. 원인은 두 가지였고 둘 다 고쳤다.
- **v7 에 안 물어봤다.** `fields=` 없이 호출하면 야후는 기본 필드셋만 주고, 그 기본셋이 계속
  좁아져 왔다. `preMarketPrice`·`postMarketPrice` 등을 ★명시적으로★ 요구한다.
  `fields=` 자체를 거부하는 환경을 대비해, 첫 배치가 0건이면 필드 없이 한 번 더 본 뒤에야
  v7 사망으로 판정한다(있는 길을 스스로 닫지 않는다).
- **v8 폴백은 구조적으로 시간외를 못 만든다.** 일봉엔 시간외 체결이 안 들어가고
  `meta.regularMarketPrice` 는 마감 후 정규장 종가라 "차이 없음"이 된다.
  → `fetchExtendedQuoteUS()` 신설: `interval=5m&range=1d&includePrePost=true` 분봉에서
  `currentTradingPeriod` 의 pre/post 구간에 속한 마지막 체결을 직접 뽑는다.
  프리는 전일종가 대비, 애프터는 정규장 종가 대비로 계산한다(야후 표기와 같은 규칙).
  종목당 1 subrequest 라 ①미국 시간외 창일 때만 ②값이 실제로 빈 종목만
  ③보유종목 우선·나머지는 회전하며 상한(기본 24)까지만 돌린다.

**관측**: `yahoo_v7` 상태를 기록하고 자가진단이 읽는다. 종전엔 `v7Dead` 를 계산만 하고
한 번도 안 읽어 1차 수집원이 죽어도 아무 신호가 없었다 — 이번 결측을 오래 못 본 이유다.
v7 사망은 곧 시간외 전면 결측이라 `error` 로 올린다.

### ② 서머타임 — 손계산에서 표준시 DB로
`getUSEtOffset` 은 "2007년 이후 미국 규칙(3월 2째 일요일~11월 1째 일요일)"을 코드로 옮긴
손계산이었다. 규칙이 바뀌면 조용히 틀린다(미국은 상시 서머타임 법안이 반복 발의된다).
→ `Intl.DateTimeFormat` 의 IANA 표준시 데이터(`America/New_York`)에 먼저 묻고,
실패할 때만 기존 손계산으로 내려간다. 포매터와 분 단위 결과를 캐시한다.
게이트가 2026~2028 봄·가을 전환 경계 9건을 실측 확인한다.

### ③ 시간외 거래 — fastWatch 가 집행한다
메인 사이클의 무거운 평가(일봉 라운드로빈·전종목 위원회)는 시간외에 돌리지 않는다.
대신 이미 보유종목만 도는 경량 경로(`runFastWatch`)를 시간외 집행기로 쓴다.
- **청산**: 시간외 체결가로 판단한다. 값이 없거나 가드에 걸리면 그 종목은 건너뛴다.
  ★정규장 가격으로 떨어지지 않는다★ — 그건 "16시 값으로 20시에 손절"이라 규칙이 거짓말을 한다.
- **진입**: 그날 위원회가 `minPickP`(0.62) 이상으로 본 종목에서만. 시간외에 새 판단을
  내리지 않는다. 크기는 `sizeMult`(0.5)배, 세션당 `maxNewPerSession`(2)종목까지.
- **가드**(`extTradePrice`): 값 없음 / 7분 초과 노후 / 변동 12% 초과 / 3달러 미만 → 거래 안 함.
- **정규장 상한을 그대로 지킨다**: 이 경로는 메인 루프 밖이라 동시보유(`maxConcurrent`)·현금·
  중복매수를 직접 센다. 보유 수를 못 세면 사지 않는다(모르면 쉰다).
- `fastWatch` 가 시간외 세션을 스스로 판정한다 — 메인 사이클이 한 번 걸러져
  `fastwatch:markets` 가 낡아도 시간외 감시가 쉬지 않는다(사고가 나는 시간대다).
- **되돌리기**: `cfg.extTrade.enabled=false` 로 통째로, `entries=false` 로 청산만,
  `us.post=false` 처럼 세션별로도 끌 수 있다. 게이트가 이 스위치들이 실제로 먹는지 확인한다.

### 게이트
`tools/check-ext-trading.mjs` 신설(deploy.yml 배선) — 함수를 꺼내 돌린다.
DST 경계 9건 · 세션 판정 9종 · 가격 가드 7종 · 분봉 추출 2종 · v7 필드 3종 · 집행 12종.
되돌림 세 가지(가격이 정규장으로 떨어짐 · 표준시 DB 조회 삭제 · 청산이 정규장 가격 사용)를
실제로 넣어 게이트가 지목하는 것을 확인한 뒤 원복했다. 게이트 94개 전부 통과.
기존 `check-order` 가 "yahoo_v7 을 쓰기만 하고 안 읽는다"를 잡아 줘서 자가진단 연결을 마쳤다.

### 배포 후 확인할 것
미국 애프터(16:00~20:00 ET)나 프리(07:00~09:30 ET)에 `/api/watchlist` 를 떠서
`post`/`postPct` 가 실제로 채워지는지 본다. 여전히 비면 v7 이 필드를 끝내 안 주는 것이므로
분봉 폴백이 도는지(`yahoo_v7.dead`, 자가진단 "시간외" 경고)로 갈라 본다.

## Current handoff — V33.332 (DMA·스토캐스틱 추가 + 시간외 전 종목 거래)

사용자 지시: "DMA 지표랑 스토캐스틱 스무딩 지표도 추가해라 / 에프터랑 프리 제대로 들고 오고
있는지 확인도 해 / 시간외 거래 전 종목에서 할 수 있게 만들어 정규장이랑 똑같이".

### ① 지표 두 개 추가
- **`getDMA(closes, 10, 50, 10)`** — 국내 HTS 표준형 **이동평균 차이**.
  DMA선 = MA(단기) − MA(장기), AMA선(시그널) = DMA선의 이동평균. 가격으로 나눠 %정규화한다
  (안 하면 30만원짜리와 3달러짜리를 같은 잣대로 못 본다 — 위원회는 종목을 섞어 본다).
  ※ 'DMA' 는 Displaced MA(이동평균을 N봉 밀어 놓은 것)를 뜻하기도 한다. 스토캐스틱과 같이
    쓰이는 지표라는 맥락으로 보아 **이동평균 차이** 로 구현했다. 다른 쪽을 의도했다면 알려 달라.
- **`getStochSlow(highs, lows, closes, 14, 3, 3)`** — 스토캐스틱 **슬로우(스무딩)**.
  Fast %K → 평활(kSmooth) = Slow %K → 평활(dSmooth) = Slow %D. 이 평활이 '스무딩'의 전부다.
  Fast 를 그대로 쓰면 하루 노이즈로 과매수/과매도가 번갈아 켜져 못 쓴다.
  고가·저가가 없으면 종가로 근사한다. 고가=저가(상하한·거래정지)면 0으로 안 나누고 중립 50.

**배선**: `taPredictDirection`(기술 컨센서스)에 넣었다. 가중치는 설정으로 뺐다
(`dmaW 0.4 · dmaCrossW 0.3 · stochW 0.25 · stochCrossW 0.45`).
**최대 0.45 로 기존 최대(MA기울기 0.9)보다 작게 잡았다** — 지표는 늘리되 판단의 무게중심은
옮기지 않는다. 스토캐스틱은 수준보다 교차에 큰 가중을 준다(추세장에선 80 이상에 계속 붙어
있으므로 단순 과매수를 크게 깎으면 상승 추세를 계속 거스른다).

**ML 피처 벡터에는 넣지 않았다.** 넣으면 `featVer` 가 올라가 학습표본이 전부 죽고 위원 전원이
처음부터 재학습한다(회복에 몇 주). 화면에는 `/api/ta-explain` 의 `extra` 로 따로 내려
TA 패널에 DMA·스토캐스틱 칸으로 붙는다. 피처 편입은 별도 판단이 필요하다.

### ② 프리·애프터 확인 — ★아직 확정 못 한다★
V33.331 배포가 **20:16 ET 에 끝났고 애프터마켓은 20:00 에 닫혔다.** 새 코드가 시간외에
한 번도 안 돌았다. 배포 직후 자가진단은 이렇게 말했다:
```
미국 시간외 시세가 안 들어온다 — 세션 표시 336종목 중 값이 있는 건 2종목뿐
```
0 → 2 로 늘긴 했지만 세션이 끝난 뒤의 잔상이라 근거가 못 된다.
**다음 미국 프리마켓(07:00~09:30 ET = 11:00~13:30 UTC)에 다시 떠서 확인해야 한다.**
확인 지점: `/api/watchlist` 의 `post`/`pre` 채움 비율, 자가진단 "시간외" 경고 소멸 여부,
`yahoo_v7.fields`(true 면 fields 지정이 먹은 것), `yahoo_v7.dead`.

### ③ 시간외 거래 — 전 종목으로 확대
종전엔 시간외에 메인 사이클이 `extOnly` 에서 통째로 빠져나가고, fastWatch 가 보는
"보유종목 + 그날 픽" 에만 거래가 걸렸다. 이제 **유니버스 전체를 정규장과 같은 경로로 평가·거래**한다.
- 시간외 세션이면 `extOnly` 에서 빠져나가지 않고 평가 루프를 그대로 돈다
  (위원회·게이트·히트·동시보유 전부 정규장과 동일하게 적용된다).
- **가격은 시간외 체결가**(`extTradePrice`). 값이 없거나 가드에 걸린 종목은 평가에서 뺀다 —
  시간외에 체결이 없는 종목이 대부분이라 이게 정상이고, 제외 개수를 prefetch 로그에 남긴다.
- `prevClose` 는 두 세션 모두 **전일 정규장 종가**를 쓴다 → `dayPct` 가 "어제 종가 대비 누적
  변동"이라는 평소 의미를 그대로 유지한다.
- **일봉 라운드로빈은 돌리지 않는다** — 시간외에 봉은 안 변한다. 회전 위치(`rr_idx`)도
  전진시키지 않는다(전진시키면 정규장 순회에 구멍이 난다). 캐시된 일봉으로 평가한다.
  이게 시간외 확대의 fetch 비용을 대부분 막아 준다.
- **fastWatch 의 픽 진입은 기본 off (`fastEntries: false`)** — 메인이 전 종목을 맡은 지금
  둘 다 켜면 같은 종목을 두 경로가 각각 사는 길이 된다(메인이 분 0에 사고, 같은 분의
  fastWatch 는 매수 전 포지션 스냅샷을 들고 또 산다). fastWatch 의 **청산**은 그대로 둔다 —
  분 단위보다 빠른 반응은 시간외에 더 필요하다.

**비용**: 시간외 거래 창은 미국 5.5h + 한국 5.5h/일이다. 평가가 그만큼 늘어난다.
일봉 fetch 를 뺐고 시간외 평가는 2분 주기(기존 `extOnly` 격분 스킵 유지)라 fetch 는 크게
안 늘지만 **CPU 는 늘어난다.** 사용량 가드(`isUsageShutdown`)가 최종 방어선이다.
며칠 `[EVAL-COST]`·사용량 지표를 보고 필요하면 시간외 평가 주기를 3~5분으로 늘리는 게 맞다.

### 게이트
- `tools/check-indicators.mjs` 신설 — 수식이 아니라 **성질**을 검사한다. 추세 방향과 부호,
  가격 1,000배에도 같은 %정규화, 골든/데드 교차를 실제 계열에서 잡아내는지, **슬로우가 정말
  평활되는지(fast 변동 29.5 → slow 22.7)**, 고가=저가에서 0으로 안 나누는지,
  그리고 **새 지표 가중이 기존 최대를 넘지 않는지**.
  ※ 처음엔 교차 검사 창을 e=100 부터 잡았는데 교차는 e=92 에 이미 있었다 — "신호가 없다"고
    잘못 읽을 뻔했다. 계산 가능한 전 구간(e=61~)을 훑도록 고쳤다.
- `check-ext-trading.mjs` 확장 — 메인 사이클의 시간외 전 종목 경로 5항목 추가.
- 게이트 95개 전부 통과.

## Current handoff — V33.334 (DMA·스토캐스틱 피처 통합 — ★표본을 버리지 않고★)

사용자 지시: "피처 통합하는데 이거 학습표본 안 죽게 기존에 있는 데이터로 dma 스토캐스틱
전부 계산해서 학습표본에 넣은다음에 학습시켜 모델들 안죽게 잘처리해봐".

### 무엇이 문제였나
이 저장소의 종전 방식은 **featVer 를 올리고 옛 표본을 버린 뒤 딥이력에서 재수확**이었다
(V33.239 65→69, V33.265 69→75 모두 그랬다). 되긴 하지만 **며칠 동안 위원 전원이 굶는다** —
표본이 0 에서 다시 자라기 때문이다. 이번엔 지우지 않고 **제자리 이관**한다.

가능한 이유: DMA·스토캐스틱은 **순수 OHLC 파생**이라 그 시점 값을 일봉 이력에서 정확히
되계산할 수 있다. 옵션 체인처럼 "지금만 알 수 있는" 값이 아니다.

### 피처 5종 (75 → 80, featVer 15 → 16)
`dmaPct` · `dmaGap` · `stochSlowK` · `stochSlowD` · `dsKnown`.
**반드시 featNames 의 꼬리에 붙였다** — 중간에 끼우면 앞 75칸 인덱스가 전부 밀려
기존 표본의 의미가 통째로 어긋난다(되돌릴 수 없다). `dsKnown` 은 **결측 표식**이다
(`fomcKnown` 과 같은 규율) — 이력을 못 찾은 표본은 중립값 + 0 으로 두어 모델이
"모른다"를 구분해 배우게 한다.

### 이관 (`mlFeatMigrate`)
- `ml_samples` · `ml_samples_st` 를 id 순으로 훑으며, 표본 `ts` 로 일봉 캐시의 `days`
  (에폭 이후 일수) 배열에서 봉 위치를 찾아 그 시점까지의 이력으로 지표를 되계산해 꼬리에 덧붙인다.
- 봉을 못 찾거나 이력이 61봉 미만이면 **버리지 않고** 중립 + `dsKnown=0`.
- **크론 매 분 9초씩** 민다(밤에 한 번이 아니라). featVer 를 올린 순간부터 학습은 새 판만
  보므로 이관이 늦을수록 위원이 굶는다. 끝나면 상태 1건만 읽고 즉시 빠져나온다.
- 소급 복원이 두 단계가 되어 `_calBackfillX` 를 `_featBackfillX(x, ts, dsVals)` 로 통합했다:
  D−11칸(달력·DS 둘 다 없음) → 달력 6 + DS 5, D−5칸 → DS 5. **한 함수가 두 경우를 다 안다** —
  판마다 복원기를 새로 만들면 "같은 규칙이 두 곳에 살다 한쪽만 고쳐지는" 사고가 재현된다.

### ★모델이 죽지 않게 한 세 가지 잠금★
1. **구판 정리 정지** — 야간 정리(`DELETE … WHERE featver != ?`)는 밤마다 30만 건을 지운다.
   이관 대상이 바로 그 행들이다. `mlFeatMigPending()` 을 먼저 물어보고, **모르면 안 지운다**.
   V33.328 의 alt 표 정리(`_altPrune`, ml_samples_st 포함)도 같은 조건에 걸었다.
2. **야간 학습 보류** — 이관 도중 학습하면 "지금까지 옮겨진 몇 만 건"만 보고 배운 뒤
   정확도가 낮게 나와 **신뢰게이트가 모델을 강등**시킨다. 데이터는 멀쩡한데 위원이 내려앉는다.
   기존 가중치를 그대로 두고 이관 완료 후 재개한다.
3. **Modal 트레이너는 손댈 필요 없었다** — `xn = len(featNames)` 로 서버가 알려주는 폭을
   따라간다(V33.255 설계). 80칸을 그대로 받는다.

### 게이트
`tools/check-featmig.mjs` 신설 — **가짜 DB 로 이관을 실제로 돌린다**. 표본 50건(이력 있음 40 +
없음 10)을 넣고: 전후 개수 동일 · 전부 featVer 16 · 전부 80칸 · 실측 40/중립 10 구분 · 완료 마감.
되돌림 두 가지(구판 정리 가드 무력화 · 새 칸을 앞에 붙이기)를 넣어 잡히는 것 확인.

기존 게이트 셋이 판갈이 부작용을 잡아 줘서 함께 고쳤다:
- `check-cal-backfill` — 달력이 더는 꼬리가 아니다. 위치 산수를 갱신하고, "달력까지 있는 판에
  달력을 두 번 붙이지 않는가" 검사를 추가했다.
- `check-calendar-feats` — featVer 15·75종을 **숫자로 박아** 두어 판이 오를 때마다 무관하게 깨졌다.
  "피처가 늘면 featVer 도 함께 오른다"는 **관계**로 바꿨다.
- `check-bandit-memo` — 피처가 75→80 이 되자 우연히 살아남는 잡음축이 하나 더 생겨 깨졌다.
  실제 가중은 0.02(신호축 1.00·0.81·0.74)로 잡음바닥은 멀쩡했다. **개수가 아니라 무게**로
  보도록 바꿨다(0.05 초과 잡음축 0개) — V33.273 사고(잡음축 0.2~0.5)는 이 잣대로 즉시 걸린다.

### 배포 후 확인
`[FEATMIG]` 로그로 진행을 본다. 완료되면 "★이관 완료★ featVer 16 — 구판 정리 재개" 가 한 번 찍힌다.
표본 총량이 이관 전후로 유지되는지(`/api/ml-status` 의 표본 수)를 확인할 것.
이관이 끝난 다음 밤부터 위원회가 80칸으로 다시 배운다.

## Current handoff — V33.335 (DMA 를 Displaced MA 로 바로잡음 + 이관 결함 3건 수정)

사용자: "displaced ma 말한거였는데 그리고 피쳐 문제 없는지 다시 확인해봐".
재점검에서 **실제 결함 3건**을 찾았다. 하나는 학습 데이터를 조용히 오염시킬 뻔했다.

### ① DMA 정의 교정 — 이동평균 차이 → **Displaced MA**
V33.332/334 는 국내 HTS 의 '이동평균 차이(DMA−AMA)'로 구현했었다. 사용자가 말한 것은
**이동평균선을 N봉 앞으로 민 선**이다.
- `getDisplacedMA(closes, 20, +5)`: 봉 i 의 값 = MA(20) 을 봉 (i−5) 에서 계산한 값.
- **앞(오른쪽)으로만 민다** — 뒤로 미는 변형은 미래를 참조하므로 학습에 쓰면 누출이다.
  게이트가 "현재 봉 종가를 바꿔도 선 값이 안 변한다"로 이 성질을 못박는다.
- 피처 이름도 정직하게 바꿨다: `dmaPct/dmaGap` → **`dispMaPct`**(가격이 선보다 몇 % 위/아래)
  · **`dispMaSlope`**(선 자체의 기울기 %/봉).

### ② ★표본의 ts 로 봉을 찾으면 안 됐다★ — 가장 위험했던 결함
수확 표본의 `ts` 는 실제 날짜가 아니다. 소스에 그렇게 적혀 있다:
```
// ts는 봉 시점 근사(일봉 1개=1일)로 역산
const ts = baseTs - (L - 1 - i) * 86400000;
```
주말·휴장을 세지 않으므로 **100봉 전 표본의 ts 는 실제 날짜보다 몇 주 앞선다.**
V33.334 의 이관은 그 ts 로 일봉을 찾았다 — 엉뚱한 봉의 지표를 `dsKnown=1`(실측)이라고
붙이게 된다. 중립으로 두는 것보다 나쁘다: 모델이 거짓을 진짜 관측값으로 배운다.

→ **내용으로 찾는다.** 저장된 벡터에 `dayPct·ret5·ret20` 이 들어 있고, 셋 다 종가만으로
정해지며 클램프도 없어 이력에서 그대로 재현된다. 세 값이 일치하는 봉이 그 봉이다.
둘 이상이 일치하면 애매하므로 쓰지 않는다(`-1` → 중립).
부수 효과로 **딥이력(hist:, 최대 2400봉)을 쓸 수 있게 됐다** — `days` 배열이 필요 없어졌기
때문이다. 종전 `daily:` 는 320봉(~15개월)뿐이라 오래된 표본을 대부분 못 덮었을 것이다.

### ③ 고가·저가가 어긋나면 스토캐스틱이 조용히 종가근사로 떨어졌다
`getStochSlow` 는 highs/lows 길이가 안 맞으면 종가로 대체한다. 그 값은 진짜 스토캐스틱과
**분포가 다르다** — 섞어서 학습하면 같은 칸에 다른 지표 둘을 밀어 넣는 셈이다.
→ 정렬이 안 맞으면 `dsKnown=0`. 실측이라고 하지 않는다.

### 그 밖에 확인하고 문제없던 것
- `mlBuildFeatures` 호출부 **12곳 전부** highs/lows 를 넘긴다 — 수확·라이브 스큐 없음.
- Modal 트레이너는 `xn = len(featNames)` 로 폭을 따라간다 — 손댈 필요 없음.
- 이관 종목 캐시에 상한(6종목)을 넣었다. 딥이력(2400봉×배열 3개)+앵커맵을 종목마다 들고
  있으면 한 호출에서 수십 MB 가 되어 아이솔레이트(128MB)를 위협한다. 행이 id 순이라
  같은 종목이 몰려 있어 작은 캐시로도 적중률이 높다.

### featVer 16 → 17, 전량 재각인
V33.334 가 붙인 값은 정의가 다르고 봉도 틀렸을 수 있으므로 **다시 각인해야 한다.**
`_featBackfillX` 가 이미 현재 폭인 벡터도 처리한다 — **값을 주면 꼬리를 갈아 끼우고,
안 주면 그대로 둔다**(내보내기 경로가 실값을 중립으로 덮어쓰지 않게).
이관 질의를 `featver <> 현재판` 으로 바꿔 15칸판·16칸판을 함께 처리한다.
구판 정리·야간 학습 정지는 V33.334 그대로 유지된다 — 재각인이 끝날 때까지 표본은 보존된다.

게이트 97개 전부 통과.

## Current handoff — V33.336 (차트 지표 버튼 + 3차 피처 점검)

사용자: "dma랑 스토캐스팅 스무딩선 종목 상세 그래프 위에 그려질수 있는 버튼 만들어
ma, boll지표 처럼 그리고 한번더 피쳐 점검해".

### ① 차트 지표 버튼 — DMA · STOCH
- **DMA** — 가격 패널에 ★점선★ 으로 그린다. 일반 MA 와 같은 실선이면 "MA 가 하나 더 있네"로
  읽히고 밀어놓은 선인 줄 모른다. 범례도 `DMA20+5` 로 적어 +5봉 이동을 못박는다.
  MA 와 같은 clip·y범위 규칙을 따른다 — 확대했을 때 먼 DMA 가 축을 끌지 않는다.
- **STOCH** — RSI·MACD 처럼 서브패널. %K(실선)·%D(점선), 20/50/80 밴드,
  과매수·과매도 구간을 옅게 칠했다.
- 기본은 **꺼짐** — 화면이 갑자기 복잡해지지 않게. 켰을 때만 계산한다(끈 지표를 매 프레임
  계산하면 드래그가 버벅인다). 지표 캐시 서명에 두 토글을 넣어 켜고 끌 때 옛 캐시를 안 쓴다.
- **게이트가 화면과 엔진을 교차 검증한다**: 설정(20,+5 / 14,3,3)이 서버 `DS_PARAMS` 와 같은지,
  화면이 그린 마지막 값이 서버 `getDisplacedMA`·`getStochSlow` 와 소수점까지 같은지.
  지표가 두 곳(브라우저·워커)에 구현돼 있어 갈라지기 쉬운 자리다.

### ② 3차 점검에서 찾은 것 — ★라이브 표본이 통째로 '모름'이 될 뻔했다★
V33.335 는 봉을 **내용 앵커**(dayPct·ret5·ret20)로 찾게 고쳤다. 그런데 다시 보니
**그 앵커는 수확 표본에만 맞는다**:
- 수확: `price = closes[L-1]` 로 ret5·ret20 을 계산한다 → 이력에서 그대로 재현된다.
- 라이브: `price = 그 순간의 호가` 로 계산한다 → **어떤 봉과도 안 맞는다.**

즉 라이브 표본은 전부 `dsKnown=0` 이 됐을 것이다. 하필 학습가중이 가장 높은
(`liveSrcWeight 1.5`) 표본들이다.

→ **두 갈래를 다 둔다.** 라이브 표본은 `ts` 가 진짜 날짜이므로(`Date.now()` 로 기록된다)
`daily:` 의 `days` 로 그 날 봉을 이진탐색해 찾는다. 표본이 이력보다 10일 이상 최근이면
쓰지 않는다(캐시가 낡았다는 뜻이라 엉뚱한 옛 봉을 붙이게 된다).
게이트에 라이브 표본 10건(앵커 불일치 + 진짜 ts)을 넣어 실제로 복구되는지 확인한다.
날짜 경로를 지우면 `0/10` 으로 즉시 걸린다.

### 점검하고 문제없던 것
- 앵커 세 값(`dayPct·ret5·ret20`)은 클램프가 없다 — `_mlStructFeats` 확인. 재현 가능.
- `sl(a, e)` 는 길이가 안 맞으면 `null` 을 준다 → `_hlOk` 가 그걸 받아 `dsKnown=0`.
  조용히 종가근사로 떨어지지 않는다(V33.335 에서 막은 경로가 실제로 작동).
- 딥이력(`hist:`)에는 `days` 가 없다. 그래서 앵커 경로는 딥이력, 날짜 경로는 `daily:` 로
  나눠 쓴다 — 두 배열의 인덱스가 다르므로 섞으면 안 된다.

게이트 97개 전부 통과.

## Current handoff — V33.337 (판 불일치·검증 미달 근본원인 — ★재시도할 기회가 없었다★)

사용자 화면(2026-09-10 10:35): 위원 좌석 **2/10**, **규칙엔진 비상운용**,
MIND·SEQ "판 불일치 — 재학습 대기", XGB·LGB·CAT "검증 미달 — 억제".

### 근본원인 두 가지 — 둘 다 V33.334~336 이 만든 것이다

**① `_stg` 가 "아직 준비 안 됨"에도 '오늘 완료' 도장을 찍었다.**
야간 파이프라인의 `_stg(nm, fn)` 은 함수가 무엇을 돌려주든 `ai_stage:<nm> = 오늘` 을 찍는다.
그래서 표본이 아직 모자라 물러난 단계는 **그날 다시 돌지 않는다.**
V33.334 가 `mlTrainNightly` 에 넣은 "이관 중 학습 보류"는 조용한 `return` 이었으므로
L1 이 그날 완료로 도장 찍혔고, MIND 도 `표본 N/M — 대기` 로 같은 길을 갔다.
→ **모델이 나쁜 게 아니라 다시 시도할 기회가 없었다.** 위원장이 빠지니 규칙엔진 비상운용.

이건 V33.334 만의 문제가 아니라 **원래 있던 함정**이다(V33.245 가 `_PIPE_VER` 로 featVer
바뀌면 도장을 지우게 해 뒀지만, 그 재실행 **한 번**이 표본 준비 전에 지나가면 끝이었다).

→ `_stg` 가 **"아직"과 "오늘 다 했음"을 구분**한다. 결과 문자열이 `⟳` 로 시작하면 완료 도장
대신 `<날짜>|wait|<시각>` 표식을 찍고, 12분 뒤 만료되면 같은 날 안에 스스로 다시 돈다.
표본 부족으로 물러나는 학습 단계 **12곳**(MIND·L1·BRAIN·GBDT·DNN·MEMO·BANDIT·SCAN·CAL 등)에
`⟳` 를 붙였다. "R2 없음"·"감성 없음" 처럼 **오늘은 할 일이 없는** 단계는 그대로 뒀다 —
그건 재시도해도 달라지지 않는다.

**② 이관이 ★오래된 표본부터★ 돌았다.**
학습은 최근 `trainWindow`(6만건)만 본다. 그런데 이관은 `id ASC` 라 **가장 오래된 것부터**
옮겼다 — 학습에 필요한 구간이 제일 마지막에 준비된다. 40만 건이면 몇 시간이고,
그동안 위원들은 "표본 부족"으로 대기한다.
→ `id DESC` 로 바꿨다. 몇 분 안에 학습창이 새 판으로 채워져 위원들이 곧바로 돌아온다.

### 외부 모델(SEQ·XGB·LGB·CAT)
`_luxAutoRetrainModal` 은 V33.245 부터 **현재 판과 일치하는 모델만 신선도에 센다** —
featVer 를 올리면 `anyExt=false` 가 되어 Modal 재학습을 스스로 건다.
다만 현재판 표본이 200건 미만이면 `insufficient_samples` 로 30분 뒤 재확인한다.
①②를 고치면 표본이 빨리 차므로 그 경로도 자연히 풀린다. **코드 수정은 필요 없었다.**

### 게이트에서 배운 것 — ★문장이 아니라 동작으로★
`_stg` 검사를 처음엔 "소스에 표식 문자열이 있는가"로 썼다. 그런데 분기를 `if(false)` 로
죽여도 문자열은 남아 **검사가 통과했다** — 헛도는 검사였다.
→ `_stg` 를 꺼내 **실제로 돌린다**: ⟳ 를 준 단계는 완료 도장이 안 찍히는가, 바로 다음 실행은
건너뛰는가, 표식이 만료되면 다시 도는가, 성공하면 그때 도장이 찍히는가, 완료된 단계는
그날 다시 안 도는가. 되돌림(분기 무력화)이 이제 즉시 잡힌다.
※ 추출 범위를 함수만 잡았다가 `_STG_RETRY_MS` 가 빠져 ReferenceError 가 `_stg` 자신의
  catch 에 삼켜졌다 — "재시도가 안 된다"로 잘못 읽을 뻔했다. 상수 선언까지 포함해 잘랐다.

`check-gate-anchors`(V33.328 메타검사)가 이번엔 **오탐**을 냈다: `indexOf(x) === 0` 은
자르기 앵커가 아니라 **접두사 검사**다. 제외 규칙에 추가하고 자가시험에도 넣었다.

게이트 97개 전부 통과.

## Current handoff — V33.338 (DNN 12층 → 4층 원인: 워커 폴백이 GPU 자리를 조용히 차지)

사용자: "dnn 또 12층에서 4층됐는데 수정해". **'또'** 가 핵심이다 — 판을 올릴 때마다 반복되는 구조다.

### 원인
- GPU 모델(Modal): `DNN.hidden = [640,512,384,256,192,128,96,64,48,32]` = **은닉 10층(총 12층)**,
  6시드·약 460만 파라미터.
- 워커 폴백: `DNNW.hidden = [128,64]` = **은닉 2층(총 4층)**, 넷당 1.7만 — GPU 망의 **1/46**.

featVer 를 올리면 외부 GPU 모델이 판 불일치로 빠지고, `mlDNNTrainNightly`(워커)가 새 판으로
자가학습해 **같은 자리(`dnn_model`/`dnn_trust`)를 차지한다.** 그런데 종전엔 그 사실이
**어디에도 안 적혔다** — 화면은 "DNN 정식 합류 · 신뢰 통과 ×0.69" 라고만 했고, 가중도 GPU 모델과
같은 식(MIND 와의 softmax)으로 계산됐다. 즉 **46배 작은 망이 실제 돈을 걸고 같은 목소리로 투표**했다.
사용자는 층수를 세어서 알아챘다 — 화면이 말해 줬어야 했다.

### 고친 것 세 가지
1. **폴백임을 기록한다** — `dnn_trust` 에 `source:"worker"` · `arch` · `hiddenLayers` · `params`.
   화면 이름도 "DNN (워커 폴백 · 은닉 2층)" 으로 바뀌고 tier 는 `provisional` 이 된다.
2. **가중을 깎는다** — `DNN.workerWeightMult = 0.5`. 0 으로 두면 판을 올릴 때마다 DNN 이 몇 시간
   통째로 빠지므로(V33.50 이 고친 '영구 학습대기'의 반대편 함정) 절반만 준다.
   이 저장소가 이미 쓰는 방식이다(`icPathWeightMult 0.35` · `fwdWeak 0.60`).
3. **12층을 빨리 되찾는다** — `_luxAutoRetrainModal` 의 8h 쿨다운을 **판당 한 번 건너뛴다**.
   종전엔 직전에 Modal 을 돌렸으면 쿨다운에 막혀, **가장 재학습이 급한 순간에 재학습이 가장
   확실히 잠겼다.** `meta.fvDispatched` 로 판별하고 **성공했을 때만 기록**한다(실패했는데
   기록하면 그 판에서 다시 시도할 길이 없어진다).

### ★게이트에서 진짜 버그를 찾았다 — 없는 위반 25건★
`.toLocaleString()` 한 줄을 추가하자 `check-pipeline-graph` 가 "순서 위반 25건"을 냈다.
코드가 아니라 **검사가 틀린 것**이었다:
`fnRanges()` 가 이름표를 `{}` 로 만들어, `R["toLocaleString"]` 이 **Object.prototype 의 메서드**를
돌려주며 "아는 함수" 로 잡혔다. 그러면 `ownChannels` 가 `R[fn][0]`·`[1]` 을 `undefined` 로 읽어
`lines.slice(undefined, undefined)` = **파일 전체**가 그 단계의 본문이 되고,
파일 안 모든 `setState` 가 그 단계의 산출물로 붙는다.
→ `Object.create(null)` 로 고치고, **자가시험**을 넣었다(toString·valueOf·constructor 등이
"아는 함수"로 잡히면 실패). `{}` 로 되돌리면 즉시 걸린다.
로그 문자열의 `toLocaleString()` 도 뺐다 — 같은 값이 환경에 따라 다르게 적힌다.

게이트 97개 전부 통과.

---

## V33.339 — 진단 스냅샷 처리: 판 불일치의 진짜 이름을 되찾고, 시간외 회전을 실제로 돌렸다

사용자가 운영 진단 스냅샷(`aistatus 2026-09-10T04:07:46Z`, 빌드 V33.338)을 올리며 "이거 해결해".
ERROR 1건 + WARN 7건이 있었고, 그중 **두 건은 증상 설명 자체가 틀려 있었다.**

### ① ★같은 세 모델을 두고 화면 세 곳이 다른 말을 했다★ (사용자가 앞서 지적한 "판 불일치")
스냅샷 실측:

| 보고처 | XGB 에 대해 |
|---|---|
| `aiMode.committee.xgb` | `trusted:true · promoted:true · w 0.4766` |
| `alt.roster.xgb` | `tier:"reject" · mult:0 · why "검증 미달 — 억제" · featVer **null**` |
| 실제 투표(`_boostersCached`) | **불참** — 위원회는 GBDT 한 명뿐(`committee.n = 1`) |
| 자가진단 | `XGB 미합류 accLB 51.46% (문턱 50.5%)` |

마지막 줄이 특히 나쁘다 — **문턱보다 높은 숫자를 미합류 사유로 적었다.** 그 문장을 믿으면
문턱을 만지러 간다. 진짜 이유는 넷 중 어디에도 없었다: XGB 21.5h · Cat 9.4h · LGB 3.5h 전
학습분이라 셋 다 `featVer 16` 이고, 판이 17 로 오른 건 그 뒤(V33.335, 01:14 UTC)다.
**판 불일치**였고 고칠 곳은 재학습이었다.

원인은 구조였다. 부스터 명단 행만 `featVerOk: true` 를 **박아 두고** 사유를 한 문장으로
뭉갰다 — 다른 위원(MIND·GBDT·FLOW·MEMO·SEQ)은 전부 "판 불일치 — 재학습 대기" 를 말할 수
있는데 **부스터 3종만 그 말을 할 수 없었다.** 그리고 `aiMode` 쪽은 `<nm>_trust.trusted`
(업로드 시점 도장)만 읽어, 그 뒤 판이 올라 실제로 빠진 모델을 계속 "가동 · w 0.4766" 이라 적었다.

→ `_boosterAdmit(live, ext)` 하나가 판정과 **사유**를 낸다. `_boostersCached` 가 그 함수를 부르고,
본문 확인까지 마친 결과를 `_boosterDiag(DB)` 로 내보낸다. 명단·`aiMode`·자가진단 **셋 다 그것만 읽는다.**
사유는 다섯 갈래로 갈린다: 모델 없음 / 판 불일치(16≠17) / 섀도우 미승격 / 승격 기록 미신뢰 /
검증 미달(실제 숫자와 함께). 스냅샷 값을 넣어 돌려 확인했다 — 세 보고처가 같은 문장을 낸다.

### ② ★committee_cal 이 featVer 15 에 박혀 있던 이유 — 준비 안 된 걸 '완료' 로 도장 찍었다★
경고: `committee_cal featVer 불일치(15≠17) — 확률 보정 무시 중`. 판이 15→16→17 로 올랐는데
보정은 15 에 머물렀다 = **두 판 동안 위원회 확률 보정이 통째로 꺼져 있었다**(읽는 쪽이 판
불일치 보정을 옳게 무시하므로).

`mlCalibrateCommittee` 의 이탈 경로 셋(`!mind` → `null`, 표본 <60, catch)이 **전부 재시도
신호(⟳)가 없었다.** 판이 오른 직후엔 새 판 표본이 아직 60건도 없다(이관 중) → "보정 대기" →
`_stg` 가 **오늘 할 일 끝**으로 도장 → 다음 UTC 자정까지 재시도 없음. 그 사이 판이 또 오르면 반복.
V33.337 이 12개 단계에 넣은 ⟳ 가 이 단계의 표본부족 경로만 비켜 갔다.
→ 셋 다 ⟳ 로 바꿨다. 같은 성질의 다른 단계 5곳도 함께(ANLREVK·CONFK·MIND-SHADOW·EXPREG·OPTX).

### ③ ★"세션 표시 336종목 중 값이 있는 건 2종목뿐" — 원인은 두 겹이었고 둘 다 우리 쪽이다★
- **회전이 안 돌았다.** 시간외 보강 커서가 `extOffset: shard * 7` — **샤드마다 상수**다.
  회전 상한(24종목)에 걸린 뒤로는 사이클마다 **같은 24종목**을 다시 물었고 나머지 수백 종목은
  차례가 오지 않았다. 가격 폴백은 상태에 저장한 커서(`qp_rr`)로 제대로 돌고 있었다 — **한쪽만 안 돌았다.**
  → `ext_rr:<시장>` 커서를 상태에 저장하고 시도한 만큼 전진시킨다.
- **세션 딱지가 굳었다.** `mstate` 를 야후 v7 이 줄 때만 갱신했다. v7 이 죽자 며칠 전 딱지가
  그대로 남고 `COALESCE` 보존이 그걸 계속 지켰다 — 화면은 **정규장 종가를 '장후 시세' 라고 말한다.**
  자가진단의 "336종목" 도 그 유령을 센 것이다.
  → 세션은 시각의 함수다(`getUSEt` = IANA, 서머타임 자동). `usMarketStateNow()` 로 우리가 찍고,
  `extKeepMaskUS()` 로 **지난 세션 값을 이어받지 않는다**(장전엔 어제 장후를, 정규장엔 둘 다 지운다.
  휴장 중엔 마지막 장후 체결가를 그대로 둔다 — 그게 최신 체결이다). 저장 경로 둘(`saveQuote`,
  가격 샤드 SQL) 다 같은 규칙을 지난다.

### ④ ERROR 문장이 자기 코드보다 낡아 있었다
`야후 v7 이 응답하지 않는다 — v8 폴백은 시간외를 못 만든다(시간외 시세 전면 결측)`.
**틀렸다.** V33.331 이 이미 v8 분봉(`includePrePost`)에서 프리·애프터를 만드는 경로
(`fetchExtendedQuoteUS`)를 넣었다. 진짜 손실은 다른 것이다 — v7 이 살아 있으면 50종목이
1 subrequest 인데 죽으면 **종목당 1 subrequest**(미국 558종목)가 되고, 그 예산 압박이 보강 회전을 느리게 한다.
→ 문장을 사실에 맞추고, `_v7ErrTag()` 로 **실패 사유(HTTP 상태·타임아웃·예산)** 를 상태에 남긴다.
인증(401/403)·요청형태(fields=)·과부하(429)는 처방이 전혀 다른데 종전엔 `dead:true` 뿐이었다.
덤으로 **지난번에 통한 방식을 기억한다**(fields 거부 환경에서 매 호출 버려지던 subrequest 하나 제거).
단 **사망 판정은 기억하지 않는다** — 야후가 되살아나며 필드셋을 복구할 길을 스스로 닫지 않는다.

### 게이트
- 신규 `check-committee-truth.mjs` — 판정 함수를 **실제로 돌려** 사유가 상황마다 갈리는지 본다
  (판 불일치 / 검증 미달 / 섀도우 / 없음 / 정상통과 / IC 경로). 돌연변이 3종으로 물리는 것 확인.
- 신규 `check-extquote-rotation.mjs` — 커서 저장·전진, 세션 경계 10곳, 마스크 4종, 낡은 값 삭제를 실행 검사.
- `check-overmarket.mjs` 를 세션 인식으로 확장하다가 **내 버그를 잡았다** — `saveQuote` 의 마스크
  기준을 `_keep("mstate")`(저장된 딱지)로 잡아 두어, 낡은 딱지가 스스로를 증명하는 순환이 생겼다.
  기준을 이번 조회값 또는 시계로 바꿨다.
- `check-model-evidence` · `check-ext-trading` 을 새 단일 출처에 맞춰 갱신(앵커 포함).

게이트 99개 전부 통과 · `node --check src/index.js` 통과.

### 남은 것(코드로 못 정하는 판단)
- **DNN accLB 45.66%** — 이번엔 워커 폴백이 아니라 **GPU 모델**(`source:"external"`, featVer 17,
  valAcc 48.8%)이 문턱 50.5% 에 못 미친 것이다. 즉 V33.338 의 진단은 맞았고, 지금은 모델 실력 문제다.
  억제는 정상 동작 — 고칠 대상은 학습 쪽이다.
- **XALPHA 전진 IC −0.039** 가 `fwdReady=false` 라 안 잡히는 건 여전히 열려 있다(문턱 설계 판단).
- **구버전 표본 505,728** 은 이관이 돌면서 줄어든다(324,821 / 832,676 = 39%).

---

## V33.340 — ★시간외 거래의 신선도 가드가 한 번도 걸린 적이 없었다★

V33.331~332 가 시간외 거래를 열면서 넣은 가드다. 설정에 `freshMs: 420000` 이 있고
주석도 "낡은 값으로는 거래하지 않는다" 라고 적혀 있었다. **그런데 작동한 적이 없다.**

```js
const _ts = _num(q.ts, 0);
if (_ts > 0 && (Date.now() - _ts) > freshMs) return null;   // ← _ts 는 언제나 0
```
`extTradePrice` 가 읽는 `q.ts` 는 **거래 경로의 quote 객체에 아예 없는 필드**다.
그 객체는 `fetchBatchQuotes` 가 만드는데(`{price, prevClose, dayPct, mstate, pre, post…}`),
`ts` 를 넣는 곳이 한 군데도 없다. 그래서 `_ts > 0` 이 항상 거짓 → 검사 통째로 건너뜀.
DB 에 저장된 행에는 `ts` 가 있지만 그건 **가격 샤드가 마지막으로 쓴 시각**이지 체결 시각이
아니다 — 그 값을 썼더라도 5시간 전 체결이 "방금"으로 통과한다.

실측: `extTradePrice({pre:100, prePct:1}, "pre", cfg)` → `100` 을 그대로 돌려준다.

### 왜 위험한가
얇은 종목은 프리마켓에 **04:05 에 한 번 찍고 09:00 까지 체결이 없다.**
`fetchExtendedQuoteUS` 는 "창 안의 마지막 체결"을 고르므로 그 04:05 값을 돌려주고,
그 값으로 **손절이 나가거나 신규 진입이 들어갔다.** 시간외 저유동성 구간에서
정확히 하면 안 되는 일이다.

### 고친 것
1. **값을 만드는 쪽이 체결 시각을 함께 싣는다** — `extTs`.
   v7 은 `preMarketTime`/`postMarketTime` 을 **명시적으로 요구**하고(안 물으면 안 준다 —
   V33.330 에서 배운 그대로), v8 분봉은 **고른 봉의 시각**을, 네이버는 폴링 호출 시각을 적는다.
2. **시각을 모르면 거래하지 않는다** — 이 저장소의 원칙 그대로다. 표시 경로
   (`applyDisplayOverMarket`)는 **건드리지 않았다**. 화면에 마지막 체결이 남는 것과
   그 값으로 돈을 거는 것은 다른 문제다.
3. **시각이 값과 함께 살고 죽는다** — `saveQuote` · 가격 샤드 SQL · `refreshQuotesOnly` ·
   `normalizeExtUS` 넷 다. 하나라도 빠지면 DB 를 거친 순간 시간외 거래가 통째로 막힌다.
4. **폴백의 추측을 측정으로 바꿨다** — `fetchQuoteViaChart` 는 두 개의 추측 위에 서 있었다:
   *"live 와 일봉 종가가 다르면 시간외 체결이다"* (마감 후 `meta.regularMarketPrice` 는 보통
   **정규장 종가**다 — 갈리는 건 대개 일봉 갱신 지연이지 시간외 체결이 아니다) 와
   *"정규장 5.5시간 전 안쪽이면 장전"* (야후가 창을 `currentTradingPeriod.pre/post` 로
   이미 주는데 손으로 다시 적었다 — 서머타임·조기폐장을 우리가 재계산하는 셈).
   → **체결 시각이 pre/post 창 안인가**를 재서 판정한다. 둘 다 아니면 아무것도 만들지 않는다.

### ★가드를 조이면서 새 침묵을 만들지 않았는가★
이게 이번에 가장 신경 쓴 부분이다. 수집원이 시각을 안 주기 시작하면 시간외 거래가
통째로 멈추는데, 증상은 **"오늘은 시간외 체결이 없었나 보다"와 구별되지 않는다** —
이 저장소가 반복해서 당한 함정이다(`v7Dead` 를 계산만 하고 안 읽던 것과 같은 모양).
→ `extTradePriceEx` 가 **막은 이유**를 함께 돌려주고(`noprice`/`nots`/`stale`/`bigmove`/`lowprice`),
메인 사이클이 이유별로 세어 로그와 `ext_block:<시장>` 상태에 남긴다.
자가진단은 **구성**을 본다: 체결없음이 대다수면 정상이고, **시각미상이 대다수면
시장이 조용한 게 아니라 우리 수집이 고장난 것**이라고 말한다.

### 게이트
신규 `check-ext-freshness.mjs` — 가드를 **실제로 돌려** 본다:
시각 없는 값·`q.ts` 만 있는 값·5시간 전·8분 전은 막히고, 1분 전은 통과하고,
초저가·과대변동 가드는 그대로 살아 있는지. **사유 이름표까지 못박는다** —
돌연변이 시험에서 시각미상 분기를 죽여도 낡은값 검사가 대신 막아 주므로
*가격만 보면 통과*했다. 그러면 수집 고장이 "낡은체결"로 보고돼 원인을 엉뚱한 데서 찾게 된다.
돌연변이 6종(가드 제거·`q.ts` 회귀·시각 위조·저장 누락·v7 필드 제거·사유 뒤바꿈) 전부 물린다.

게이트 100개 전부 통과 · `node --check src/index.js` 통과.

---

## V33.341 — 내부 AI: ★모델마다 다른 자로 재고 있었다★ (+ 시간외 체결비용)

사용자 지시: "내부 탑재 ai 문제부터 해결해라 잡음이랑 구별 안되는거랑 검증 성능 안나오는거
전부 해결시켜라 시간외 문제도 더 해결해라".

### ★먼저 — 틀린 가설 두 개를 세우고 버렸다★
증거 없이 고치지 않기 위해 둘 다 시뮬레이션으로 재고 접었다. 기록으로 남긴다.

1. **"수확/라이브 피처 스큐가 DNN 을 무너뜨린다"** — `sigWeight`·`confluence`·전략원핫4 는
   수확이 원리적으로 만들 수 없어 표본의 80%에서 상수이고 라이브에서만 값이 튄다.
   그럴듯했지만 **재 보니 아니었다**: 합성 실험(수확 80%/라이브 20%, 검증꼬리 라이브 55%)에서
   MLP 56.69% → 57.08%, **잡음 수준**. 이유도 분명하다 — 상수 칸은 그래디언트를 못 받아
   가중이 초기값(작음)에 머문다. 3σ 스파이크 × 작은 가중 = 작은 섭동이다.
   (중립화는 **위생 목적으로만** 넣었다. 성능 주장으로 팔지 않는다.)
2. **"엠바고 누락이 트리 성적을 부풀린다"** — 실측 **0.14%p**. 실재하지만 4%p 격차의 원인이 아니다.

### ★진짜로 찾은 것 — 자가 서로 달랐다★
문제는 "모델이 약하다"가 아니라 **같은 문턱을 서로 다른 자로 통과시키고 있었다**는 것이다.

| | 엠바고 | τ* 선택 | 채점 구간 | 유효표본(실측 역산) |
|---|---|---|---|---|
| DNN | 있음(6일) | **검증 앞절반** | **검증 뒤절반** | **≈690** |
| MIND(FM) | **없음** | **검증 앞절반** | **검증 뒤절반** | **≈950** |
| GBDT · XGB/LGB/CAT · 시장별 | **없음** | — | 검증 전체 | **≈7,400** |

**같은 풀인데 유효표본이 10배 차이다.** 승격 게이트는 Wilson 하한을 보므로 표본이 적으면
하한이 그만큼 내려간다 — 즉 **"DNN 검증 미달(accLB 45.66%)"의 상당 부분은 실력이 아니라
자의 길이였다.** 그리고 경계를 안 비운 모델들이 바로 **위원회에 앉아 실제 돈을 거는** 모델들이다.

추가로 **엠바고 길이 자체가 지평보다 짧았다**: `embargoDays = 6` 인데
`predictionHorizonDays = 10`. V32.10 이 지평을 5→10 으로 올릴 때 엠바고는 안 따라갔고
주석만 "라벨 horizon(5일)"로 남았다. 손으로 적은 숫자가 다른 손으로 적은 숫자를 못 따라갔다.

### 고친 것
1. **분할을 한 곳에서만 낸다** — `_split_ts()`. 학습기 5곳(DNN·GBDT·부스터·시장별·MIND)이
   전부 같은 함수를 부른다. 경계를 안 비우는 옛 분할(`Xs[:-nval]`)은 소스에서 사라졌다.
2. **엠바고를 지평에서 파생시킨다** — `LUXML.embargoDays` 는 이제 getter 로
   `max(6, ceil(horizonDays))`. 지평을 바꾸면 따라온다. 트레이너도 지평 미만이면 지평으로 올려 쓴다.
3. **τ* 를 검증에서 뗀다** — 학습 구간의 꼬리 10%를 보정 구간으로 떼어(학습에서 제외)
   거기서 τ* 와 시드를 고르고, **검증은 전부 채점에 쓴다.** DNN·MIND 의 유효표본이
   부스터와 같은 자로 돌아온다. 엠바고가 보정과 검증을 갈라 놓으므로 누출은 없다.
4. **죽은 손잡이 하나를 살렸다** — `liveSrcWeight: 1.5` 는 워커 자체 학습기 다섯 곳이 쓰는데
   `_mlExportConfig` 가 안 내려보내서 **Modal 은 그런 값이 있는 줄도 몰랐다**
   (`np.where(HV > 0, hv_w, 1.0)` — 라이브는 언제나 1.0). 위원회에 앉는 모델은 전부
   external 이므로, "실거래 표본을 우대한다"는 설정이 **위원회에는 한 번도 적용된 적이 없다.**
   V33.260 이 dropout·l2 에서 잡은 것과 같은 종류다 — 학습기가 둘인데 설정 통로가 하나만 넓었다.
5. **신호 컨텍스트 6종 중립화**(위생) — 수확이 못 만드는 칸을 라이브에서도 안 만든다.
   피처 생성기 한 곳에서 덮어쓰고, 트레이너는 저장된 옛 표본의 같은 칸도 눌러 맞춘다
   (**표본을 버리지 않는다 — 값만 규약에 맞춘다**).

### 시간외 — ★비용을 낮게 잡는 것은 수익을 지어내는 것과 같다★
`_slipRate(market, ts)` 는 `ts` 를 받으면서 **세율 적용 여부에만 쓰고 세션은 안 봤다.**
그래서 V33.332 가 시간외 거래를 전 종목으로 연 뒤에도 장전·장후 체결이 정규장과 똑같이
5bp(미국)·8bp(한국)로 기록된다. 시간외 호가는 그럴 수가 없다 — 마켓메이커 의무가 없고
장부가 얇다. 낮게 잡으면 셋이 한꺼번에 틀어진다: ①원장이 시간외를 실제보다 잘한 것으로 적고
②그 pnl 이 학습 라벨이 되어 모델이 "시간외가 유리하다"고 배우고 ③켈리가 부풀린 엣지 위에서
크기를 정한다.
→ `_extSessionAt(market, ts)` 로 **체결 시각의 세션**을 보고 ×3 을 건다.
**배수는 측정이 아니라 가정이라고 코드에 적어 뒀다.** 소급 적용하지 않는다
(시간외 거래를 연 판부터만 — 과거 원장을 조용히 다시 쓰면 현금 체크포인트와 어긋난다).
실측 확인: 정규장 5.0bp / 장전·장후 15.0bp / 심야·주말 5.0bp / KR 장후 24.0bp /
암호화폐 언제나 5.0bp / 2026-09-01 장후 5.0bp(소급 안 함).

### 게이트
신규 `check-split-embargo.mjs` — 분할 단일출처·엠바고≥지평·τ* 위치·보정구간 학습제외·
출처가중 전달·중립화 배선을 못박는다. 돌연변이 6종 전부 물린다.
기존 `check-stack-oof` 는 "같은 비율" 대신 **"같은 함수를 부르는가"** 로 강화했다 —
실제 문제는 비율이 아니라 엠바고였기 때문이다.

게이트 101개 전부 통과 · `node --check` · `ast.parse` 통과.

### 남은 것 — ★고치지 않고 보고한다★
- **DNN 은 여전히 은닉 10층이다.** V33.204 실측 기록: 10층 valAcc 49.3% vs 워커 폴백 2층 53.3%.
  이 저장소는 그 측정을 갖고도 10층을 기본값으로 둔다 — **사용자 지시**이기 때문이다
  (V33.193, 그리고 "dnn 또 12층에서 4층됐는데 수정해"). 스윕은 신뢰 실패 시에만 돌고
  승자를 `dnn_arch` 에 저장해 다음 학습이 쓴다. **깊이를 바꾸는 것은 코드가 아니라 사람이
  정할 문제라 손대지 않았다.** 이번 수정으로 자가 같아졌으니, 다음 학습의 숫자가
  "10층이 정말 나쁜가"에 대한 처음으로 공정한 답이 된다.
- XALPHA 전진 IC −0.039 가 `fwdReady=false` 라 안 잡히는 건 여전히 열려 있다(문턱 설계 판단).

---

## V33.342 — 프로덕션 실측이 드러낸 것: ★내 진단 문구 두 개가 틀렸고, 재시도를 허락하는 쪽이 없었다★

2026-09-10 11:41 UTC(미국 장전 07:41 ET) 자체 점검. `worker-probe` 로 실측했다.

### ★먼저 — V33.340 의 배선은 완벽히 작동했다★
```
US 시간외 거래가 시각 문제로 막히는 중 — 제외 556종목 중 시각미상 0 · 낡은체결 389
```
- **시각미상 0** — 값이 있는 종목은 전부 체결 시각(`extTs`)을 갖고 있다. 배선 성공.
- 그리고 v7 경고가 사라졌다 → **v7 이 살아나 `preMarketTime` 을 주고 있다**(V33.340 이 명시적으로 요구한 필드).
- **낡은체결 389** — 값은 있는데 마지막 체결이 7분보다 오래됐다.
  **이것이 장전의 실제 모습이다.** V33.340 이전이었다면 이 389종목 전부를
  "지금 값" 으로 거래했을 것이다.

### ★그런데 내가 그 정상 상태를 고장이라고 적어 놨다★
1. **자가진단 문구** — "체결이 없는 게 아니라 수집 경로가 체결 시각을 안 준다".
   **정확히 반대다.** 시각은 잘 들어오고 있었고(시각미상 0), 그 시각이 말해 준 사실은
   "장전엔 대부분 종목이 몇십 분째 체결이 없다" 는 것이다. 고장이 아니라 시장의 성질이다.
   → `nots` 우세(수집 고장, **warn**)와 `stale` 우세(정상, **info**)를 갈라 말하게 고쳤다.
   *고장을 정상으로 읽는 것도 나쁘지만, 정상을 고장으로 읽으면 멀쩡한 걸 고치러 간다.*
2. **`[FETCH] 평가가능 0종목(… 일봉결측)` ERROR ×15회** — 원인을 "일봉결측" 으로 **박아 뒀다**.
   같은 순간 일봉은 멀쩡했고 걸린 곳은 시간외 신선도 가드였다(평가 루프에서 시간외 가드가
   일봉 검사보다 **앞**에 있다). 시간외에 체결이 드문 건 정상인데 ERROR 로 15번 적으면
   **진짜 사고가 그 사이에 묻힌다.** → 시간외에는 실제 차단 구성을 적고 등급도 사실에 맞춘다.

### ★V33.337 이 고친 결함이 한 단계 위에 그대로 있었다★
`committee_cal featVer 불일치(15≠17)` 가 **V33.339 배포 5.5시간 뒤에도 그대로**였다.
보정 단계는 ⟳ 로 재시도를 정상적으로 요청하고 있었다. 문제는 **허락하는 쪽**이었다:

```js
await setState(env.DB, "ai_trained_day", _aiDay);   // ← 무조건
...
if (_aiLast !== _aiDay && !_aiLockFresh) { …파이프라인… }   // ← 도장 찍히면 그날 닫힘
```
V33.337 은 **단계**에 "다시 오겠다" 고 말할 권한을 줬는데, **파이프라인**은 끝에서
완주 도장을 무조건 찍는다. 도장이 찍히는 순간 그날은 닫히고, 대기 중인 단계는
다시 들어올 길이 없다. **말할 권한만 주고 문을 잠근 셈이다.**
그래서 두 판(15→16→17)에 걸쳐 위원회 확률 보정이 꺼져 있었다.

→ 완주 직전에 `ai_stage:*` 중 `<오늘>|wait|` 인 것을 세고, **하나라도 있으면 그날을 닫지 않는다.**
  `ai_pipe_partial` 로 6분 쿨다운 후 재진입해 **대기 단계만** 돌린다(끝난 단계는 자기 도장으로
  즉시 빠지므로 재진입이 싸다). 6시간 상한을 두되 **닫을 때 누가 못 끝냈는지 이름을 적는다** —
  조용히 닫으면 이 결함이 그대로 반복된다.

### 게이트
신규 `check-pipeline-resume.mjs` — **조건식을 소스에서 뽑아** 상태기계를 굴린다.
(조건을 게이트에 다시 적으면 소스가 바뀔 때 게이트만 옛 조건을 지킨다 — 통과해도 무의미해진다.)
정상 진입 / 완주 후 미진입(CPU 보호) / 쿨다운 / 6분 뒤 재개 / 락 존중 / 어제 기록 무시 6가지를
실제로 평가한다. 돌연변이 4종 전부 물린다.
`check-ext-trading` 의 신선도 시험이 아직 `q.ts` 를 쓰고 있어 **엉뚱한 것을 시험하고 있었다** —
`extTs` 로 고치고 "q.ts 는 체결 시각으로 인정하지 않는다" 를 한 줄 더 넣었다.

게이트 102개 전부 통과.

### 남은 것
- **신선도 문턱 7분이 장전에는 사실상 대부분을 막는다**(556종목 중 389 차단). 보수적이라
  실거래 안전 쪽으로는 옳지만, "시간외에도 정규장처럼 거래" 라는 목표와는 상충한다.
  더 나은 설계는 **체결 나이에 비례해 크기를 줄이는 것**(막느냐 마느냐의 이분법 대신)인데,
  이건 위험 정책 변경이라 사용자 판단이 필요하다 — **손대지 않고 보고한다.**
  이제 v7 이 `preMarketTime` 을 주므로 체결 나이 분포를 실제로 잴 수 있다.
- DNN 은 여전히 은닉 10층(사용자 지시). V33.341 로 자가 같아졌으니 다음 학습 숫자가 공정한 답이 된다.

---

## V33.343 — ★V33.342 는 이미 닫힌 문을 열지 못했다★ (내 수정의 구멍)

V33.342 배포 89분 뒤 실측. 두 개는 고쳐졌고 하나는 **내 수정에 구멍이 있었다.**

### 고쳐진 것 (실측 확인)
```
issues.2.level = info      ← warn 이었다
US 시간외 장전 — 제외 556종목 중 낡은체결 469 · 시각미상 0. 시간외엔 체결 자체가 드물어
대부분 종목의 마지막 체결이 오래됐다(정상). 가드가 그 값으로 거래하는 것을 막고 있다 — 신선도 문턱 7분
```
```
[FETCH] US 시간외 pre 평가가능 0종목(priced=556) — 체결없음 86 · 낡은체결 469 · 과대변동 1
        · 시간외에 체결이 드문 것은 정상이다(일봉과 무관)      ← INFO
```
`warnCnt` 7 → **6**. 문구가 사실에 맞고 등급도 맞다.

### ★안 고쳐진 것 — 그리고 원인은 내 코드였다★
`committee_cal featVer 불일치(15≠17)` 가 그대로였다. 로그를 봤더니:

**워커 로그 900줄에 `[SCHED]` 가 0줄.** 완주도 부분완주도 없다 — 파이프라인이 오늘
**한 번도 들어가지 않았다.**

V33.342 의 진입 게이트는 `ai_pipe_partial` 을 본다. 그런데 **그 값은 파이프라인 안에서만 써진다.**
배포 시점에 이미 오늘 도장이 찍혀 있으면:
- `_aiLast === _aiDay` → 첫 항 거짓
- `ai_pipe_partial` 없음 → `_resume` 거짓

**닭과 달걀이다.** 내 수정은 *다음 날부터* 효과가 있고, 이미 닫힌 오늘은 못 연다.
"재시도를 허락하는 쪽이 없었다" 를 고친다면서, 허락하는 쪽이 **자기가 열어야 볼 수 있는 기록**을
근거로 삼았다. 같은 모양의 실수를 한 단계 아래에서 반복한 것이다.

### 고친 것 — ★안 끝났다는 사실이 실제로 있는 곳을 본다★
`ai_pipe_partial` 은 쿨다운용 보조 상태일 뿐이다. 진실은 두 곳에 있다:
- **(A)** `ai_stage:*` 에 오늘의 `|wait|` 표식 — 단계가 스스로 그렇게 적었다.
- **(B)** 단계의 **산출물이 옛 판** — 이번 판에서는 끝난 적이 없다는 직접 증거.

**오늘의 실제 상황은 (B)다.** `calibrate` 는 V33.339 이전에 찍힌 완료 도장을 갖고 있어
(A)로는 안 잡힌다. 하지만 `committee_cal.featVer 15 ≠ 17` 이 말해 준다.
→ `_pipeOpenStages()` 가 둘 다 확인하고, (B)면 **그 단계의 완료 도장을 지운다**
(안 지우면 재진입해도 `_stg` 가 "오늘 완료"로 보고 그냥 빠진다).
**6분에 한 번만** 스캔한다 — 매분 도는 크론에 범위 스캔을 얹지 않는다.

`_STAGE_OUT_VER` 는 표다(현재 `calibrate ← committee_cal` 한 줄). 같은 성질의 단계가
생기면 한 줄을 더한다 — 규칙을 코드에 흩뿌리지 않는다.

### 게이트
`check-pipeline-resume` 를 확장했다. 조건식은 여전히 **소스에서 뽑아** 평가하고,
`_openStages` 를 주입해 **두 번째 문**을 따로 시험한다. 그리고 스캔 함수 자체를
**가짜 DB 로 실제로 돌린다**: (A) 대기 표식 발견 · (B) 옛 판 산출물 발견 + 도장 삭제 ·
판이 맞으면 무동작 · 6분 스로틀. 돌연변이 5종 전부 물린다.

게이트 102개 전부 통과.

### 곁가지 관측
- **MLOPS 열화 문턱**: 검증정확도가 48.2% → **50.0%** 로 올라왔다(문턱 50.5%). 아직 미달이지만
  V33.341 이후 처음 도는 학습은 아직 안 끝났다(자동 재학습 12.1h 전 트리거).
- 옛 `[FETCH] … 일봉결측` ERROR 가 21회로 집계되지만, 이는 자가진단이 **최근 12시간** 창을
  보기 때문이다(배포 전 발생분). 새 INFO 문구가 나오고 있으므로 새 경로가 도는 것은 확인됐다.

---

## V33.344 — ★관측할 수 없으면 고칠 수 없다★ (스캔 위치 + 파이프라인 상태 노출)

V33.343 배포 48분 뒤에도 `committee_cal` 은 15 였다. 원인을 쫓느라 프로덕션 로그를
**네 번** 긁었고, 그때마다 새로운 추측이 하나씩 늘었다. 그 자체가 결함이다.

### 실측으로 확정한 것
워커 로그 **2498줄 = 01:30~13:51 UTC, 12시간치** 를 받아 보니:
- `[SCHED]` **0줄** · `[CAL]` **0줄** (7줄은 SCALP 등 우연한 부분일치)
- 같은 창에 거래 사이클 로그는 가득하다(BUY/SIGNAL/BLOCK, 13:43~13:51)

→ **야간 파이프라인 블록의 뒷부분이 12시간 내내 한 번도 실행되지 않았다.**

### ★내 앞선 판단 하나를 정정한다★
V33.343 에서 나는 "[SCHED] 가 0줄 = 파이프라인이 한 번도 들어가지 않았다" 고 적었다.
**그건 과했다.** `[SCHED]` 는 파이프라인 ★끝★ 에서만 찍힌다 — 중간에 죽으면 들어갔어도 0줄이다.
없는 것을 근거로 "안 들어갔다" 를 단정한 것이고, 그 위에 다음 수정을 얹었다.

### 진짜 구조
야간 블록은 한 invocation 안에서 **수확 캐치업 → 반사실 → 딥이력 → … → 진입 게이트** 순으로 돈다.
앞의 무거운 일이 CPU·시간 예산을 먹으면 **뒤쪽은 도달하지 못한다.**
V33.343 의 스캔은 하필 그 뒤쪽(진입 게이트 바로 앞)에 있었다 — 장중 내내 굶었다.

→ **스캔을 블록 맨 앞으로 옮겼다.** D1 읽기 몇 번이라 값싸고, 6분 스로틀이 있다.
   결과를 `ai_pipe_partial` 에 남기면 **다음 틱의 진입 게이트가 그걸 보고 연다** —
   그 invocation 이 뒤까지 못 가도 상관없다.

### ★그리고 상태를 볼 수 있게 했다★
이 결함 하나를 쫓는 데 로그 네 번이 든 이유는 단순하다:
`ai_trained_day` 가 언제 찍혔는지, 어떤 단계가 대기 중인지, 스캔이 돌기는 했는지 —
**아무 데서도 볼 수 없었다.** 그러면 남는 건 추측이고, 추측으로 고치면 이 저장소가
여러 번 겪은 방식으로 실패한다(worker-logs 워크플로 주석이 같은 말을 한다).

→ `/api/selfcheck` 가 `pipe` 를 함께 돌려준다:
   `{ day, trainedDay, closed, stagesToday, stagesOld, waiting[], waitingN, partial, lastScanMin }`
그리고 **"닫혔는데 안 끝난 게 있다"** 는 조합을 경고로 소리 내게 했다 — 오늘의 사고가 정확히 그것이다.
다음부터는 조회 한 번으로 원인이 갈린다.

### 게이트
`check-pipeline-resume` 에 두 절을 더했다 — 스캔이 블록 맨 앞에 있는가, 상태가 노출되는가,
그 조합을 경고하는가. 게이트 102개 전부 통과.

### 아직 미해결 — 정직하게
`committee_cal` 은 **여전히 featVer 15** 다. 이번 수정이 실제로 문을 여는지는
**다음 크론 틱 이후에야** 확인된다. 그리고 확인 수단이 이제 로그 추측이 아니라
`selfcheck.pipe` 이므로, 다음 점검은 한 번의 조회로 끝난다.

---

## V33.345 — ★확률 보정 복구 확인★ + v7 문구 정정

### 결과: committee_cal 이 돌아왔다
V33.344 배포 30분 뒤 `/api/selfcheck` 한 번으로 확인:
```
pipe.day = 2026-09-10 · trainedDay = 2026-09-10 · closed = true
pipe.stagesToday = 45 · stagesOld = 0 · waitingN = 0 · lastScanMin = 2.9
```
- **`committee_cal featVer 불일치(15≠17)` 경고가 사라졌다** — 두 판 동안 꺼져 있던
  위원회 확률 보정이 다시 켜졌다.
- `stagesOld = 0` · `waitingN = 0` — 안 끝난 단계가 없다. 스캔은 2.9분 전에 돌았고
  열 것을 못 찾았다(= 정상).
- **`lastScanMin` 이 값을 갖는다는 것 자체가 V33.344 의 성과다** — 스캔이 굶지 않는
  자리로 옮겨져 실제로 돌고 있다.

### ★그리고 관측이 실제로 값을 했다★
V33.343 까지는 이 하나를 판단하려고 로그를 네 번 긁고도 못 갈랐다.
V33.344 이후에는 **조회 한 번**으로 끝났다. `pipe` 를 넣은 이유가 그것이고,
넣자마자 그 값을 했다.

### 곁가지 — 오늘 내가 쓴 문장 하나를 또 정정한다
새로 뜬 ERROR: `야후 v7(미국 시세 1차 수집원)이 응답하지 않는다` — **err 태그가 비어 있었다.**
`v7Err` 는 ★예외가 났을 때만★ 채워진다. 비어 있다는 것은 예외 없이 200 을 받았는데
종목을 하나도 못 준 경우다 — **통신 실패가 아니라 응답이 빈 것**이고, 처방이 다르다
(앞은 재시도·crumb 재발급, 뒤는 요청 형태·심볼 목록).
→ 두 경우를 갈라 적는다. 오늘 이 파일에서 반복해 고친 것과 같은 종류다.

### 남은 상태(참고)
- `errCnt 1 · warnCnt 6`. ERROR 는 v7 하나뿐이고, v7 은 오늘 아침엔 살아 있었다가
  다시 빈 응답을 주는 중이다(간헐적). v8 폴백이 받치고 있다.
- MLOPS 열화 **50.0% < 50.5%** — V33.341 의 자 교정 이후 첫 학습은 아직 안 끝났다
  (자동 재학습 13.8h 전 트리거). 미국 정규장 마감(00:00 UTC) 뒤 야간 파이프라인이
  새 자로 도는 것이 **DNN 층수에 대한 처음으로 공정한 답**이 된다.

---

## V33.346 후속 — ★내가 부스터 학습을 죽였다★ + 자 교정 첫 측정

### 먼저: Codex 가 잡은 결함은 내 것이다
`fix: recover embargo-trained models…`(29437ef) 의 1번 항목:

> V33.341 의 엠바고 분할은 학습 인덱스를 바꿨는데 **크기가중 배열은 옛 연속 경계로 잘린 채** 남았다.
> 합성 5,000행으로 실제 전처리를 돌려 재현: 학습행 3,900 vs 옛 가중치 4,000.

```python
Xtr, Ytr = Xs[_tri], Ys[_tri]      # ← V33.341 에서 내가 바꿨다(엠바고, 비연속)
Wtr, Wva = W[:-nval], W[-nval:]    # ← ★그대로 뒀다★ (연속)
```
길이가 어긋나면 `xgb.DMatrix(..., weight=Wtr)` 가 던진다 → **XGB·LGB·CAT 학습이 통째로 죽는다.**
실제로 그랬다: 오늘 01:31 실측에서 **세 모델 모두 featVer 15** 다(현재 판 17). V33.341 배포
이후로 한 번도 재학습하지 못했다.

**내 검증의 구멍**: 게이트를 돌연변이 6종까지 돌렸지만 전부 *소스 문자열*과 *분할 함수 자체*만 봤다.
**분할 결과를 쓰는 하류 코드와의 정합성**은 한 번도 안 봤고, 트레이너 전처리를 실제로 돌려보지도 않았다.
Codex 의 `check-recovery-provenance.mjs` 는 `assert Wtr == W[_tri]` 를 실제 실행으로 확인한다 —
그게 옳은 방식이다.

### 자 교정 이후 첫 측정 (2026-09-11 01:31 UTC)
| 모델 | featVer | valAcc | accLB | 상태 |
|---|---|---|---|---|
| MIND | 17 | 0.5169 | **0.5052** | full(문턱 0.505 턱걸이) |
| GBDT | 17 | 0.5230 | **0.5110** | full · wGbdt 0.5174 |
| DNN | 17 | 0.5070 | **0.4970** | 미신뢰(wDnn 0) |
| XGB/LGB/CAT | **15** | — | — | ★위 버그로 재학습 실패★ |

**DNN accLB 추이: 45.66%(9/10 04:07) → 49.70%(9/11 01:31), +4.04%p.** 문턱까지 0.8%p 남았다.

### ★이 숫자를 과하게 읽지 않는다★
"유효표본이 자의 길이 때문에 10배 차이였다" 는 내 가설을 **지지하는 방향**이지만 **증명은 아니다**:
- 같은 기간에 표본 풀이 **324,821 → 988,239(3배)** 로 늘었다(featVer 이관 완료, `data.stale = 0`).
  개선이 자 교정 덕인지 표본 덕인지 이 관측만으로는 못 가른다.
- **결정적 비교가 아직 없다**: 부스터가 엠바고를 받은 뒤 accLB 가 ★내려가는지★ 를 봐야
  "종전 값이 누출로 부풀어 있었다" 가 확인된다. 그 학습이 내 버그로 못 돌았으므로 **미측정**이다.
→ 다음 학습(부스터 3종이 featVer 17 로 올라오는 시점)이 진짜 검증이다.

### 잘 도는 것 (실측)
- `pipe.closed=false · stagesToday=44 · waitingN=1 · stagesOld=0 · lastScanMin=94.5`
  → 44단계 끝났고 1단계(`⟳ [ANLREVK] 개정 원장 없음 — 대기`)가 대기 중이라 **그날이 안 닫혔다.**
  V33.342~344 가 하려던 그 동작이 프로덕션에서 확인됐다.
- `committee_cal featVer 불일치` 경고 없음 — 17 유지.
- v7 문구가 V33.345 대로: "응답은 하는데 종목을 하나도 안 준다(예외 없음 · 파싱 0건)".

### 다음 도구에게
- **부스터 3종이 featVer 17 로 올라오면 accLB 를 기록할 것.** 내려가면 종전 값이 누출로 부풀어
  있었다는 증거이고, 그대로면 엠바고 누출은 이 데이터에서 작다는 뜻이다(내 시뮬레이션은 0.14%p 였다).
- DNN 층수(은닉 10층)는 **사용자 지시**다. 임의로 바꾸지 말 것. V33.204 기록은
  "10층 49.3% vs 워커 폴백 2층 53.3%" 이고, 자가 같아진 지금 49.70% 다 — 판단 재료는 쌓이는 중이다.

## V33.399 — 달력 칸이 학습구간에서 상수였다(분포이동 개혁)

출발점은 2026-09-20 회차 로그의 `[분포이동]` 한 줄이다. 진단은 V33.392 가 이미 심어 뒀고
회차 로그에 찍혀 있었는데 **아무도 안 읽었다**:

```
[분포이동] 평균|z| 학습 0.494 → 검증 0.950 (×1.92) · 포화율 0.04% → 1.60%
           포화 상위: fomcTo(15.9%) fomcSince(13.8%) opexQuad(13.5%) opexToNext(12.9%) sectorBeta(11.7%)
```

- **AO-1** FOMC 표가 2021-01 시작인데 표본 바닥은 ≈2020-05 — **258일이 `fomcTo=0·fomcSince=0`**.
  표 안에서 `fomcTo` 는 절대 0 이 안 되므로(`next > t`) 0 은 "모른다" 전용 값인데
  학습기는 **수치 0** 으로 표준화한다. → 2020 추가(긴급 2회는 **긴급이라고 선언**,
  열리지 않은 2020-03-18 정례는 **넣지 않는다**).
- **AO-2** `check-calendar-feats` 가 표의 **만료(미래)만** 지키고 **시작 쪽은 안 봤다**.
  수확기는 `range=max` 라 바닥이 매일 내려간다 → `CAL_COVER_FROM` 선언 + 하한/구멍/센티널 대칭 검사.
- **AO-3** V33.341 의 손목록 중립화를 **측정으로** 바꿨다 — 학습 최빈 ≥90% **그리고**
  검증 포화 ≥5% 인 칸만. **σ 를 키워** 중립화한다(업로드된 자를 워커도 쓰므로 서빙까지 같이 중립).
  `Xn` 만 덮으면 **고치려던 train/serve 스큐를 새로 만든다** — 게이트가 그걸 막는다.
- **AO-4** `check-cal-backfill` ⑥ 이 파이썬에서 달력 이름을 **말하는 것조차** 금지하고 있었다.
  금지 대상은 **계산**이지 **언급**이 아니다 → ⓐ날짜 리터럴 ⓑ요일 산술 ⓒ**칸에 값 쓰기** 만 막는다.

**다음 회차가 답할 숫자**: `[달력결측]`(학습·검증 `fomcKnown=0` 비율) · `[달력분포]`(칸별 σ·최빈) ·
`[퇴화칸]`(실제로 무엇을 껐나). AO-1 이 전부였다면 학습 결측이 ~0% 로 떨어지고 `[퇴화칸]` 이 빈다.
**안 떨어지면 원인은 표 커버리지가 아니라 `obsTs` 배선이다 — 다음 고침은 거기다.**

라벨 실험대에는 축을 하나 더 넣었다(A′ 운영전처리 / A″ 운영가중 / A‴ 둘 다 / C2 시장별 횡단면).
운영 앙상블 초과 **+0.17%p** vs 실험대의 평범한 LightGBM **+1.46%p** = **8.6배**를 가르기 위한 것이다.
**C(횡단면)는 운영 라벨로 올리지 않았다** — V33.78 이 의도적으로 버린 `alpha` 와 같은 부류다
(자세한 근거는 `docs/OPEN-DEFECTS.md` V33.399 절).

검사: 154종 전부 통과 · 돌연변이 달력 6/6 · 퇴화칸 6/6 · 파이썬금지 3/3.

## V33.400 — 측정이 내 가설을 자릿수째로 뒤집었다

V33.399 가 심은 진단이 다음 회차에서 바로 답했다(run 35547364406):

```
[달력결측] fomcKnown=0 비율 — 학습 ★99.88%★ · 검증 52.50%
[달력분포] opexToNext σ0.568/최빈100% · fomcTo σ0.886/최빈100% · …6칸 전부 최빈 100%
```

**AO-1 의 추정(258일 ≈ 11%)이 틀렸다 — 실제는 학습행의 99.88% 다.**
원인이 FOMC 표가 아니라는 증거가 같은 줄에 있다: ★opexToNext 도 최빈 100%★ 다.
`_opexCtx` 는 표 없이 ts 만으로 계산된다 → 상수라면 원인은 ★수확 당시 obsTs 부재★ 다.

- **AP-1** 저장된 120만 행의 달력 6칸을 ★내보내기 직전에 ts 로 다시 각인★ 한다.
  같은 `_calFeats` · 멱등 · ts 없으면 기권 · **D1·R2 두 경로 모두**(한쪽만 고치면 출처가 갈라진다).
- **AP-2** 선점(preemption) 재시작이 ★같은 일을 두 번★ 했다 — `last_ok`·`stage_cost` 를
  회차 맨 끝에서만 저장했기 때문. 75분 중 55분을 재작업에 썼고 라벨 실험대가 잘렸다.
  → 단계마다 저장한다.
- **AP-3** `check-cal-backfill` 이 이름 뒤 고정 600자를 봐서 ★다음 함수의 주석★("Date.now() 로
  때우지 않는다")을 위반으로 읽었다. 본문을 중괄호로 떼어 보고 위임처까지 따라간다.
  같은 모양 전수조사 결과 저장소에 이 한 곳뿐이었다.

**AO-3(퇴화칸 중립화)의 측정된 효과** — 같은 표본·같은 분할, 시드는 오히려 더 적었다:
초과 +0.17%p → **+0.26~+0.31%p(1.8배)** · AUC 0.511 → 0.519 · 블록IC −0.0217 → −0.008.
정직하게: **여전히 승격 문턱 미달**(하한 49.84% vs 50.50%). AP-1 이 되살릴 6칸이 다음 시험대다.

**다음 순서**(같은 로그가 드러냄, `docs/OPEN-DEFECTS.md` V33.400 절에 수치와 함께):
regBear·rs20·sectorRs20·sectorBeta 도 학습 최빈 100%(날짜로 복원 불가 — 수확기에서 고쳐야 함) ·
표본의 81% 가 한국(gbdt_kr t 4.01 승격 / gbdt_us t −0.16 탈락) · GitHub 75분 상한과 Modal
자체 예산이 다른 시계를 본다 · 최근성 손잡이 여전히 죽음 · 실거래 표본 68건.

검사: 155종 전부 통과 · 돌연변이 재각인 8/8 · 재시작 3/3 · 게이트 자해 1/1.

## V33.401 — 신규 위원이 왜 안 서는가 · 승격 후 검증 신뢰도는 왜 무너졌는가

- **AQ-1** `altSampleBackfill` 이 FLOW 표본을 `[peer 6칸, 0,0,0,0,0,0,0]` 으로 적는다 —
  ★13칸 중 7칸이 하드코딩 0★. 진짜 값은 실거래에만 들어가는데 실거래는 120만 중 ★68건★.
  학습엔 상수, 서빙엔 라이브 값 → flowScore 가 ±4 클램프라 들어오는 즉시 슬램한다.
  V33.400 이 Modal 에 넣은 퇴화칸 방어가 워커엔 없었다 → 같은 σ 경로로 중립화.
  (저장된 std 를 채점기가 그대로 쓰므로 학습·홀드아웃·서빙이 한 번에 중립)
- **AQ-2** 서로 다른 표본 825,027/1,200,068 = ★31.3% 중복★. 위생 검사가 세기만 하고
  문턱(50%)에 안 걸려 통과시켰다. 중복은 featVer 승격이 부른 전량 재수확 탓에
  ★최근 구간 = 홀드아웃★ 에 몰린다 → 고유도 0.031 → Wilson 하한 −0.89%p.
  ★"피처판 올린 뒤 검증 신뢰도 붕괴" 의 기계적 원인이 이것★ → 완전히 같은 행만 버린다.
- **AQ-3** [과적합진단] 이 바로 위 [기저율] 의 경고를 어기고 날것 정확도를 비교했다.
  다수클래스가 학습 55.61% / 검증 50.42% 로 다르다 → 초과로 읽으면 실력 격차 +2.92%p.
  판정이 "과적합(규제↑)" → "신호·피처·라벨 품질이 병목" 으로 ★뒤집힌다★.

**게이트가 내 버그를 잡았다**: 포화율을 i=ntr 부터 쟀는데 ntr~nvalStart 는 퍼징으로
잘라낸 ★학습행★ 이다(V33.155). check-purge 가 잡았고 nvalStart 로 고쳤다.

**정직하게 — 안 한 것**: AQ-1 은 막힌 것을 치운 것이지 성능을 올린 게 아니다.
중립화 뒤 FLOW 는 사실상 peer 6차원 모델이다. XALPHA 는 원인이 다르다 —
소급·라이브가 같은 빌더라 0 오염은 없고, 대신 `daily:` 캐시(≈320봉) 깊이가 천장이고
minPanel 20 을 날짜 게이트가 두 시장 합쳐 본다(V33.178 이 진단만 해 둔 자리). 다음 측정 대상.

검사: 156종 전부 통과 · 돌연변이 워커 6/6 · 트레이너 5/5(실행 검증 포함).

## V33.402 — 사건 하나로 얻은 70.4% 가 t 4.01 을 막고 있었다

**AR-1** `gbdt_kr` 하한 52.93% · KR 194,212행 · 블록IC t ★4.01★ · trusted·sane 전부 통과인데
`activated: false` 였다. 막은 것은 통합 `gbdt_model` 의 하한 ★70.4%★ — V33.398(AN-1)이
"겹치지 않는 블록 ★1.5개★" 라고 밝힌 그 숫자다. 15일 시장 상승이라는 ★사건 하나★ 가
22블록·t 4.01 을 막고 있었다.
 · 기저율이 다르다(KR 행 vs 전 시장) → ★각자의 무실력 기준점 대비 초과★ 로 비교한다
 · 못 잰 기준이 거부권을 갖고 있었다 → AN-1 규율의 대칭: 못 잰 기준은 ★이겼다고도 안 읽는다★
 · 문턱은 한 글자도 안 내렸다. 진짜로 나은 통합 모델은 여전히 막는다(게이트가 확인).
 · 판정을 순수 함수 `_mktBeatsPooled` 로 빼서 게이트가 ★돌려 본다★.

**B-3 는 이미 해결돼 있었다(V33.355).** 소스 주석과 OPEN-DEFECTS 가 같이 낡아 있었고,
그 낡은 경고를 믿고 같은 고침을 한 번 더 넣을 뻔했다 — 그랬다면 ★이중 처벌★ 이 된다.
실행 검증: fwdN=399 에서 +0.039 → ×0.5991 · −0.039 → 제외. 음수 경사로는
×0.2188 → ×0.1875 → ×0.125 → ×0 로 단조 감소해 하드 리젝과 이어진다(절벽 없음).
이제 게이트가 그 회귀를 잡는다.

**게이트가 또 내 헐거움을 잡았다** — B-3 회귀를 fwdN 한 점으로만 봐서 frac 상수화 돌연변이를
놓쳤다. 계약은 부호만이 아니라 ★단조성★ 이다. 다섯 점으로 경사로 모양을 본다.

검사: 157종 전부 통과 · 돌연변이 6/6.

## V33.403 — XALPHA·FLOW 소급이 닿지 못하는 과거

**구조적 사실**: 소급은 패널을 `daily:` 캐시(≈320봉)로 만든다 — 배치당 D1 1회라는 ★의도된
비용 설계★ 다(deep history 는 종목마다 R2 읽기라 워커 예산에 안 맞는다). 그런데 ml_samples 는
★2,320일★ 을 덮는다. 그 차이만큼의 과거는 우주가 얇아 통째로 건너뛰고 커서는 전진한다
= ★영구 손실★.

- **AS-1** 그 손실을 아무도 세지 않았다("건너뜀 N" 뿐) → 닿은 날/못 닿은 날·손실 행수·
  우주 폭·★닿은 가장 오래된 날짜★ 를 센다. 문구도 "영구손실" 이라고 적는다.
- **AS-2** ★닿지 않는 날짜가 배치 예산을 먹었다.★ 종전엔 가장 오래된 6일을 ★잘라 놓고★
  돌았는데, ml_samples 의 id 는 ts 순이 아니라(캐치업이 옛 봉을 나중에 적재) 닿지 않는
  옛 날짜만 들어오면 ★그 회차가 0건으로 끝난다★ → 전부 훑되 ★실제로 처리한 날짜만★
  예산에 센다(훑기 상한 _bDates×10). V33.187 의 커서 전진 규칙은 그대로다.

**게이트를 뜻으로 다시 적었다**: check-xalpha 가 "루프 머리 80자 안의 break 에 _dDone > 0 이
있다" 로 보고 있었는데 그건 break 가 하나뿐일 때만 맞는 대리 지표다. AS-2 가 예산 가드를
앞에 넣자 멀쩡한 코드가 실패로 읽혔다 → 루프 머리의 ★모든★ 이탈 조건을 뽑아 초기값에서
전부 거짓인지 ★실제로 계산★ 한다. 앞으로 어떤 가드가 붙어도 같은 규칙으로 검사된다.

검사: 158종 전부 통과 · 돌연변이 소급 7/7 · xalpha 3/3.

## V33.404 — 지수·섹터를 봉 수로 맞추고 있었다 (달력이 다른데)

Modal 의 [퇴화칸] 이 rs20·sectorRs20·sectorBeta 를 "학습 최빈 100%" 로 찍었다. 그런데
수확기는 idxCloses·sectorCloses 를 ★넘기고 있었다★. 폴백값을 실제로 재 보니 서명이 나왔다:
  없을 때 rs20·rs60·corrIdx·rsiRel·idxTrend·idxVol·idxMom20·sectorRs20 → 0
          ★betaIdx·volRatioRel·sectorBeta → 1★ · idxRsi → 0.5
★베타 1 은 실제로 나올 수 있는 값★ 이라 "모른다" 와 구별되지 않는다(fomcTo=0 과 같은 충돌).

**AT-1** "봉 i = 지수 끝에서 (L−1−i)봉 전" 은 두 계열이 ★같은 거래일 집합★ 일 때만 참이다.
KR 종목 딥이력은 네이버·^KS11 은 야후라 길이가 안 맞고(V32.5) 휴장일도 어긋난다.
어긋나면 옛 봉일수록 idxEnd 가 0 이하로 내려가 지수 상대 피처 13칸이 통째로 폴백된다.
게다가 딥이력이 싣고 오는 days 를 ★캐시가 버리고 있었다★ → days 를 함께 싣고 ★날짜로★
찾는다(_altBarIdx — 소급생성과 같은 함수). days 없는 옛 캐시는 봉 수 세기로 떨어진다.

실행 확인(종목 1,600봉 vs 지수 1,200봉): 봉 수 세기는 옛 봉 11개 지점에서 무너지고,
날짜 정렬은 33개를 찾아낸다. ★룩어헤드 0★ — 미래 지수 봉을 고른 적이 없다.

게이트가 또 내 헐거움을 잡았다: 계수 보고를 ★이름 존재★ 로만 봐서 증가문을 지운
돌연변이를 놓쳤다(출력에 이름이 남아 통과 — 그러면 로그는 영원히 0 을 찍는다).
★증가시키는 곳★ 과 ★출력하는 곳★ 을 따로 본다.

검사: 159종 전부 통과 · 돌연변이 7/7.

## V33.405 — 실험대가 8.6배를 분해했다 · 그리고 DNN 은 더 나빠졌다

회차 35561549153(52분·선점 없음). ★나쁜 것부터★:

DNN 초과 +0.31%p → ★−0.71%p★ · AUC 0.519 → 0.512 · 하한 49.84% → 48.85% (시드 3→2).
시드만으로 설명 안 된다. 교정된 진단이 성격을 바꿔 말한다 —
  [과적합진단·초과] 학습 +0.53%p(★균등 −5.65%p★) vs 검증 −0.71%p → 실력 격차 +1.24%p
  → 과적합이 아니라 ★과소적합★ 이다(자기 학습집합조차 다수클래스만 못하다).

앞선 고침은 전부 먹혔다(측정):
  AP-1 달력 — fomcKnown=0 학습 99.88% → ★0.00%★ · fomcTo σ0.886/최빈100% → σ13.283/최빈7%
  AO-3 퇴화칸 — 9칸(달력 4칸 포함) → ★4칸(달력 0칸)★
  AQ-2 중복 — ★114,461건 버림★ · 고유도 0.032→0.035 · 동시성 32.7→28.4
  AQ-3 진단 — "과적합(규제↑)" → "과소적합 쪽" 으로 뒤집힘
  MIND — ★activated: true★ (valAcc 51.57% 하한 50.5%) · 낡은 의자 0.629 가 교체됐다

★실험대가 8.6배를 분해했다★:
  A  운영(sign)·가중=고유도만  +2.26%p / IC 0.0663
  A′ +운영전처리              +2.40%p / 0.0712   ← ★전처리는 무죄★
  A″ +운영가중                +1.21%p / 0.0482   ← ★가중이 절반을 먹는다★
  A‴ +둘 다                   +1.56%p / 0.0593   (운영 DNN 은 −0.71%p → ★남는 2.27%p 는 모델★)
  C  횡단면(당일중앙)         +2.03%p / 0.0539
  C2 횡단면(시장별)           ★+0.89%p / 0.0199★ ← ★C 를 죽였다(시장 섞임 artifact)★
  D  변동성정규화+데드밴드     +3.12%p / 0.0872   ← 여전히 최강(ATR 칸 선행조건)

AU-1 가중의 범인은 ★|pnl| 크기 가중★ 이다. 같은 로그가 나머지를 지운다 —
  [최근성] 100% 가 바닥(상수) · 실거래 68/1,085,607(상수). 상수 배수는 가중적합을 못 바꾼다.
  부호 분류인데 크기 가중은 "많이 움직인 표본"(=가장 예측 어려운 구간)에 최대 10배 발언권을 준다.
  → 손잡이로 빼고 ★기본 끔★. 워커가 유일한 출처(LUXML.pnlWeight), === true 로 내려보낸다.
  다음 회차의 ★A⁗ +크기가중만★ 이 이 판단을 측정으로 확인한다.

아직 안 풀린 것: gbdt_kr 은 이번엔 ★정당하게★ 미승격(t 1.40 < 1.65) — AR-1 은 발동 안 했다.
앞 회차 t 4.01 → 이번 1.40, trees 93 → 20 으로 ★회차 간 진동이 크다★. gbdt_us 는 t −1.35(역방향).

검사: 160종 전부 통과 · 돌연변이 7/7.

## V33.406 — 신규 위원이 전진검증을 ★구조적으로★ 못 모으고 있었다

화면 실측(2026-09-22 08:09)이 답을 통째로 들고 있었다:
  FLOW   전진 0/400 · ★고른 행 0★ < 배치하한 30
  XALPHA 전진 0/400 · ★고른 행 0★
  STACK  전진 0/400 · (2026-09-17 이후 · ★과거표본 3000건 제외★)  ← 행은 있는데 전부 과거
  MEMO   전진 ★16,086★/400   ← ml_samples 를 직접 읽는 쪽은 찬다

AV-1 전진검증 조건은 `id > 체크포인트 AND ★ts > 학습셋 최대 관측시각★` 인데,
  altSampleBackfill 은 원본 행의 ★과거 봉 날짜★ 를 물려준다(V33.173 — 그 고침은 옳다).
  두 규칙이 만나면 새 표본의 ts 가 언제나 과거라 전진창을 ★영원히★ 못 채운다.
  신선한 ts 는 실거래 청산뿐인데 전 시스템 68건이다. ★문턱이 아니라 생산 구조다.★
  → 소급 커서(과거)와 프런티어(최근 30일)를 ★경계 하나로★ 가른다. 겹치지 않아 중복도 없다.

깨뜨리면 안 되는 것(전부 게이트가 잡는다, 돌연변이 8/8):
  ① 프런티어 행이 lastId 를 올리면 안 만든 과거 행을 건너뛴다(id 는 ts 순이 아니다)
  ② 프런티어를 id 커서로 거르면 조용히 안 만들어진다
  ③ 과거가 예산을 먼저 먹으면 프런티어가 또 굶는다 — ★지금 상태를 만든 구조★
  ④ 워터마크는 ★날짜 끝★ 으로 올린다(행 단위면 잘린 날의 잔여를 잃는다)
  ⑤ frontierMax < 하루치 → 교착. 5,000 으로 두고 교착이면 소리내어 말한다.

다음 회차가 답할 숫자: ★프런티어 XALPHA +n / FLOW +n★ — 전진검증을 채우는 건 이것뿐이다.

★GBDT 73.7% 는 "억제" 가 아니라 "진짜인지 먼저 가른다"★ (미해결):
  사용자 지적이 옳다. 갈라야 할 것이 둘인데 내가 뭉뚱그렸다 —
    짧은 ★학습★ 창(최근만 학습) → 진짜 이득일 수 있다(Modal 은 최근성이 죽어 6년 균등)
    짧은 ★평가★ 창(9,000행 ≈ 1.5블록) → 반드시 부풀린다
  그리고 코드 결함을 찾았다(아직 안 고침): V33.398 의 블록 최소값이 ★τ* 캘리브레이션에 덮인다★.
  min 을 먼저 적용한 뒤 τ* 단계가 `if (accLBC > accLB) accLB = accLBC` 로 행 수 기반 하한으로
  되올린다 — 그래서 배포했는데도 70.1% 가 살아 있다. ★순서가 틀렸다.★

검사: 161종 전부 통과 · 돌연변이 8/8.

## V33.407 — AI 가 못 설 때 규칙엔진이 매수하고 있었다 (죽은 스위치)

사용자 관측: "rule 이랑 scalp 이 AI 없는 규칙 버전이 작동해서 처참한 성적."

AW-1 AI_PARAMS.autonomy.emergencyFallback 의 주석은 "false 면 미준비 시 신규진입 관망"
  이라고 ★동작을 약속★ 하는데 ★읽는 코드가 한 줄도 없었다.★ 그래서 AI 가 못 설 때
  규칙엔진이 항상 신규매수를 했다.
  → 배선하고 기본 ★끔★. 판정은 ruleEntryAllowed() 한 곳에서만(메인·CM·대체시장).
    ★scalp 도 같이 막는다★ — AI 가동 시 scalp 를 살리는 전제는 "위원회가 최종 판단" 인데
    AI 가 못 서면 그 전제가 사라진다.
  ★경계가 핵심: 진입만 막고 청산은 한 톨도 안 건드린다.★ 넘으면 폭락장에 못 판다.

AW-2 check-dead-knobs 가 AI_PARAMS 를 통째 면제해 AW-1 을 못 잡았다. 숫자는 구현부를
  비추는 게 정상이지만 ★불리언은 스위치다★ → 불리언만 읽히는지 검사. 즉시 3개 더 찾았다:
    ensembleDynamic(표기용) · krEnabled(표기용 — ★false 로 바꿔도 안 꺼진다★) · gnnEnabled(미구현)

한국장 거래 부재 — ★아직 원인 미확정★. 셋을 배제했다:
  krEnabled(한국어 감성 스위치, 거래 무관) · autoDisable(전략별이지 시장별 아님) ·
  유동성 하한(달러 환산 시 KR 이 더 낮다).
  → [EVAL] 에 시장별 깔때기를 붙였다: 평가 N → 후보 C(신호 S) → 체결 B · AI픽풀 P
    ★후보 0 = 상류(신호·AI픽·커버리지) / 후보는 있는데 체결 0 = 하류(위원회·사전검사·예산)★
    처방이 정반대라 이 구분이 핵심이다. 다음 KR 사이클이 한 줄로 답한다.

아직 안 한 것(다음 판): ①재학습이 좋은 모델을 버리는 문제 ②현재 신뢰도로 다음 학습 때
살릴지 정하는 시스템 ③GBDT 의 τ* 가 블록 하한을 되올리는 순서 결함.

검사: 162종 전부 통과 · 돌연변이 7/7.
