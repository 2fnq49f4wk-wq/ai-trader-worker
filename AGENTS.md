# 공통 AI 작업 규약

> ## ⚠️ 먼저 읽을 것 — 미해결 결함 목록
> **`docs/OPEN-DEFECTS.md`** 에 현재 미수정 결함이 전부 정리돼 있다 (조사: Claude, 2026-09-11).
> 사용자 지시로 **조사만 하고 수정하지 않은** 목록이다. 새 작업을 시작하기 전에 이 파일을 읽고,
> 지금 손대는 영역에 해당 항목이 있는지 먼저 확인한다.
> - **치명**: `A-1`(시간외 안전장치 4개가 실제 경로에 안 걸림) · `B-1`(부스터 학습 전멸, 수정됨/결과 미확인)
> - **돈이 직접 틀리는 것**: `D-2`(한국 증권거래세율이 2년째 법정세율과 다름 — 2026년은 0.20%인데 코드는 0.18%)
> - **날짜가 오면 터지는 것**: `A-8`(미국 조기폐장 2026-11-27·12-24, 한국 수능일 2026-11-19 를 시스템이 모른다)
> - 그 파일의 마지막 절 `Z. 확인했으나 문제 아님` 을 먼저 보면 헛수고를 줄일 수 있다.
> - 항목을 고쳤으면 **그 파일에서 해당 항목을 지우거나 '해결됨 + 커밋해시'로 표시**한다.

이 파일은 Claude Code, Codex 및 다른 자동화 도구가 공유하는 단일 작업 규약이다.

## 작업 시작

1. `git status --short --branch`, `git log -5 --oneline`을 확인한다.
2. `AI_HANDOFF.md`를 읽고 `Status`, `Branch`, `Last commit`이 실제 Git 상태와 맞는지 확인한다.
3. 다른 AI의 미완료 작업이 있으면 새 구현을 시작하지 말고 해당 커밋과 남은 작업부터 검증한다.
4. 이 저장소는 사용자 지시에 따라 `main`에서 직렬로 작업한다. 같은 작업 트리에서 두 AI를 동시에 실행하지 않는다.

## 체크포인트와 인계

- 토큰이 부족해지거나 작업을 중단하기 전, 빌드 가능한 최소 단위로 커밋한다.
- 빌드 불가능한 상태는 커밋하지 않는다. 불가피하면 변경 파일과 정확한 실패 원인을 인계 문서에 적는다.
- `AI_HANDOFF.md`에는 추측이 아니라 실행한 명령, 결과, 남은 작업, 되돌릴 위험을 기록한다.
- 새 담당자는 이전 결과를 신뢰만 하지 말고 기록된 검사를 다시 실행한다.
- 완료 커밋에는 코드와 인계 문서 갱신을 함께 포함한다. 비밀키·토큰·Cloudflare secret은 기록하지 않는다.
- 다른 AI를 시작하기 전에 현재 AI 작업을 끝내고 `main` 커밋 및 `AI_HANDOFF.md` 갱신까지 완료한다.

## Cloudflare 안전 규약

- `main` push는 프로덕션 Worker 자동 배포임을 전제로 한다.
- `wrangler.toml`, D1/R2 바인딩, cron, 모델 업로드 경로 변경은 코드 검증과 별도로 명시한다.
- Claude API 호출 비활성화 상수는 사용자가 의도적으로 정한 정책이다. 별도 요청 없이 활성화하지 않는다.
- 모델 변경 전 최소 `node tools/check-model-evidence.mjs`와 관련 계약 검사를 실행한다.
